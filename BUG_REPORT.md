# MyExpenseApp 生产级问题报告

> 分级口径：Critical = 可直接造成大范围敏感数据泄露/不可逆损坏；High = 可造成用户数据越权、丢失、篡改或核心账务错误；Medium = 明显功能错误、可靠性或扩展性问题；Low = 局部体验、维护或防御性不足。  
> “已确认”表示由仓库代码直接证明；“条件风险”表示有效性依赖仓库外配置，报告不会将其写成已被利用。

## BUG-001 持久化 DOM XSS

- 严重等级：High
- 位置：`src/js/render.js:65-82,132`；输入来源见 `src/js/main.js:57-79`、`src/js/quick-add.js:49-76`、`src/js/sync.js:83-96`
- 问题：交易原始值和备注未经 HTML 属性编码就拼接进 `value="..."`、`data-raw="..."`，最后通过 `innerHTML` 重建表格。直接输入或 JSON 导入包含双引号和事件属性的内容，可在月切换/重渲染时形成持久化脚本执行。
- 影响：恶意或被污染的共享数据可以同源身份执行脚本、读取页面财务数据、发起 Firestore 写入并持续感染其他打开该账本的客户端。
- 建议方案：停止用字符串拼接用户数据；使用 DOM API/模板框架并以 `.value`/`.dataset` 赋值；对导入 schema 做严格校验；增加 CSP 作为纵深防御；加入 XSS 回归测试。
- 修复优先级：P0

## BUG-002 云端超时被错误标记为“已同步”并丢失重试数据

- 严重等级：High
- 位置：`src/js/sync.js:29-45`
- 问题：待保存数据在写入前被清空；`Promise.race` 的 3 秒计时器正常 resolve，随后无论 Firestore 是否完成都执行 `updateSyncStatus("synced")`。超时后发生的失败不会进入当前 `catch`，也不会把数据合并回 pending。
- 影响：用户会在数据尚未服务端确认时看到“已同步”；允许切年/离开页面；网络异常时存在未保存修改无法重试和静默丢失风险。
- 建议方案：同步状态必须由 Firestore 写入确认驱动；引入明确的 queued/saving/synced/error 状态机、写入序号和可恢复 outbox；超时只改变 UI 为“仍在等待”，不能当成功；离开前等待所有 pending writes。
- 修复优先级：P0

## BUG-003 仓库无法证明固定共享账只允许两个既有账号

- 严重等级：High（条件风险；最终可利用性依赖仓库外 Security Rules）
- 位置：`src/js/sync.js:36,51,94`；`src/js/auth.js:31-54`
- 问题：固定共享路径符合产品设计，但账号与线上 Rules 配置不在仓库。应用内注册入口已由 T010 移除；仍无法仅凭仓库证明线上规则只允许两个既有账号，或确认第二账号实际是只读还是可写。
- 影响：配置错误时第三方已登录 UID 可能读取或覆盖私人财务数据。风险来自授权规则是否精确，而不是缺少多家庭数据模型。
- 建议方案：导出并人工核对线上 Rules；分别记录女朋友账号和项目所有者账号所需权限；用假 UID 在 Emulator 重现同一契约，并拒绝第三 UID/匿名访问。不要引入 Household、邀请或角色系统。
- 修复优先级：P0

## BUG-004 多终端并发采用无版本的最后写入覆盖

- 严重等级：High
- 位置：`src/js/state.js:22-42`；`src/js/sync.js:29-63`；`src/js/quick-add.js:58-72`
- 问题：快速追加是“读取旧公式 → 本地拼接 → setDoc”；没有事务、版本号、幂等操作 ID 或冲突检测。实时快照又整体替换本地三张 map。两个终端同时追加同一日同分类时，后写者可覆盖先写者；settings 保存还会复制整个当前 settings 快照。
- 影响：女朋友在多设备使用，或项目所有者查看期间发生并发写入时，金额或设置可能被静默覆盖，且没有交易日志可恢复。
- 建议方案：用独立 transaction 文档和不可变 ID 追加，不在客户端做聚合字段读改写；聚合用查询或受控汇总；过渡期使用 Firestore transaction/version precondition 并显示冲突。
- 修复优先级：P0

## BUG-005 使用浮点数和公式字符串保存金额

- 严重等级：High
- 位置：`src/js/utils.js:10-23,34-42`；`src/js/main.js:61-67,99-109`；`src/js/quick-add.js:52-68`；`src/js/budget.js:96-123`
- 问题：金额用 JavaScript `Number` 计算，CNY×汇率可能产生小数误差；VND 只在显示时四舍五入，底层仍可保存小数与表达式。输入合法性失败时 `safeEval` 可能退回 `parseFloat`，导致“100abc”按 100 计算但原字符串仍被持久化。
- 影响：累计、预算、资产对账和导出结果可能出现不可见小数误差或“显示值与原值不一致”，无法满足财务数据确定性。
- 建议方案：以 VND 整数保存唯一事实金额；禁止把表达式或 CNY 展示值作为事实数据；在输入边界一次性解析并返回明确 validation error。只有 T019 明确选择独立交易迁移后，才设计新的持久字段。
- 修复优先级：P0

## BUG-006 JSON 导入可覆盖整本账且缺少 schema、安全与大小校验

- 严重等级：High
- 位置：`src/js/sync.js:82-100`
- 问题：只检查顶层 `entries` 是否存在，就把任意 JSON 以 `merge:false` 覆盖当前年度文档；未校验版本、年份、字段、类型、金额范围、文本长度、危险内容或文件大小，也没有导入前快照与事务性恢复。
- 影响：选错文件、畸形数据或恶意文件可永久覆盖账本、触发 XSS、写入不可计算字段，或因文档过大导致失败。
- 建议方案：定义版本化 JSON Schema；先解析到隔离预览并给出差异；限制大小/条目/字符串；服务端或事务内导入；覆盖前自动备份并提供恢复点。
- 修复优先级：P0

## BUG-007 连续记账长期停留 1 天，奖励无法触发

- 严重等级：High
- 位置：`src/js/main.js:57-83`；`src/js/quick-add.js:85-91`；`src/js/render.js:151-233,258-280`
- 问题：已由用户复现为“不管怎么记都显示 1 天”。代码存在四个直接原因：①直接编辑表格完全不调用 `updateStreakAfterRecord()`；②快速记账入口先用 `localStorage.expense_last_date` 判断，`incrementStreak()` 却优先读取云端 `settings`，一次操作使用两个不同事实源；③ `expense_streak/expense_last_date` 是可覆盖的计数状态，实时快照、待提交写入和跨设备本地缓存可能互相覆盖；④快速记账忽略所选业务日期，只按设备当前日期打卡。
- 影响：连续天数不能表达女朋友的实际记账行为；7 天和 30 天判断 `newStreak === 7/30` 永远难以达到，奖励逻辑失效；历史补录、直接录入和快速录入结果不一致。
- 建议方案：近期先从当前年度 `entries` 的有效记账日期派生 streak，统一直接编辑和快速记账后的刷新入口，注入越南账本时区与 clock；不再把 `expense_streak` 数字作为事实源。旧 settings 仅用于兼容展示，验证后停止写入但暂不删除。后续若切换独立 Transaction，再从未删除交易的 `localDate` 使用同一纯函数计算。
- 修复优先级：P0（下一施工任务）

## BUG-008 页面跨日后“今天”状态不会刷新

- 严重等级：Medium
- 位置：`src/js/config.js:5-8`；`src/js/quick-add.js:18`；`src/js/render.js:47-48`
- 问题：`TODAY`、`CURRENT_MONTH`、`CURRENT_DAY` 在模块加载时固定。PWA 长时间打开跨过午夜/月末/年末后，今日行、快速记账默认日期和初始月份仍使用旧日期，直到刷新页面。
- 影响：用户可能把新一天的交易录入前一天，或看不到正确的“今日”定位。
- 建议方案：用可注入 Clock 在每次打开快记/渲染时读取当前时间；监听 `visibilitychange` 和日期边界刷新；建立跨午夜测试。
- 修复优先级：P1

## BUG-009 CSV 导出格式错误且存在表格公式注入

- 严重等级：High
- 位置：`src/js/main.js:302-331`
- 问题：导出原始单元格字符串，通常以 `=` 开头；导入或直接输入还可带任意公式前缀。字段没有 RFC 4180 引号/换行转义，备注的 `.replace(/,/g, ",")` 是无操作；“当日总支出”列始终留空。
- 影响：在 Excel 等软件打开时可能执行恶意公式；逗号、引号和换行会破坏列结构；导出的汇总字段不完整。
- 建议方案：只导出解析后的数值与规范交易字段；所有文本 CSV 转义；对 `= + - @ \t \r` 前缀做安全处理；补齐日支出；用含中英文、逗号、换行和攻击载荷的测试验证。
- 修复优先级：P0

## BUG-010 非法 JSON 结构会令导入 Promise 永久 pending

- 严重等级：Medium
- 位置：`src/js/sync.js:84-99`
- 问题：JSON 可解析但没有 `entries` 时，`reader.onload` 既不 resolve 也不 reject。
- 影响：调用方永久等待，用户无任何错误提示，文件控件也不会进入明确终态。
- 建议方案：所有分支显式返回结构化结果；缺字段立即抛出 schema validation error；增加异步完成性测试。
- 修复优先级：P1

## BUG-011 单年度单文档存在硬容量与写热点上限

- 严重等级：High
- 位置：`src/js/sync.js:36,51,94`；实际结构见 `state.js:18`
- 问题：所有年度 entries、备注、余额、预算、streak 放入一个 Firestore 文档。Firestore 单文档有硬大小限制；两名使用者也争用同一写热点。当前没有大小监控、分片或归档。
- 影响：备注和公式增长后年度账本会突然无法写入/导入；未来新增标签、附件、审计日志不可能安全塞入现结构。
- 建议方案：在固定共享账本命名空间下拆分 account/transaction/category/budget 集合；一笔交易一文档；分页查询；聚合另存受控摘要并可重建，不引入多家庭租户层。
- 修复优先级：P1

## BUG-012 年份切换范围固定且可能清掉未确认写入

- 严重等级：Medium
- 位置：`src/js/main.js:40-44,262-284`；`src/js/sync.js:38-40`
- 问题：选择器只生成当前年 -2 到 +3，其他已有年度无法进入。切年只看 `state.isSaving`；BUG-002 的超时会把它提前置为 false，随后切年会清空 pending 和 appState。
- 影响：较老账本在 UI 中不可访问；写入仍未确认时可丢失本地恢复状态。
- 建议方案：年份来自账本数据索引并允许创建/归档；切换前等待 outbox 清空或让 outbox 按 ledger/year 独立持久化。
- 修复优先级：P1

## BUG-013 会话超时在页面重载时被重置

- 严重等级：Medium
- 位置：`src/js/auth.js:22-27,31-37,84-99`
- 问题：初始化时 `checkSessionTimeout()` 因 `state.currentUser` 为空直接返回；Firebase 恢复用户后先 `updateActivityTime()`，覆盖旧时间。用户在闲置超过 20 分钟后刷新页面不会被超时退出。
- 影响：“20 分钟无操作退出”不是跨页面生命周期的可靠安全控制，容易给用户错误安全预期。
- 建议方案：恢复认证状态后先比较旧活动时间再决定是否退出，验证通过后才更新时间；同时说明 Firebase token 生命周期与本地 UI 锁定的区别。
- 修复优先级：P1

## BUG-014 CNY 辅助显示的汇率源缺少超时与可信时间

- 严重等级：Medium
- 位置：`src/js/auth.js:68-80`；`src/js/main.js:148-179`
- 问题：页面初始化直接请求第三方 `@latest` JSON，无 AbortSignal 超时，也不展示汇率时间/来源。最新产品规则已经确认 CNY 只是查看换算，因此不需要历史交易汇率快照，但必须确保切换显示不会改写 VND raw/state/pending 数据。
- 影响：网络慢会长期显示加载中；项目所有者无法判断 CNY 是实时值还是缓存值；若显示转换边界混入输入/保存路径，可能污染 VND 事实。
- 建议方案：建立只读 FX ViewModel；增加超时、响应校验、最后有效缓存、来源和更新时间；服务失败时仍正常显示/录入 VND；加入切换前后持久状态深度相等测试。
- 修复优先级：P1

## BUG-015 预算剩余天数对非当前月份语义错误

- 严重等级：Medium
- 位置：`src/js/budget.js:72-78`
- 问题：查看任何历史或未来月份时，`remainingDays` 都被设为该月总天数；历史月份仍显示“剩 N 天/日均可用”，未来月份也未按“尚未开始”表达。
- 影响：预算建议对非当前月具有误导性，可能导致错误消费判断。
- 建议方案：当前月计算剩余自然日；过去月显示最终结果；未来月显示预算计划与开始日期；用时区化日期服务测试三种状态。
- 修复优先级：P2

## BUG-016 缺少输入域约束，负数、异常文本和超长备注可进入核心数据

- 严重等级：Medium
- 位置：`src/js/main.js:57-79`；`src/js/quick-add.js:49-57`；`src/js/utils.js:10-23`
- 问题：快速金额只检查 `isNaN`，未要求正数、上限或小数位；直接表格输入没有 schema 校验，`safeEval` 的解析失败结果仍不阻止保存；备注无长度限制。
- 影响：负支出会反向增加结余，错误字符串可形成显示/存储不一致，超长文本推动文档超限。
- 建议方案：按领域命令验证类型、符号、范围、精度和文本长度；错误时不修改状态、不排队保存，并显示字段级错误。
- 修复优先级：P1

## BUG-017 注册错误直接显示第三方原始消息

- 严重等级：Low
- 状态：已随 T010 移除应用内注册功能，不再可达；保留为历史审计记录。
- 位置：`src/js/auth.js:111-128`
- 问题：注册失败把 Firebase `e.message` 直接展示给用户，而登录失败做了统一文案。错误信息与产品语言不一致，也可能暴露实现细节。
- 影响：体验不一致，可能辅助账号枚举或泄漏内部错误上下文；具体 Firebase 防枚举配置无法从仓库确认。
- 建议方案：按稳定错误码映射用户文案，详细上下文只进脱敏遥测；启用并验证 Firebase email enumeration protection。
- 修复优先级：P2

## BUG-018 PWA 与发布配置存在双份源文件且无自动校验

- 严重等级：Low
- 位置：根目录 `sw.js`/`manifest.json` 与 `public/sw.js`/`public/manifest.json`
- 问题：两套内容目前相同，但 Vite 构建实际复制 `public` 版本；根目录副本容易被误改。Manifest 名称固定为 2026 年，和动态年份 UI 不一致。
- 影响：版本日志、缓存名或 PWA 元数据可能发生发布漂移；维护者难以判断单一事实源。
- 建议方案：只保留 `public` 为事实源；PWA 元数据使用不带年份的产品名；构建中验证清单和 Service Worker。
- 修复优先级：P3

## BUG-019 全年聚合在每次编辑时完整重算

- 严重等级：Medium
- 位置：`src/js/main.js:80-82`；`src/js/budget.js:95-153`；`src/js/charts.js:117-148`
- 问题：每次输入在 150 ms 后遍历全年所有天和 10 类数据，并在 300 ms 后更新两个 Chart.js 实例；模块还会频繁重建当月整张表。
- 影响：当前固定规模尚可，但增加自定义分类、交易明细或低端移动设备后会产生输入延迟和耗电；构建产物也已有 737 kB 主包警告。
- 建议方案：先用性能基线确认；采用交易查询结果的增量 selector/memoization；按路由懒加载图表与烟花；避免整表 `innerHTML` 重建。
- 修复优先级：P2

## BUG-020 缺少自动化测试、Rules 测试、CI 与可观测性

- 严重等级：High
- 位置：`package.json:6-10` 及全仓库缺失项
- 问题：只有 dev/build/preview 脚本，没有单元、集成、端到端、Firestore Emulator Rules 测试；无 CI、错误监控、结构化日志、数据迁移或回滚验证。
- 影响：金额、安全和同步逻辑无法回归证明；任何重构都可能静默破坏财务数据；线上错误无法量化或定位。
- 建议方案：先建立 Vitest + jsdom、Firebase Emulator、Playwright 和 CI 四层最小基线；核心门禁覆盖金额、权限、并发、导入导出和跨日。
- 修复优先级：P0
