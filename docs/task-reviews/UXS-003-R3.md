# UXS-003 Review R3

## Decision

`CHANGES_REQUESTED`

R3 fixes the R2 active-state defect and supplies six reviewable viewport screenshots. Automated gates pass. Approval remains blocked because the implementation adds a new browser dependency, lock-file churn, and two capture scripts outside the Task's explicitly permitted file list.

## Scope

| Field | Value |
|---|---|
| Base SHA | `a9fc9fcf67e04b15f2ffc99347896f55dbc55bed` |
| Reviewed implementation head | `5db02b7fbde137f4cb653a7e6b2961fd4e99c192` |
| Branch head at review start | `33c7d91` |
| Branch | `task/uxs-003-apple-warm-tokens` |
| Review round | `3` |
| Full implementation range | `a9fc9fc..5db02b7` |
| Incremental rework range | `f5b4ed3..5db02b7` |

## Findings

### Medium - Screenshot capture tooling expands the Task beyond its allowed files

- **Files/lines:** `TASK_PLAN.md:181,189`; `package.json:17`; `package-lock.json:1-122`; `scripts/capture-screenshots.cjs:1-121`; `scripts/capture-screenshots.mjs:1-107`.
- **Evidence:** UXS-003 specifies the complete allowed change set and does not include `package.json`, `package-lock.json`, or `scripts/`. R3 adds `puppeteer@^25.3.0`, 455 lines of lock-file changes, and two overlapping screenshot-capture implementations (CommonJS and ESM). The six PNG evidence assets themselves are required by `TASK_PLAN.md:188,192`; the persistent dependency and scripts are not. This also conflicts with the repository rule requiring Coder to stay within the current Task's exact modified-file list.
- **Impact:** A visual-shell task changes the project dependency graph and leaves duplicate maintenance surface without approved scope. The change is avoidable now that the required evidence assets have been captured.

## Minimum Correction

1. Remove `puppeteer` from `package.json` and revert the corresponding `package-lock.json` churn.
2. Remove both `scripts/capture-screenshots.cjs` and `scripts/capture-screenshots.mjs`. Retain the six committed synthetic screenshot assets and their verification record under `docs/review-evidence/assets/UXS-003/`.
3. Return only the scope cleanup to `READY_FOR_REVIEW`; do not alter approved R3 navigation behavior or begin UXS-004.

## Completion Criteria

| Criterion | Result | Evidence |
|---|---|---|
| Unified tokens, focus ring, reduced motion, safe-area support | PASS | `src/css/app.css:32-79`; recorded in `VISUAL_VERIFICATION.md:23-50`. |
| `<768px` bottom nav, `>=768px` sidebar, never two focusable nav sets | PASS | `index.html:140-165,326-339`; `src/css/app.css:81-95,744-750`; viewport captures at 360, 768, and 1920 personally inspected. |
| Desktop sidebar is a responsive app shell beside main content | PASS | `index.html:140-165` and the 768/1920 captures. |
| Five destinations, one active state, click and keyboard navigation | PASS | `src/js/navigation.js:56-69` reasserts active state after the real view switch; `tests/unit/navigation.test.js:49-112` covers click and Enter with the conflicting active-state behavior present. |
| Existing business DOM IDs/events and state are preserved | PASS | Full diff introduces no budget/entry/sync calculation, Firestore, or business-schema change. |
| No page overflow/content obstruction across six widths, focus/reduced-motion/standalone evidence | PASS | Six PNGs at 360/390/430/768/1440/1920 and `VERIFICATION.md`; existing visual record documents focus, reduced motion, and standalone/safe-area checks. |
| Implementation stays within UXS-003 permitted files | FAIL | Adds `package.json`, `package-lock.json`, and two `scripts/` files not permitted by `TASK_PLAN.md:181`. |
| Implementation remains unmerged from `main` | PASS | `git merge-base --is-ancestor 5db02b7 main` exited `1` (expected). |

## Verification Performed

| Command/check | Exit code | Key output |
|---|---:|---|
| Full `a9fc9fc..5db02b7` diff and incremental `f5b4ed3..5db02b7` diff | 0 | Confirmed R2 active-state repair, six image assets, and unapproved dependency/script additions. |
| Screenshot asset inventory and visual inspection | 0 | Six PNGs exist; personally inspected 360, 768, and 1920 captures for responsive navigation and shell layout. |
| `npm test -- --run tests/unit/navigation.test.js tests/unit/app-shell.test.js` | 0 | 2 files; 12 tests passed. |
| `npm test -- --run` | 0 | 21 files passed / 2 skipped; 150 tests passed / 8 skipped. |
| `npm run typecheck` | 0 | Completed without type errors. |
| `npm run build` | 0 | Vite built 46 modules; known chunk-size warning only. |
| `git diff --check a9fc9fc..5db02b7`; working-tree `git diff --check` | 0 | No whitespace errors. |
| `git merge-base --is-ancestor 5db02b7 main` | 1 (expected) | Implementation is not merged into `main`. |

`npm run test:rules` is not applicable because UXS-003 does not change Firebase Rules.

## Reviewer Boundary

I modified no production code or tests. This review record and the accompanying `TASK_STATUS.md` transition are the only intended changes in the review commit. The Coder must make only the scope cleanup above, return UXS-003 to `READY_FOR_REVIEW`, and must not start UXS-004.
