# T018 Review R2

## Decision

`CHANGES_REQUESTED`

## Scope

- Base SHA: `470c43066906cb4b0856da6f22910d1f46f84f14`
- Previous reviewed implementation: `9aa947b5277448fd30a9732e9b8d320834dfeaff`
- R2 implementation head: `ccd5f40e1fd6f60197a275e3668b1196b6bf8bdc`
- Branch head at review start: `099242a`
- Review round: `2`
- Full implementation range: `470c4306..ccd5f40e`
- R2 rework increment: `9aa947b5..ccd5f40e`
- Evidence/status follow-up range: `ccd5f40e..099242a`

## Findings

### Medium

1. The only verified local recovery record uses the fixed key `myExpenseApp.importRecovery.latest` at `src/js/sync.js:8,189-223`. Every subsequent overwrite replaces that record at `:197`, so the first recovery is no longer locally retained after a second import. The browser download remains only a dispatched `link.click()` at `:207-212`, not the verified persistence boundary. `tests/unit/import-backup.test.js:113-127` tests repeated operations with an injected array-collecting downloader and never exercises the production storage adapter, so it cannot reveal the replacement. Impact: if a browser download is unavailable or lost, repeated overwrite operations do not each retain a verified, recoverable point; this violates T018's repeated-operation completion criterion. Minimum correction: persist and verify each recovery under a distinct key derived from its recovery filename/hash (and document how all retained items are recovered), then test two production-adapter calls prove both verified payloads remain readable and neither overwrite write occurs on persistence failure.

R1's other blocking findings are resolved: the recovery payload is validated before download/write at `src/js/sync.js:245-252`, a locally persisted read-back is required before download dispatch at `:194-203`, and the base SHA in status/evidence is resolvable.

No Firebase deployment/Auth change, production-data operation, production backup collection, CNY/VND change, T011/T012 expansion, or unrelated refactor was found in the reviewed range.

## Completion Criteria

| Criterion | Result | Evidence |
|---|---|---|
| User cancellation performs no read, recovery dispatch, or overwrite | PASS | `src/js/sync.js:243`; `tests/unit/import-backup.test.js:49-57`. |
| Invalid recovery payload blocks download and overwrite | PASS | `src/js/sync.js:245-252`; `tests/unit/import-backup.test.js:69-83`. |
| Local recovery is persisted and read back before overwrite | PASS | `src/js/sync.js:194-203`; `tests/unit/import-backup.test.js:129-162`. |
| Local-persistence verification failure blocks overwrite dispatch | PASS | `src/js/sync.js:195-203`; `tests/unit/import-backup.test.js:164-185`. |
| Write failure occurs only after recovery handling and does not log ledger contents | PASS | `src/js/sync.js:263-271`; `tests/unit/import-backup.test.js:85-96`. |
| Repeated operations each retain a verified recovery point | FAIL | Fixed storage key at `src/js/sync.js:8,197`; repeated test uses only a mock downloader at `tests/unit/import-backup.test.js:113-127`. |
| Hash and schema-valid recovery content are covered | PASS | `tests/unit/import-backup.test.js:43-45,98-110`; recovery schema validation at `src/js/sync.js:247-252`. |
| Base and implementation evidence is reproducible | PASS | `TASK_STATUS.md:13`, `docs/review-evidence/T018.md:7-8`; full base resolves in Git. |
| No unauthorized Firebase/production/T011/T012 scope expansion | PASS | Full diff `470c4306..ccd5f40e`; changed-file inspection. |

## Verification Performed

| Command | Exit code | Key output |
|---|---:|---|
| `npm test -- --run tests/unit/import-backup.test.js` | 0 | 1 file, 9 tests passed. |
| `npm test -- --run` | 0 | 14 files passed, 2 skipped; 106 tests passed, 8 skipped. |
| `npm run typecheck` | 0 | `tsc -p tsconfig.json --noEmit` completed. |
| `npm run build` | 0 | Vite built 41 modules. Existing chunk-size warning was emitted; no build error. |
| `git diff --check 470c4306..ccd5f40e` | 0 | Full implementation range is whitespace-clean. |
| `git diff --check ccd5f40e..099242a` | 0 | Evidence/status follow-up range is whitespace-clean. |
| `git merge-base --is-ancestor ccd5f40e main` | 1 | Expected: the implementation is not merged into `main`. |

## Required Rework

1. Retain and read-back verify a distinct local recovery payload for every overwrite operation; a fixed `latest` key is insufficient.
2. Add production-adapter tests for two successive recoveries proving both verified payloads remain available, plus the corresponding no-overwrite failure path.
3. Keep the rework within T018. Do not deploy Firebase, alter Auth/Rules, write production data, or enter T019.

## Reviewer Boundary

The reviewer modified no business code or tests. This review record and the accompanying `TASK_STATUS.md` transition are the only intended changes in the review commit.
