# UXS-002 Review R6

## Decision

`CHANGES_REQUESTED`

The R5 renderer test is now real, and the i18n icon/streak fixes are relevant to the Task. R6 also changes the CNY FX-mode control's active-state design, which is not a localization correction and is not authorized by UXS-002. The evidence and status counts are additionally stale against the current test run.

## Scope

| Field | Value |
|---|---|
| Base SHA | `460d337018106118b19c74fb3ca66ea33e07fa37` |
| Reviewed implementation head | `b9916cd2d60037902bc0c5497f092e8d996916a3` |
| Branch head at review start | `3a73770` |
| Branch | `task/uxs-002-i18n` |
| Review round | `6` |
| Full implementation range | `460d337..b9916cd` |
| Incremental range after R5 | `874ca16..b9916cd` |

## Findings

### High - FX mode-button visual behavior is unrelated scope expansion

- **Files/lines:** `index.html:95-96`; `src/js/main.js:266-285`; `TASK_PLAN.md:163-171`.
- **Evidence:** R6 adds FX button IDs and changes `changeFxMode()` to replace both controls' classes with amber active/slate inactive visual states. The current Task permits localization, locale persistence, HTML language metadata, and explicitly excludes the FX display paths; it also prohibits design refactoring. This behavior is neither required to preserve i18n icons nor to rerender localized text.
- **Impact:** UXS-002 alters an already approved CNY/FX interaction's design outside its authorized scope, increasing regression surface in a frozen currency-display area.

### Medium - Evidence and status report stale/conflicting test counts

- **Files/lines:** `TASK_STATUS.md:19`; `docs/review-evidence/UXS-002.md:13,31,87-93,149-155`.
- **Evidence:** Current targeted verification reports 20 tests; current full verification reports 19 passed files / 2 skipped and 138 passed / 8 skipped tests. `TASK_STATUS.md` says 137 tests; evidence still calls `i18n.test.js` an 18-test file and retains older 136-test gate rows, even though its latest R6 row says 138.
- **Impact:** The evidence handoff does not give one accurate, current verification count for the reviewed head.

## Minimum Correction

1. Revert only the FX active-state visual change: remove `btn-fx-auto`/`btn-fx-manual` additions and restore the pre-R6 `changeFxMode()` behavior. Retain the necessary `applyI18n()` icon preservation and `renderStreakPanel()` language-switch rerender.
2. Consolidate/update status and evidence with current R6 facts: 20 targeted tests and 138 passed / 8 skipped full tests across 21 files; remove contradictory older summary counts or clearly label historical runs.
3. Re-run the targeted/full/typecheck/build/diff gates and return UXS-002 to `READY_FOR_REVIEW`. Do not start UXS-003.

## Completion Criteria

| Criterion | Result | Evidence |
|---|---|---|
| Vietnamese default, `vi`/`zh-CN` only, persistence and state invariance | PASS | Targeted i18n/budget suite passed: 20 tests. |
| Rendered budget heading is verified in actual DOM across locale switch | PASS | `tests/unit/budget-heading-i18n.test.js` calls `renderMonthTable()` and asserts production DOM. |
| i18n preserves icon children while localizing icon-only controls | PASS | `src/js/i18n.js:75-79`; focused i18n test passed. |
| Locale switch rerenders visible streak text | PASS | `src/js/main.js:208-209` applies rebuild then `renderStreakPanel()`. |
| No unrelated scope/design expansion | FAIL | FX active-state rewrite is outside UXS-002. |
| Evidence/status accurately describe reviewed verification | FAIL | Current test counts conflict. |
| No prohibited schema/Firebase/deployment change | PASS | Full diff review found none. |
| Implementation remains unmerged from `main` | PASS | `git merge-base --is-ancestor b9916cd main` exited `1` (expected). |

## Verification Performed

| Command/check | Exit code | Key output |
|---|---:|---|
| Full `460d337..b9916cd` and incremental `874ca16..b9916cd` diff review | 0 | Confirmed renderer/i18n fixes and unauthorized FX visual rewrite. |
| `npm test -- --run tests/unit/i18n.test.js tests/unit/budget-heading-i18n.test.js` | 0 | 2 files; 20 tests passed. |
| `npm test -- --run` | 0 | 19 files passed / 2 skipped; 138 tests passed / 8 skipped. |
| `npm run typecheck` | 0 | Completed without type errors. |
| `npm run build` | 0 | Vite built 45 modules; only known chunk-size warning. |
| `git diff --check 460d337..b9916cd`; working-tree `git diff --check` | 0 | No whitespace errors. |
| `git merge-base --is-ancestor b9916cd main` | 1 (expected) | Implementation commit is not merged into `main`. |

`npm run test:rules` is not applicable because UXS-002 does not change Firestore Rules.

## Reviewer Boundary

I modified no production code or tests. This review record and the accompanying `TASK_STATUS.md` transition are the only intended changes in the review commit. The Coder must make only the R6 minimum correction, return UXS-002 to `READY_FOR_REVIEW`, and must not start UXS-003.
