# T020 Review R2

## Decision

`APPROVED`

## Scope

- Base SHA: `27776b994d558faa2e3ccd13ac91e443932f6000`
- Previous reviewed implementation: `b986c24fcd509916c7e30d30887d1d4cf10b728c`
- R2 implementation head: `a4d853e34f5a094f8528c7cad5f85325788cad57`
- Branch head at review start: `77385d2`
- Review round: `2`
- Full implementation range: `27776b99..a4d853e3`
- R2 rework increment: `b986c24f..a4d853e3`
- Evidence/status follow-up range: `a4d853e3..77385d2`

## Findings

无阻断问题。

R1 的两项阻断项均已解决。`src/js/state.js:15-18` 将自动汇率初值设为 `null`；`src/js/auth.js:66-70` 在 live/cache 均不可用时明确保留 `null`；`src/js/currency-view.js:1-29`、`src/js/main.js:119-130` 和 `src/js/quick-add.js:59-66` 均拒绝无效自动汇率，因而不会把默认值用于 CNY 显示或写入转换。`src/js/fx-display.js:29-41` 现在拒绝不可解析的缓存时间戳。

新增的 `tests/unit/auth-fx.test.js` 直接覆盖 `auth -> state`，而 `tests/unit/currency-view.test.js:165-188,231-255` 覆盖直接录入与快速记账两条实际写入路径。完整 diff 未发现 Firebase 部署、Rules/Auth 配置、生产数据、历史账目、Transaction 迁移或 T021 改动。

## Completion Criteria

| Criterion | Result | Evidence |
|---|---|---|
| Slow FX request times out and falls back to valid cache | PASS | `src/js/fx-display.js:7-19,69-84`; `tests/unit/fx-display.test.js:52-75`. |
| HTTP failure does not write zero and uses valid stale cache | PASS | `src/js/fx-display.js:69-84`; `tests/unit/fx-display.test.js:78-94`. |
| Malformed live JSON/rate is rejected without cache write | PASS | `src/js/fx-display.js:72-76`; `tests/unit/fx-display.test.js:96-110`. |
| Cache hit/staleness has valid source and update time | PASS | `src/js/fx-display.js:29-41`; `tests/unit/fx-display.test.js:112-128`. |
| Complete FX outage makes automatic CNY unavailable while VND accounting remains usable | PASS | `src/js/state.js:15-18`; `src/js/auth.js:66-70`; `tests/unit/auth-fx.test.js:30-47`. |
| Direct CNY input cannot persist a VND conversion without verified automatic rate | PASS | `src/js/main.js:119-130`; `tests/unit/currency-view.test.js:165-188`. |
| Quick Add cannot persist a VND conversion without verified automatic rate; manual rate remains supported | PASS | `src/js/quick-add.js:59-66`; `tests/unit/currency-view.test.js:210-228,231-255`. |
| FX failure does not mutate historical/pending VND data | PASS | `tests/unit/currency-view.test.js:165-188,231-255`; adapter has no ledger writes. |
| No prohibited Firebase/schema/transaction/T021 scope expansion | PASS | Full changed-file list and diff `27776b99..a4d853e3`. |
| Implementation remains unmerged from `main` | PASS | `git merge-base --is-ancestor a4d853e3 main` exited `1`. |

## Verification Performed

| Command | Exit code | Key output |
|---|---:|---|
| Invalid-cache probe with `updatedAt: "not-a-date"` and HTTP 503 | 0 | Returned unavailable, with `rate: null` and no invalid cache result. |
| Unavailable-rate display/conversion probe | 0 | Returned `display: "汇率不可用"` and `converted: null`. |
| `npm test -- --run tests/unit/fx-display.test.js tests/unit/auth-fx.test.js tests/unit/currency-view.test.js` | 0 | 3 files, 15 tests passed. |
| `npm test -- --run` | 0 | 16 files passed, 2 skipped; 116 tests passed, 8 skipped. |
| `npm run typecheck` | 0 | `tsc -p tsconfig.json --noEmit` completed. |
| `npm run build` | 0 | Vite built 42 modules. Existing chunk-size warning was emitted; no build error. |
| `git diff --check 27776b99..a4d853e3` | 0 | Full implementation range is whitespace-clean. |
| `git diff --check a4d853e3..77385d2` | 0 | Evidence/status follow-up range is whitespace-clean. |
| `git merge-base --is-ancestor a4d853e3 main` | 1 | Expected: implementation is not merged into `main`. |

## Reviewer Boundary

The reviewer modified no business code or tests. This review record and the accompanying `TASK_STATUS.md` transition are the only intended changes in the review commit. T020 is approved; this review does not start T021.
