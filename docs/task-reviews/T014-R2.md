# T014 Review R2

## Decision

`APPROVED`

## Scope

- Base SHA: `1d4903c40205cef48c241e4313fe97ebf574dbb5`
- Previous reviewed implementation: `08da24d0819b2a69f1bddb03446666853df873e7`
- R2 implementation head: `c1eadecf1cef2d2dc87f07bac2d628dd76d6c573`
- Current branch head at review start: `d86ae70e479a91b81c089a8c40d6eedff2d29d0c`
- Full implementation range reviewed: `1d4903c..c1eadec`
- Rework increment reviewed: `08da24d..c1eadec`
- Evidence correction/status range reviewed: `c1eadec..d86ae70`

The implementation changes only the legacy streak, reward, snapshot, state, tests, and T014 evidence paths. No Firebase Rules, production data, T011/T012 model, reward thresholds, or firework visual implementation changed. `src/js/quick-add.js` is unchanged but its existing `updateStreakAfterRecord()` call was inspected.

## Functional Review

- `buildLegacyStreak()` derives eligible dates from non-zero, calculable income and expense entries, de-duplicates days, and uses `Asia/Ho_Chi_Minh` for the cursor date.
- It derives Dec 31 to Jan 1 continuity from `previousYearEntries`, never from legacy `expense_streak` or `expense_last_date` fields.
- Both direct table input and quick add call the same refresh path. Both current-year and previous-year Firestore snapshots use the refresh path with ordinary fireworks suppressed, while 7/30 milestones remain date-plus-threshold deduplicated.
- The previous-year listener is torn down and replaced together with the active-year listener.
- `T014_GREEN.md` now records the immutable R2 implementation SHA `c1eadec` in a follow-up evidence/status commit, rather than a self-referential value.

## Findings

No Critical, High, Medium, or Low blocking findings.

## Completion Criteria

| Criterion | Result | Evidence |
|---|---|---|
| Pure income, pure expense, and mixed entries form a streak | PASS | `tests/unit/legacy-streak.test.js:125-151`. |
| Two consecutive accounting days equal 2 | PASS | Targeted suite passes. |
| 1/2/6/7/8/29/30/31 day boundaries | PASS | `tests/unit/legacy-streak.test.js:223-231`. |
| Same-day multiple entries count once | PASS | `tests/unit/legacy-streak.test.js:233-243`. |
| Yesterday gap restarts at 1 | PASS | `tests/unit/legacy-streak.test.js:245-255`. |
| Historical backfill and deletion recompute | PASS | `tests/unit/legacy-streak.test.js:203-220` and `272-289`. |
| Invalid/zero formulas do not count | PASS | `tests/unit/legacy-streak.test.js:257-270`. |
| Direct and quick entry paths are consistent | PASS | `src/js/main.js:75-80`, `src/js/quick-add.js:90-92`, and targeted tests. |
| Current and remote snapshots use the unified refresh/milestone path | PASS | `src/js/sync.js:9-12,118-136`; `tests/unit/sync-state.test.js:146-198`. |
| 7/30 milestones fire once and no legacy counter is written | PASS | `tests/unit/legacy-streak.test.js:292-305`; `tests/unit/sync-state.test.js:169-198`. |
| Dec 31 to Jan 1 continuity uses adjacent entries | PASS | `src/js/streak.js:55-75`, `src/js/sync.js:116-123`, and `tests/unit/legacy-streak.test.js:307-324`. |
| GREEN evidence identifies the exact implementation SHA | PASS | `docs/review-evidence/T014_GREEN.md:31-35`. |

## Verification Performed

| Command | Exit code | Key output |
|---|---:|---|
| `npm test -- --run tests/unit/legacy-streak.test.js tests/unit/sync-state.test.js` | 0 | 2 files, 29 tests passed. |
| `npm test -- --run` | 0 | 11 files passed, 2 skipped; 83 tests passed, 7 skipped. |
| `npm run typecheck` | 0 | `tsc -p tsconfig.json --noEmit` completed. |
| `npm run build` | 0 | Vite built 39 modules. Existing chunk-size warning was emitted; no build error. |
| `git diff --check 1d4903c..c1eadec` | 0 | Full implementation range is whitespace-clean. |
| `git diff --check c1eadec..d86ae70` | 0 | Evidence/status follow-up range is whitespace-clean. |

## Reviewer Boundary

The reviewer modified no business code or tests. This commit contains only the review record and `TASK_STATUS.md` state transition.
