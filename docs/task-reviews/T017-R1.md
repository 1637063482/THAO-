# T017 Review R1

## Decision

`CHANGES_REQUESTED`

## Scope

- Base SHA: `c2a726ed6a1a3f335c1f4ca55ed9edb035bf0f48`
- Reviewed implementation head: `73b02e470354707bade80e6fc56fa8cc42849ce2`
- Branch head at review start: `11b5cb838147de22b42a554d83f82fb63954803c`
- Review round: `1`
- Full implementation range: `c2a726ed..73b02e47`
- Evidence/status follow-up range: `73b02e47..11b5cb83`
- Changed implementation files: `docs/firebase-access-contract.md`, `docs/review-evidence/T017.md`, `firestore.rules`, and `tests/rules/legacy-ledger.rules.test.js` (plus Task status transitions).

The reviewed implementation remains on `task/t017-rules-contract`; `git merge-base --is-ancestor 73b02e4 main` exited `1`, so it is not merged into `main`.

## Findings

### High

1. `docs/review-evidence/T017.md:160` stores three account-identifying email search patterns, despite the document's assertion at `:23` and `:191` that real emails were not committed. Escaping the dot for a search regex does not make those account identifiers safe to record. This also makes the claimed secret-scan evidence non-reproducible as a proof of absence: the scan input itself is now retained in the file being scanned. Impact: the repository contains avoidable real-account identifying information, violating the repository prohibition on real emails in source, tests, logs, or documentation. Minimum correction: remove every real-account identifier from the evidence, replace the command with a redacted description or non-sensitive generic scan, and record only its exit code/count; do not repeat the identifiers in any rework evidence.

### Medium

1. `TASK_STATUS.md:13` and `docs/review-evidence/T017.md:8` record base SHA `c2a726e9f8d68d7f80a523ff8d79fd3ac76ecb5f`, which is not a Git object. The actual merge base of the recorded implementation is `c2a726ed6a1a3f335c1f4ca55ed9edb035bf0f48`. Impact: the claimed implementation range cannot be reproduced from the Task handoff, so the evidence fails the required audit traceability. Minimum correction: update both records to the resolvable base SHA and retain the same implementation SHA; no production-code change is required.

No other blocking production-code, Firebase deployment, Firebase Auth-account, production-data, or T011/T012 scope expansion was found in the reviewed diff.

## Completion Criteria

| Criterion | Result | Evidence |
|---|---|---|
| Owner-confirmed two-account permissions are explicit | PASS | `docs/firebase-access-contract.md:5-13,26-32` records matching read/create/update permissions, delete denial, and default denial. This is repository evidence only; it does not claim live-console verification. |
| Candidate Rules mirror the documented semantics without real values | PASS | `firestore.rules:4-22` requires an email token, compares the two fixture emails case-insensitively, permits only ledger read/create/update, and denies delete/default paths. |
| Direct SDK tests cover read/create/update/delete and negative principals | PASS | `tests/rules/legacy-ledger.rules.test.js:35-75` uses `getDoc`, `setDoc`, `updateDoc`, and `deleteDoc` for both fixtures, anonymous, missing-email, and third-fixture principals. |
| Anonymous and third fixture identity are denied | PASS | `tests/rules/legacy-ledger.rules.test.js:48-59` denies ledger access for anonymous, missing-email, and a third UID/email fixture. |
| No real UID/email is written to repository artifacts | FAIL | `docs/review-evidence/T017.md:160` records real-account-identifying email patterns. |
| Base and implementation evidence is reproducible | FAIL | The declared base at `TASK_STATUS.md:13` / `docs/review-evidence/T017.md:8` does not resolve; the actual base is recorded above. |
| No unauthorized deployment, production-data operation, client role UI, or T011/T012 expansion | PASS | Full diff `c2a726ed..73b02e47`; changed-file inspection. |

## Verification Performed

| Command | Exit code | Key output |
|---|---:|---|
| `npm run test:rules` | 0 | Firestore Emulator started; 1 test file and 4 tests passed. Expected rejected operations were logged by the emulator and asserted by the suite. |
| `npm test -- --run` | 0 | 13 files passed, 2 skipped; 97 tests passed, 8 skipped. |
| `npm run typecheck` | 0 | `tsc -p tsconfig.json --noEmit` completed. |
| `npm run build` | 0 | Vite built 41 modules. Existing chunk-size warning was emitted; no build error. |
| `git diff --check c2a726ed..73b02e47` | 0 | Full implementation range is whitespace-clean. |
| `git diff --check 73b02e47..11b5cb83` | 0 | Evidence/status follow-up range is whitespace-clean. |
| `git cat-file -e <declared-base>^{commit}` | 128 | The recorded base SHA does not resolve; `git merge-base c2a726e 73b02e4` returned `c2a726ed6a1a3f335c1f4ca55ed9edb035bf0f48`. |
| `git merge-base --is-ancestor 73b02e4 main` | 1 | Expected: the implementation is not merged into `main`. |

## Required Rework

1. Scrub the account-identifying email patterns from `docs/review-evidence/T017.md` and any T017 rework evidence. Record secret-scan evidence without real values or reversible identifiers.
2. Correct the base SHA in `TASK_STATUS.md` and `docs/review-evidence/T017.md` to `c2a726ed6a1a3f335c1f4ca55ed9edb035bf0f48`.
3. Keep the scope limited to these documentation/evidence corrections, return to `READY_FOR_REVIEW`, and provide the new implementation head for R2 review. Do not deploy Rules, change Firebase Auth, or advance to T018.

## Reviewer Boundary

The reviewer modified no business code or tests. This review record and the accompanying `TASK_STATUS.md` transition are the only intended changes in the review commit.
