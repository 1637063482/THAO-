# REM-016 R1 — Changes Requested

## Review scope

- Reviewed range: `7f03eed..e24d333` (REM-015).
- Initial worktree: clean (`git status --short`, exit 0).
- Read only the REM-016 requirements, complete reviewed-range diff, and its scoped production, Rules/repository, lifecycle, test, renderer, and bundle paths.
- No deploy command, Firebase configuration change, online Rules/Auth change, or production-data action was performed.

## Evidence

- The reviewed range contains 15 files / 205 additions / 21 deletions. The feature imports in `src/js/main.js`, `src/js/charts.js`, `src/js/render.js`, and `src/features/deposits/controller.js` use retryable lazy imports; the built main asset was 629.66 kB.
- `firestore.rules` has a dedicated `shared_ledger_savings` match with authorized read/create/update validation, deny-delete, and a deny-by-default fallback. `DepositRepository` uses the same `shared_ledger_savings` path and Firestore transactions. The emulator critical path covers first create, second create, fresh-repository readback, and Vietnamese legacy-term editing from Chinese UI.
- Lifecycle paths pair starts/stops: deposit controller unsubscribes and clears runtime resources; ledger input/year/root handlers remove listeners and timers; main routes auth logout to controller/router stops. Scoped tests include repeated deposit starts and stop cleanup.
- A real synthetic-state mount used delivered `renderHeader` and `renderCommandMenu` (not injected shell markup). At 390x844, 768x1024, and 1440x900: `scrollWidth === clientWidth`, the Add button was visible, and the 390px Tab key focused the year select.

## Required gates

| Command | Result |
|---|---|
| `npm test -- --run` | exit 0; 51 passed / 4 skipped files, 342 passed / 16 skipped tests |
| `npm run test:rules` | exit 0; Firestore Emulator with `demo-no-project`; 4 passed files, 16 passed tests |
| `npm run typecheck` | exit 0 |
| `npm run typecheck:js` | exit 0 |
| `npm run build` | exit 0; main asset 629.66 kB |
| `node scripts/check-bundle-budget.mjs` | exit 0 |
| `git diff --check` | exit 0 |

`npm test -- --run` emitted two jsdom `HTMLCanvasElement.getContext()` not-implemented notices after its passing summary. The build emitted Vite chunk-size/dynamic-import advisories; neither changes the successful exit status or budget result.

## Blockers

1. **`main.js` is not a thin composition root.** At reviewed head `e24d333`, `src/js/main.js` still owns application behavior rather than only assembling and starting modules: `LEDGER_LABELS` and `updateLedgerToggleLabel` (lines 173–191), `switchLanguage` (193–208), currency/FX operations (244–275), theme/privacy operations (277–305), and sharing/CSV export (307–335). This fails REM-016's explicit composition-root review scope and the plan's overall completion condition that `main.js` retain only application composition and startup. Return this to the maintenance task responsible for the app-shell/composition-root split.

2. **The mandated reviewer context command cannot run from the implementation status.** `rtk npm run context:reviewer` was run exactly once and exited 1 because `TASK_STATUS.md` supplied `Work ID`, while `scripts/task-context.mjs` requires `Task ID`. The defect already exists at base `7f03eed`, so REM-015 did not introduce it; it nevertheless prevented the required reviewer-context gate. This review status update restores the required `Task ID` field but does not re-run the command.

## Verdict

**CHANGES_REQUESTED.** All executable regression, Rules, type, build, bundle, and whitespace gates passed, but the two blockers above mean the maintenance remediation cannot receive REM-016 approval.
