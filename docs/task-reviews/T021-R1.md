# T021 Review R1

## Decision

`APPROVED`

## Scope

- Base SHA: `d0eead5fae3132361cda8fb50eebc5747c5c9037`
- Reviewed implementation head: `311dd41b8b7b7fc4a265dcd6897c0e838c523dee`
- Branch head at review start: `0bd2a1c`
- Review round: `1`
- Full implementation range: `d0eead5f..311dd41b`
- Evidence/status follow-up range: `311dd41b..0bd2a1c`

## Findings

无阻断问题。

`.github/workflows/ci.yml` defines one read-only (`contents: read`) GitHub Actions gate job for pull requests and the repository's `main`, `task/**`, and `fix/**` push branches. Its ordered commands are `npm ci`, ordinary tests, typecheck, Firestore Rules emulator tests, and production build. No `continue-on-error`, deploy command, secret reference, Firebase token, service account, or production credential is present. `package.json` fixes Rules tests to the isolated `demo-no-project`, so the Firebase CLI does not select a locally signed-in production project.

`SECURITY.md` records the non-deployment, no-real-credential, no-real-UID/email, and no-real-ledger-data boundaries. The implementation contains no business-code, historical-ledger, Rules, Auth-account, deployment, production-data, or transaction-migration change.

## Completion Criteria

| Criterion | Result | Evidence |
|---|---|---|
| CI runs ordinary tests, typecheck, Rules tests, and build after clean install | PASS | `.github/workflows/ci.yml:31-49`; clean-install sequence verified locally. |
| Any gate failure blocks the job | PASS | Each command is an ordinary `run` step with no `continue-on-error`, `|| true`, or `if: always()` bypass. |
| CI uses fixed Node LTS and Java for Firestore emulator | PASS | `.github/workflows/ci.yml:23-30` sets Node 22 and Temurin 21. |
| Rules tests use emulator-only demo project rather than local login/project state | PASS | `package.json:11`; `npm run test:rules` reported `demo-no-project` and 4 passing Rules tests. |
| CI has no deployment, production write, secret, token, or production credential use | PASS | Workflow/static audit; `SECURITY.md:5-16`. |
| No real UID/email allowlist or financial data is added to CI artifacts | PASS | `tests/unit/ci-workflow.test.js:30-40`; reviewer static audit. |
| Windows local and CI command semantics are aligned | PASS | `npm ci`, test, typecheck, Rules, and build all passed locally with the same commands. |
| No prohibited unrelated scope expansion | PASS | Full changed-file list and diff `d0eead5f..311dd41b`. |
| Implementation remains unmerged from `main` | PASS | `git merge-base --is-ancestor 311dd41b main` exited `1`. |

## Verification Performed

| Command | Exit code | Key output |
|---|---:|---|
| `npm test -- --run tests/unit/ci-workflow.test.js` | 0 | 1 file, 2 tests passed. |
| Workflow structure/static audit | 0 | Push/PR triggers, read-only permission, no deployment, no failure suppression, and no secret reference all confirmed. |
| `npm ci` | 0 | Clean install completed; only dependency deprecation warnings. |
| `npm test -- --run` after `npm ci` | 0 | 17 files passed, 2 skipped; 118 tests passed, 8 skipped. |
| `npm run typecheck` after `npm ci` | 0 | `tsc -p tsconfig.json --noEmit` completed. |
| `npm run test:rules` after `npm ci` | 0 | Demo project `demo-no-project`; 1 file, 4 Rules tests passed. Expected emulator deny/shutdown warnings were emitted. |
| `npm run build` after `npm ci` | 0 | Vite built 42 modules. Existing chunk-size warning was emitted; no build error. |
| `git diff --check d0eead5f..311dd41b` | 0 | Full implementation range is whitespace-clean. |
| `git diff --check 311dd41b..0bd2a1c` | 0 | Evidence/status follow-up range is whitespace-clean. |
| `git merge-base --is-ancestor 311dd41b main` | 1 | Expected: implementation is not merged into `main`. |

## Reviewer Boundary

The reviewer modified no business code or tests. This review record and the accompanying `TASK_STATUS.md` transition are the only intended changes in the review commit. T021 is approved; no further task is started by this review.
