# REM-012 Review R1 - APPROVED

## Decision

`APPROVED`

Reviewed implementation: `5dffdb2` on base `69d0f04`.

## Findings

No blocking findings.

- The yearly document contract remains `shared_ledger_<year>` and the legacy `balances` / `entries` / `settings` shape. Direct edits continue to update the date/category `entries` matrix and `pendingUpdates.entries`; no account or transaction model was reintroduced.
- The three accounting paths remain aligned: delegated `focusout` refreshes streaks after direct editing, Quick Add retains the same legacy matrix write, and remote snapshots invoke controller-provided soft rendering plus the non-default streak refresh path.
- Vietnam date-only handling remains injected from `getLedgerToday()` and `getNextLedgerMidnightDelay()`. The controller changes month at a local date boundary, and on Vietnam New Year it clears year-scoped state before subscribing to the new current and previous yearly documents.
- CNY stays a view/edit conversion boundary: a display-only focus cycle does not mutate pending VND; an actual CNY edit writes its converted VND value only on blur and refuses an unavailable automatic rate.
- `main.js` now composes ledger dependencies and delegates lifecycle, input, month/year switching, snapshot refresh, and midnight refresh to the ledger controllers. The replaced month/year inline handlers and ledger globals are absent. Controller `stop()` removes delegated listeners, stops both year/input controllers and realtime listeners, and clears its midnight timer; the Auth logout path calls it before a subsequent login can mount one listener set again.

## Verification

| Check | Result |
|---|---|
| REM-012 task segment, base/head, complete `69d0f04..5dffdb2` diff, and affected code/tests | PASS |
| Task-directed ledger, date, streak, and CNY tests | 48 passed |
| Full test suite | 350 passed / 20 skipped |
| Typecheck | exit 0 |
| Production build | exit 0; existing 864.48 kB chunk-size warning |
| `git diff --check 69d0f04..5dffdb2` and working diff check | exit 0 |

`npm run context:reviewer` was run once but its context loader rejected the current `TASK_STATUS.md` field layout (reported missing `Task ID` or `State`). This review therefore used the authoritative REM-012 segment, exact base/head diff, affected code, and fresh verification above; the loader mismatch is not a REM-012 product-code defect.

No deployment, online Firebase/Auth/Rules/data change, real financial data, email, or UID was introduced by this review.
