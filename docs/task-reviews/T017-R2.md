# T017 Review R2

## Decision

`APPROVED`

## Scope

- Base SHA: `c2a726ed6a1a3f335c1f4ca55ed9edb035bf0f48`
- Previous reviewed implementation: `73b02e470354707bade80e6fc56fa8cc42849ce2`
- R2 implementation head: `1a2bf9e9e91b22265bc3292f1ebf2bdaabe1c4a5`
- Branch head at review start: `5b538d3`
- Review round: `2`
- Full implementation range: `c2a726ed..1a2bf9e`
- R2 rework increment: `73b02e47..1a2bf9e`
- Evidence/status follow-up range: `1a2bf9e..5b538d3`

The R2 increment is documentation and evidence only. It does not alter the candidate Rules, rules tests, production code, Firebase configuration, Firebase Auth accounts, online Rules, or production data. `git merge-base --is-ancestor 1a2bf9e main` exited `1`; the approved implementation is not merged into `main`.

## Findings

No Critical, High, Medium, or Low blocking findings.

R1's evidence findings are resolved. `docs/review-evidence/T017.md:8-9` now names a resolvable base and the exact implementation head. Its secret-scan sections at `:155-171` and `:206-221` use a non-reversible generic pattern and omit the account values. A fresh reviewer-run scan exited `1` (no match), while `git cat-file -e` for the recorded base exited `0`.

The rules and direct-SDK tests remain aligned with the documented owner-confirmed contract: `firestore.rules:4-22` allows only the two fixture-email principals to read/create/update ledger documents, denies delete, and defaults all other paths to deny. `tests/rules/legacy-ledger.rules.test.js:35-75` covers both allowed fixtures, anonymous, missing-email, and third-fixture principals through `getDoc`, `setDoc`, `updateDoc`, and `deleteDoc`.

## Completion Criteria

| Criterion | Result | Evidence |
|---|---|---|
| Two-account permissions are explicit and are based on owner-confirmed online Rules semantics | PASS | `docs/firebase-access-contract.md:5-13,26-32`. This documents the owner confirmation without claiming an Emulator test proves live-console state. |
| Candidate Rules match the documented ledger-only contract without real account values | PASS | `firestore.rules:4-22`; fixture-only values are documented at `docs/firebase-access-contract.md:15-22`. |
| Direct SDK coverage includes read/create/update/delete with allow and deny behavior | PASS | `tests/rules/legacy-ledger.rules.test.js:35-75`; fresh Emulator run passed all 4 tests. |
| Anonymous and third fixture identity are denied | PASS | `tests/rules/legacy-ledger.rules.test.js:48-59`. |
| R1 account-identifying evidence is scrubbed without new identifiers | PASS | `docs/review-evidence/T017.md:155-171,206-221`; fresh redacted scan exited `1`. |
| Base and implementation evidence is reproducible | PASS | `docs/review-evidence/T017.md:5-10`; fresh `git cat-file -e` exited `0` for the recorded base. |
| No unauthorized Firebase deployment/Auth change, production-data operation, client-role UI, or T011/T012 expansion | PASS | Full diff `c2a726ed..1a2bf9e` and changed-file inspection. |
| Implementation remains unmerged pending subsequent integration | PASS | `git merge-base --is-ancestor 1a2bf9e main` exited `1`. |

## Verification Performed

| Command | Exit code | Key output |
|---|---:|---|
| `npm run test:rules` | 0 | Firestore Emulator suite: 1 file, 4 tests passed. Rejected operations were expected assertions. |
| `npm test -- --run` | 0 | 13 files passed, 2 skipped; 97 tests passed, 8 skipped. |
| `npm run typecheck` | 0 | `tsc -p tsconfig.json --noEmit` completed. |
| `npm run build` | 0 | Vite built 41 modules. The pre-existing chunk-size warning was emitted; no build error. |
| `git diff --check c2a726ed..1a2bf9e` | 0 | Full implementation range is whitespace-clean. |
| `git diff --check 1a2bf9e..5b538d3` | 0 | Evidence/status follow-up range is whitespace-clean. |
| Redacted T017 secret scan | 1 | Expected no-match result; account values were not printed or recorded. |
| `git cat-file -e c2a726ed6a1a3f335c1f4ca55ed9edb035bf0f48^{commit}` | 0 | Recorded base object resolves. |
| `git merge-base --is-ancestor 1a2bf9e main` | 1 | Expected: the implementation is not merged into `main`. |

## Reviewer Boundary

The reviewer modified no business code or tests. This review record and the accompanying `TASK_STATUS.md` transition are the only intended changes in the review commit. No Firebase deployment or external mutation was performed.
