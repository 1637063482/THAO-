# UXS-002 Review R2

## Decision

`CHANGES_REQUESTED`

The R1 scope blocker is resolved in the current Task Plan, and the automated gates pass. However, the implementation still leaves required user-visible text hard-coded in allowed files and retains unauthorized CSV/currency-test changes. These are Coder-fixable issues under the amended plan.

## Scope

| Field | Value |
|---|---|
| Base SHA | `460d337018106118b19c74fb3ca66ea33e07fa37` |
| Reviewed implementation head | `cf1144dbee341b5f0233d298ba5b42a47e5771b6` |
| Branch head at review start | `d074e74` |
| Branch | `task/uxs-002-i18n` |
| Review round | `2` |
| Full implementation range | `460d337..cf1144d` |
| Incremental range after R1 | `4eb443d..cf1144d` |

The amended plan now authorizes `src/js/fireworks.js` only for adding Vietnamese barrage messages, explicitly excludes `currency-view.js` and `fx-display.js`, and scopes the localization completion criterion to allowed files.

## Findings

### High - Required visible text remains hard-coded in allowed production paths

- **Files/lines:** `src/js/render.js:94-120,185-190`; `src/js/budget.js:86-90`; `src/js/auth.js:61`.
- **Evidence:** `render.js` still builds Chinese budget/table/streak labels directly; `budget.js` builds the Chinese used/remaining/daily-available text directly; `auth.js` directly appends Chinese month-tab labels. These files are explicitly in the amended UXS-002 allowed-file list and are reachable UI paths.
- **Impact:** The default Vietnamese UI and the runtime language switch remain incomplete. This fails the Task's required coverage of static/dynamic user copy and its completion criterion for all reachable text in allowed files.

### High - CSV behavior and an unrelated currency test still differ from the base despite being outside the amended Task scope

- **Files/lines:** `src/js/export.js:1-45`; `src/js/main.js:409-417`; `tests/unit/currency-view.test.js:250`; `TASK_PLAN.md:163-171`.
- **Evidence:** `git diff --numstat 460d337..cf1144d -- src/js/export.js tests/unit/currency-view.test.js` reports `19/24` changed lines in `export.js` and `1/1` in `currency-view.test.js`; `git diff --quiet 460d337 cf1144d -- src/js/export.js` exits `1`. `main.js` also changes the CSV category payload. The amended exact file list authorizes neither export/main CSV behavior nor the currency-view test, and the evidence's claim that these changes were reverted to base is therefore false.
- **Impact:** UXS-002 changes unrelated CSV export behavior and a T020 currency test. It violates the single-Task scope and can regress an already approved export contract.

### Medium - The claimed state-invariance test does not exercise application state or pending updates

- **File/lines:** `tests/unit/i18n.test.js:157-174`.
- **Evidence:** The new test creates isolated `fakeState` and `fakePending` variables that are never supplied to, imported by, or observed by the i18n module. It consequently proves only that unrelated local variables remain unchanged.
- **Impact:** The required deep-equality proof for actual `state.appState` and `state.pendingUpdates` across language switching is still absent. The current implementation appears not to write those values by static inspection, but the mandated regression guard is not meaningful.

### Medium - Evidence is internally stale after the R2 rework

- **File/lines:** `docs/review-evidence/UXS-002.md:10-32,48-72,97-108`.
- **Evidence:** It still calls the test file a 16-test suite, says the review round is 1, describes CSV i18n headers, and says the CSV output follows the current UI language. R2 targeted verification actually reports 18 tests, while the implementation has changed CSV code away from the prior i18n version.
- **Impact:** The evidence no longer accurately describes the reviewed implementation, weakening the required handoff record.

## Minimum Correction

1. Localize the remaining direct UI copy in `render.js`, `budget.js`, and `auth.js` through the existing dictionaries, including formatted month/budget/streak text.
2. Revert `src/js/export.js`, the CSV-specific portion of `src/js/main.js`, and `tests/unit/currency-view.test.js` exactly to the approved base behavior unless the user formally expands UXS-002 again. Do not alter CSV behavior as part of this Task.
3. Replace the fake-object test with a test that snapshots the actual exported application state and `pendingUpdates` around `switchLanguage`, or add a focused integration seam that genuinely observes those objects without changing their behavior.
4. Correct the evidence to R2 facts, changed-file scope, targeted-test count, and current CSV behavior; rerun the required scan and gates before returning to `READY_FOR_REVIEW`.

## Completion Criteria

| Criterion | Result | Evidence |
|---|---|---|
| `vi` default; only `vi`/`zh-CN` are selectable and persisted | PASS | `src/js/i18n.js`; targeted tests pass. |
| Real dictionaries have matching, non-empty keys and Vietnamese diacritics | PASS | `tests/unit/i18n.test.js` real-dictionary test; 18 targeted tests passed. |
| All reachable user-visible text in allowed paths is dictionary-provided | FAIL | Direct Chinese UI text remains in `render.js`, `budget.js`, and `auth.js`. |
| Switch requires no reload/cloud write and preserves actual business/sync state | FAIL | No direct i18n write found, but the required actual-state deep-equality test is not implemented. |
| Fireworks scope follows the amended plan | PASS | `src/js/fireworks.js` adds Vietnamese barrage strings only; Chinese barrage strings are explicitly retained by plan. |
| No prohibited behavior/schema/Firebase/design change or unrelated scope expansion | FAIL | CSV/export and currency-test diffs remain outside the amended allowed-file list. |
| Implementation remains unmerged from `main` | PASS | `git merge-base --is-ancestor cf1144d main` exited `1` (expected). |

## Verification Performed

| Command/check | Exit code | Key output |
|---|---:|---|
| Full `460d337..cf1144d` and incremental `4eb443d..cf1144d` diff review | 0 | Confirmed amended Task Plan, remaining direct strings, and unintended CSV/currency-test diffs. |
| `npm test -- --run tests/unit/i18n.test.js` | 0 | 1 file, 18 tests passed. |
| `npm test -- --run` | 0 | 18 files passed / 2 skipped; 136 tests passed / 8 skipped. |
| `npm run typecheck` | 0 | Completed without type errors. |
| `npm run build` | 0 | Vite built 45 modules; only chunk-size warning was emitted. |
| `git diff --check 460d337..cf1144d`; working-tree `git diff --check` | 0 | No whitespace errors. |
| Allowed-path hard-code scan | 0 | Found the direct Chinese user-copy paths cited above. |
| `git merge-base --is-ancestor cf1144d main` | 1 (expected) | Implementation commit is not merged into `main`. |

`npm run test:rules` is not applicable because this Task does not change Firestore Rules.

## Reviewer Boundary

I modified no production code or tests. This review record and the accompanying `TASK_STATUS.md` transition are the only intended changes in the review commit. The Coder must read this R2 record, make only the minimum corrections, return UXS-002 to `READY_FOR_REVIEW`, and must not start UXS-003.
