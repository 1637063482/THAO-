# UXS-005 Review R3

## Decision

`CHANGES_REQUESTED`

The R3 implementation fixes the R2 functional regression: submit control reset is centralized, successful reopen is tested, and invalid VND validation clears `aria-busy`. Targeted tests, full tests, typecheck, build, and diff checks pass. One handoff evidence defect remains: the Full Gates table is explicitly recorded “after the R2 implementation commit,” not after the submitted R3 implementation head.

## Scope

| Field | Value |
|---|---|
| Base SHA | `473e4f631affb1204df5b33527a2d4073dbd61aa` |
| Reviewed implementation head | `d31b37212c63fbe6acd9e1a983a73ee1c48220cc` |
| Branch head at review start | `ec41e6f` |
| Branch | `task/uxs-005-quick-add` |
| Review round | `3` |
| Full implementation range | `473e4f6..d31b372` |
| Incremental rework range | `16a7202..d31b372` |

## Findings

### Medium - Full-gate evidence is not explicitly tied to the submitted R3 head

- **Files/lines:** `docs/review-evidence/UXS-005.md:78-84`; `TASK_STATUS.md:11-12`.
- **Evidence:** The evidence header correctly names `d31b372...` and review round 3, but the Full Gates heading says “Recorded after the R2 implementation commit.” The table contains no final R3 implementation SHA or command context. The Reviewer independently reran the commands after R3 and obtained the same successful totals, but that fresh verification is not recorded in the Coder handoff artifact.
- **Impact:** The evidence is not a self-contained final record for the submitted head, violating the repository’s handoff requirement even though the implementation itself passes.

## Minimum Correction

1. Change the Full Gates note to explicitly state that the commands were run after implementation head `d31b37212c63fbe6acd9e1a983a73ee1c48220cc`, retaining their actual exit codes and outputs. Then return UXS-005 to `READY_FOR_REVIEW` without changing business behavior.

## Completion Criteria

| Criterion | Result | Evidence |
|---|---|---|
| Income and expense both use valid legacy entry records | PASS | Existing category path includes income; legacy writer is unchanged and related suites pass. |
| Duplicate click does not produce duplicate write | PASS | `tests/unit/quick-add-sheet.test.js` re-entrant guard test passes. |
| Failure preserves input for retry | PASS | Failure test preserves amount and remark. |
| Complete keyboard operation, Escape/overlay close, and focus return | PASS | Escape/focus test passes; existing overlay close listener remains in main. |
| Mobile keyboard/safe-area and desktop dialog are usable | PASS | 390/430/1440 production-page captures and CSS safe-area/bounded-sheet rules present. |
| VND fact remains integral | PASS | Positive safe-integer guard and decimal/zero/negative tests pass. |
| Repeated successful Quick Add remains usable | PASS | R3 success-then-reopen test verifies enabled control and no `aria-busy`. |
| No prohibited schema/sync/streak/table change | PASS | Full diff contains no prohibited model/schema/state-machine/table change. |
| Evidence includes final required gate results tied to submitted head | FAIL | Full Gates note still references R2, not R3 head. |
| Implementation remains unmerged from `main` | PASS | `git merge-base --is-ancestor d31b372 main` exited `1` (expected). |

## Verification Performed

| Command/check | Exit code | Key output |
|---|---:|---|
| Read status/workflow/Task/Review Plan/evidence/R2 review; full `473e4f6..d31b372` diff and incremental `16a7202..d31b372` diff | 0 | R3 centralizes control reset and adds success-reopen/accessibility tests; only final evidence wording remains stale. |
| `npm test -- --run tests/unit/quick-add-sheet.test.js tests/unit/currency-view.test.js tests/unit/legacy-streak.test.js tests/unit/local-date.test.js` | 0 | 4 files, 47 tests passed. |
| `npm test -- --run` | 0 | 24 files passed, 2 skipped; 180 passed, 8 skipped. |
| `npm run typecheck` | 0 | Completed without type errors. |
| `npm run build` | 0 | Vite built 48 modules; known 500 kB chunk-size warning only. |
| `git diff --check 473e4f6..d31b372`; working-tree `git diff --check` | 0 | No whitespace errors. |
| `git cat-file -e 473e4f6`; `git cat-file -e d31b372` | 0 | Both recorded commits resolve. |
| `git merge-base --is-ancestor d31b372 main` | 1 (expected) | Implementation is not merged into `main`. |

`npm run test:rules` is not applicable because UXS-005 does not change Firebase Rules.

## Reviewer Boundary

I modified no production code or tests. This review record and the accompanying `TASK_STATUS.md` transition are the only intended review changes. The Coder must make only the minimum evidence correction above, return UXS-005 to `READY_FOR_REVIEW`, and must not start UXS-006.
