# REM-008 Review R1 - APPROVED

## Decision

`APPROVED`

Reviewed implementation: `6968d6a` on base `69b22b7`.

## Findings

No blocking findings.

- `main.js` now only composes deposit dependencies and invokes `depositController.start(user)` / `stop()` from the Auth lifecycle. It contains no deposit CRUD, settlement, rollover, reminder, archive, or delete branch.
- Each `start(user)` first stops the prior session, then creates one repository and one snapshot subscription. `stop()` unsubscribes, removes all seven runtime listeners, clears the midnight timer, closes the form and reminder dialog, resets UI/document state, and is safe before a first start.
- The migrated form, view, sync, reminder, and term modules have no remaining production import through their former `src/js/deposit-*.js` paths. Dependency injection preserves browser confirmation behavior while making lifecycle tests deterministic.
- Settlement continues to use date-only validation and VND safe-integer amounts. The controller passes only `actualInterestVnd` to the legacy-ledger writer; deposit principal is not written as income or an expense.

## Verification

| Check | Result |
|---|---|
| Base/head, complete diff, affected imports, and `main.js` boundary | PASS |
| Controller/form/view/reminder/settlement targeted tests | 41 passed |
| Direct moved-sync and settlement-contract tests | 8 passed |
| Firestore Rules and deposit critical path | 16 passed, emulator project `demo-no-project` |
| Full test suite | 334 passed / 20 skipped |
| Typecheck | exit 0 |
| Production build | exit 0; existing 861.47 kB chunk-size warning |
| `git diff --check 69b22b7..6968d6a` | exit 0 |

No deployment, online Firebase/Auth/Rules/data change, real financial data, email, or UID was introduced by this review.
