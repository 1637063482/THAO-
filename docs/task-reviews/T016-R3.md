# T016 Review R3

## Decision

`APPROVED`

## Scope

- Base SHA: `f363b2b454924926fbb98240670c0487e967737a`
- Previous reviewed implementation: `02ee0113c074efde24860694361b70a28156f5a5`
- R3 implementation head: `ed734572fb2fa16892617d07185ebb4060073bb8`
- Current branch head at review start: `97bd51cf14be75f29295e697e3fa3c271db02c82`
- Review round: `3`
- Full implementation range reviewed: `f363b2b4..ed734572`
- Rework increment reviewed: `02ee0113..ed734572`
- Evidence/status follow-up range reviewed: `ed734572..97bd51cf`

The R3 rework resolves the R2 cloud-write timing finding without extending T016 scope. No Firebase configuration/rules, production data, historical cloud amount, T011/T012 path, exchange-rate provider, FX snapshot, multi-base-currency model, streak, or date-boundary behavior changed. `ed734572` remains on `task/t016-vnd-currency-view` and is not an ancestor of `main`.

## Findings

No Critical, High, Medium, or Low blocking findings.

The CNY direct-cell input handler now marks a field dirty and returns before any raw/state/pending mutation or cloud scheduling at `src/js/main.js:82-97`. On focusout, `src/js/main.js:119-135` computes the final VND value once, then uses the shared persistence and scheduling functions at `src/js/main.js:61-80`. This removes the 800ms window in which the rounded interim VND could be copied into a cloud batch.

`tests/unit/currency-view.test.js:138-163` holds the CNY field for 900ms after the input event, proves state/pending remain `1234` and `triggerCloudSave()` is not called, then proves blur retains `1234` and schedules exactly one save. This covers the R2 timing path rather than only final in-memory state.

## Completion Criteria

| Criterion | Result | Evidence |
|---|---|---|
| CNY/VND display-only switches leave VND facts and pending updates unchanged | PASS | `tests/unit/currency-view.test.js:165-190` performs 100 switches with persistence deep equality. |
| Large, zero, and decimal displays are covered | PASS | `tests/unit/currency-view.test.js:96-102`. |
| Automatic and manual exchange-rate paths are covered | PASS | Automatic direct-cell path at `104-163`; manual Quick Add at `192-211`. |
| Direct-table CNY round-trips cannot persist a rounded interim VND fact | PASS | `src/js/main.js:82-97,119-135`; timing regression test at `138-163`. |
| CNY direct-cell persistence has one final VND boundary | PASS | `src/js/main.js:61-80,119-135`; no CNY input state/pending/save work occurs before blur. |
| Quick Add converts CNY once and stores VND | PASS | `src/js/quick-add.js:49-72`; `tests/unit/currency-view.test.js:192-211` expects `=5000`, not `=20000000`. |
| No Firebase, T011/T012, historical-data, FX-provider, or multi-base-currency scope expansion | PASS | Full diff `f363b2b4..ed734572`; changed-file inspection. |
| Evidence records business-assertion RED/GREEN for each rework | PASS | `docs/review-evidence/T016_GREEN.md`, including R1 and R2 Rework sections. |
| Implementation remains unmerged pending approval | PASS | `git merge-base --is-ancestor ed734572 main` exited `1`. |

## Verification Performed

| Command | Exit code | Key output |
|---|---:|---|
| `npm test -- --run tests/unit/currency-view.test.js` | 0 | 1 file, 6 tests passed. |
| `npm test -- --run` | 0 | 13 files passed, 2 skipped; 97 tests passed, 7 skipped. |
| `npm run typecheck` | 0 | `tsc -p tsconfig.json --noEmit` completed. |
| `npm run build` | 0 | Vite built 41 modules. Existing chunk-size warning was emitted; no build error. |
| `git diff --check f363b2b4..ed734572` | 0 | Full implementation range is whitespace-clean. |
| `git diff --check ed734572..97bd51cf` | 0 | Evidence/status follow-up range is whitespace-clean. |
| `git merge-base --is-ancestor ed734572 main` | 1 | Expected: the approved implementation is not merged into `main`. |

## Reviewer Boundary

The reviewer modified no business code or tests. This review record and the accompanying `TASK_STATUS.md` transition are the only intended changes in the review commit.
