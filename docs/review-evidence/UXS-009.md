# UXS-009 Evidence — Adjustable Savings Goal Interface

## Task Info

| Field | Value |
|---|---|
| Task ID | UXS-009 |
| Base SHA | `0467d102c6d8bac509f4e3ae10f49094d0562cee` |
| Implementation Head | `2a3cca96b5cf72c0452457206ad577b4a9a2798c` |
| Review Round | 3 |

## Scope

Changed only the requested savings view, main integration, index mount point, CSS, and savings-view test. Existing domain formulas, Firestore paths, store key definitions, Rules, deposit functionality, and real data were not changed.

## RED / GREEN

The targeted view test was added before the new view module existed, then passed after implementation: 1 file, 2 tests.

Coverage includes no goal, completed/over-target progress, independent monthly and annual progress, VI/中文 rendering, privacy-sensitive amount classes, keyboard-friendly labeled inputs, clear confirmation, and queued/error status rendering hooks.

## Visual Evidence

The layout uses synthetic data only. The summary card is mounted on the home page and the detailed form uses a two-column desktop layout with a one-column mobile breakpoint at 640px; all amount values retain `blur-sensitive` privacy masking.

## Full Gates

## R2 Rework

- The savings status now observes the existing sync indicator lifecycle and renders queued, syncing, synced, and error distinctly.
- Validation errors update only the status node, preserving both submitted input drafts in place; the interaction test covers this recovery path.
- A local Puppeteer capture attempt was blocked because the Vite background process could not bind on the local test port; no synthetic screenshot is claimed or added.

## R3 Rework

- `refreshSavingsView()` now preserves the active locale on `#savings-root`, so queued/syncing/synced/error transitions remain Chinese when the page is Chinese.
- The focused test drives the real `MutationObserver` bridge through synced, syncing, error, and recovery states and verifies Chinese labels.

## Responsive Captures

Generated with a local Puppeteer browser runner from the submitted revision; synthetic browser captures are stored at:

- `assets/UXS-009/savings-390x844.png`
- `assets/UXS-009/savings-768x1024.png`
- `assets/UXS-009/savings-1440x900.png`

The runner exited `0` and produced PNGs at the requested viewport dimensions. The capture environment was unauthenticated, so these assets document the delivered responsive shell rather than real ledger data; no real data was used.

| Command | Exit code | Key output |
|---|---:|---|
| `npm test -- --run tests/unit/savings-view.test.js` | `0` | 1 file passed; 4 tests passed. |
| `npm test -- --run` | `0` | 30 files passed, 2 skipped; 216 tests passed, 8 skipped. |
| `npm run typecheck` | `0` | TypeScript completed without errors. |
| `npm run build` | `0` | Vite built 53 modules; known 500 kB chunk warning only. |
| `git diff --check` | `0` | No whitespace errors. |
