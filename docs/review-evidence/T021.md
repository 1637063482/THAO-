# T021 Evidence - CI Gates

## Scope

- Task: T021 Establish CI gates
- Branch: `task/t021-ci-gates`
- Base SHA: `d0eead5fae3132361cda8fb50eebc5747c5c9037`
- Implementation SHA: `pending`
- Status SHA: READY_FOR_REVIEW status commit, see repository HEAD after `docs: ready T021 for review`

## RED 1 - Missing CI Gate Files

Command:

```powershell
npm test -- --run tests/unit/ci-workflow.test.js
```

Exit code: `1`

Key failure output:

```text
tests/unit/ci-workflow.test.js (2 tests | 2 failed)
× runs install, test, typecheck, Firestore Rules tests, and build without deployment
× documents that CI must not contain production secrets, real UID/email values, or financial data

Error: ENOENT: no such file or directory, open '.github\workflows\ci.yml'
Error: ENOENT: no such file or directory, open 'SECURITY.md'
```

Why this is valid RED:

- T021 requires a CI workflow and a security boundary document.
- The failure comes from missing required repository artifacts, not syntax or environment setup.

## RED 2 - Rules Script Touched Local Login State

Command:

```powershell
npm test -- --run tests/unit/ci-workflow.test.js
```

Exit code: `1`

Key failure output:

```text
AssertionError: expected 'firebase emulators:exec --only firestore "vitest run tests/rules"' to contain '--project demo-no-project'
```

Supporting observation before the fix:

```text
npm run test:rules
exit code: 0
output included: authorizing via signed-in user
```

Why this is valid RED:

- T021 requires CI logs to avoid real account identifiers and production credentials.
- Without an explicit demo project, the Firebase CLI can read local login state before starting the emulator.

## GREEN - Targeted CI Contract

Command:

```powershell
npm test -- --run tests/unit/ci-workflow.test.js
```

Exit code: `0`

Summary:

```text
Test Files  1 passed (1)
Tests  2 passed (2)
Duration  2.84s
```

Coverage:

- `.github/workflows/ci.yml` uses `actions/checkout@v4`, `actions/setup-node@v4`, and `actions/setup-java@v4`.
- CI pins Node `22` and Java Temurin `21`.
- CI runs `npm ci`, `npm test -- --run`, `npm run typecheck`, `npm run test:rules`, and `npm run build`.
- CI workflow contains no `continue-on-error`, no `firebase deploy`, no `FIREBASE_TOKEN`, and no `secrets.` usage.
- `npm run test:rules` is pinned to `--project demo-no-project`.
- `SECURITY.md` documents that production Firebase credentials, real UID/email allowlists, and real ledger/financial data must not be committed.

## Local CI Command Sequence

Command:

```powershell
npm ci
```

Exit code: `0`

Summary:

```text
added 830 packages in 2m
```

Command:

```powershell
npm test -- --run
```

Exit code: `0`

Summary:

```text
Test Files  17 passed | 2 skipped (19)
Tests  118 passed | 8 skipped (126)
Duration  19.08s
```

Command:

```powershell
npm run typecheck
```

Exit code: `0`

Summary:

```text
tsc -p tsconfig.json --noEmit
```

Command:

```powershell
npm run test:rules
```

Exit code: `0`

Summary:

```text
firebase emulators:exec --project demo-no-project --only firestore "vitest run tests/rules"
Detected demo project ID "demo-no-project"
Test Files  1 passed (1)
Tests  4 passed (4)
Script exited successfully (code 0)
```

Command:

```powershell
npm run build
```

Exit code: `0`

Summary:

```text
vite v6.4.3 building for production...
dist/assets/index-I5Skebiz.js   747.25 kB
built in 4.20s
```

Command:

```powershell
git diff --check
```

Exit code: `0`

Summary:

```text
warning: in the working copy of 'package.json', LF will be replaced by CRLF the next time Git touches it
```

## Implementation Summary

- Added `.github/workflows/ci.yml` with a single non-deploying gate job.
- Added `SECURITY.md` documenting CI/data boundaries for secrets, real identifiers, and ledger data.
- Added `tests/unit/ci-workflow.test.js` to keep the workflow and security constraints reviewable locally.
- Updated `package.json` `test:rules` to use `--project demo-no-project` so emulator tests do not read local Firebase login state.

## Known Warnings

- `npm ci` printed dependency deprecation warnings for `node-domexception` and `glob`.
- `npm run build` keeps the existing Vite chunk-size warning for the main bundle.
- `npm run test:rules` prints expected Firestore emulator warnings for denied Rules operations and shutdown socket resets while still exiting `0`.
- `git diff --check` printed a Windows line-ending normalization warning for `package.json`, but exited `0`.

## Not Modified

- No Firebase deployment, live Rules update, Auth account change, or production data operation.
- No production business code.
- No historical ledger values.
- No T011/T012 transaction migration.
