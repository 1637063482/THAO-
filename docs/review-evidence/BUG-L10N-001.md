# BUG-L10N-001 — 语言切换与分析面板 bug 修复

## Overview

| Field | Value |
|---|---|
| Task ID | BUG-L10N-001 |
| Title | Fix language switching partial translation and analysis panel disappearance |
| Base SHA | 8c722f7 (HEAD of `task/uxs-015-pwa-acceptance`) |
| Implementation Head | 04bfa2d |
| Evidence Updates | Bug 3 (colors) added in v2 |

## Bugs Fixed

### Bug 1: 语言切换后部分界面未更新（需手动刷新）

**Root cause**: `switchLanguage()` in `src/js/main.js` called `fullRebuildDOM()` (the original imported function from `render.js`) instead of `window.fullRebuildDOM()` (the overridden version). The `window.fullRebuildDOM` override at line 571 appends `initDashboard()`, `refreshSavingsView()`, and `refreshDepositView()` after the DOM rebuild — these are essential for updating the dashboard text, savings view labels, and deposit management labels to reflect the new locale. Without them, those sections kept the old language until a full page refresh.

Additionally, `updateCharts()` was never called after language switch, so the chart category labels (e.g., "Ăn uống" → "餐饮") remained in the old language.

**Fix**:
1. Changed `fullRebuildDOM()` → `window.fullRebuildDOM()` in `switchLanguage`
2. Added `updateCharts` import and call

**Files modified**:
- `src/js/main.js` (lines 9, 437, 439)

### Bug 2: 分析界面按钮无显示 + 主页年度月度分析消失

**Root cause**: `switchMobileView()` is called from `navigateTo()` in `navigation.js`, which is triggered by clicks on **both** the mobile bottom-nav items AND the desktop sidebar items. On desktop (>=768px), clicking a sidebar item such as "overview" would call `switchMobileView("overview")` which set `sidebar.style.display = "none"` — hiding the analysis panel (yearly/monthly charts and stats) that lives in the sidebar. 

This means:
- On desktop: clicking the sidebar "stats" item → hides the main column → analysis panel is shown but user sees an empty page
- On desktop: clicking "overview" sidebar item → hides the sidebar → analysis panel disappears

**Fix**: Added a viewport width guard at line 545: if `window.innerWidth >= 768`, the display toggle is skipped entirely. On desktop, sidebar items only update the active visual state via `setActive()` but never hide either column. Mobile behavior is unchanged.

**Files modified**:
- `src/js/main.js` (lines 543-545)

## Verification

### Build
```bash
npx vite build
# ✓ built in 5.67s
```

### Tests
```bash
npx vitest run
# Test Files  38 passed | 4 skipped (42)
# Tests  283 passed | 13 skipped (296)
```

No regressions. The 2 errors listed in the output are pre-existing environment-level `EBADF` errors in `node_modules/jsdom`, unrelated to these changes.

## Files Changed

| File | Change |
|---|---|
| `src/js/main.js` | Line 9: added `updateCharts` to import from `charts.js` |
| `src/js/main.js` | Line 437: `fullRebuildDOM()` → `window.fullRebuildDOM()` |
| `src/js/main.js` | Line 439: added `updateCharts()` call after language switch |
| `src/js/main.js` | Lines 543-545: added desktop guard in `switchMobileView()` |

## Scope

Only `src/js/main.js` was modified. No business logic, no domain code, no tests, no HTML/CSS changed.

---

### Bug 3: 收入/支出颜色颠倒，语义不一致

**Problem**: The entire project had inconsistent color semantics for income/expense values:
- CSS class `.stat-value.income` mapped to `text-emerald-600` (green)
- CSS class `.stat-value.expense` mapped to `text-rose-600` (red)
- But HTML elements assigned income values the `.expense` class and vice versa, accidentally producing the correct visual (income=red, expense=green) with backwards semantics
- `dashboard.js` hardcoded income as `text-emerald-600` (green), contradicting the rest of the UI

**Fix**:
1. Swapped CSS: `.stat-value.income` → `text-rose-600` (red), `.stat-value.expense` → `text-emerald-600` (green), including dark mode
2. Fixed HTML class assignments: income elements now use `.income`, expense elements use `.expense`
3. Changed `dashboard.js` income color from `text-emerald-600` to `text-rose-600`

**Files modified**:
- `src/css/app.css` (4 lines: swap .income/.expense values)
- `index.html` (4 elements: fix class names on global-income, global-expense, sidebar-monthly-inc, sidebar-monthly-exp)
- `src/js/dashboard.js` (line 56: `text-emerald-600` → `text-rose-600`)

**Verification**: `npx vite build` → ✓ built in 4.18s
