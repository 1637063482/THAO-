# T018 Evidence - Import Recovery Before Overwrite

## Scope

- Task: T018 Add recovery point before overwrite import
- Branch: `task/t018-import-recovery`
- Base SHA: `470c43066906cb4b0856da6f22910d1f46f84f14`
- Implementation SHA: `ccd5f40e1fd6f60197a275e3668b1196b6bf8bdc`
- Status SHA: READY_FOR_REVIEW status commit, see repository HEAD after `docs: ready T018 rework for review`

## Implementation Summary

- Added an import recovery path before overwrite writes.
- The app validates the selected import JSON first, asks for the existing overwrite confirmation, reads the current active-year ledger, serializes only `balances`, `entries`, and `settings`, computes a SHA-256 hash, and dispatches a local JSON recovery download before calling `setDoc(..., { merge: false })`.
- User cancellation exits before reading, downloading, or writing.
- Recovery download failure throws `BACKUP_FAILED` and exits before the overwrite write.
- Write failure occurs only after the recovery file has been generated, so the previous ledger can be restored through the existing import flow.
- No production backup collection, Firebase Rules, Auth account, real data migration, CNY/VND behavior, T011, or T012 code was changed.

## Files Changed

- `src/js/sync.js`
- `src/js/import-schema.js`
- `tests/unit/import-backup.test.js`
- `IMPORT_RECOVERY.md`
- `docs/review-evidence/T018.md`
- `TASK_STATUS.md`

## RED

Command:

```powershell
npm test -- --run tests/unit/import-backup.test.js
```

Exit code: `1`

Key failure output:

```text
FAIL  tests/unit/import-backup.test.js > import recovery backup > cancels before backup or overwrite
TypeError: importLegacyLedgerWithRecovery is not a function

FAIL  tests/unit/import-backup.test.js > import recovery backup > stops before overwrite when the recovery download fails
TypeError: importLegacyLedgerWithRecovery is not a function

FAIL  tests/unit/import-backup.test.js > import recovery backup > does not mutate the imported ledger or log financial data when overwrite write fails
TypeError: importLegacyLedgerWithRecovery is not a function

FAIL  tests/unit/import-backup.test.js > import recovery backup > downloads a schema-valid recovery file with a matching hash before overwrite
TypeError: importLegacyLedgerWithRecovery is not a function

FAIL  tests/unit/import-backup.test.js > import recovery backup > creates a recovery point for each repeated overwrite
TypeError: importLegacyLedgerWithRecovery is not a function

Test Files  1 failed (1)
Tests  5 failed (5)
```

Why this is valid RED:

- The pre-change import path had no recovery-before-overwrite abstraction and no recovery point behavior to exercise.
- The failing tests described the required terminal states before implementation: cancel, backup failure, write failure, valid recovery schema/hash, and repeated import recovery.
- The failure was not caused by jsdom setup, syntax parsing, Firebase connection, or time-dependent behavior.

## GREEN - Targeted

Command:

```powershell
npm test -- --run tests/unit/import-backup.test.js
```

Exit code: `0`

Summary:

```text
Test Files  1 passed (1)
Tests  6 passed (6)
Duration  2.78s
```

Covered assertions:

- Cancel before backup or overwrite.
- Recovery download failure stops before overwrite.
- Write failure does not mutate the imported ledger or log financial data.
- Recovery JSON passes the existing legacy import schema.
- Recovery hash matches the serialized recovery content.
- Repeated overwrite operations each create a distinct recovery point.
- SHA-256 hash generation is deterministic.

## Full Gates

Command:

```powershell
npm test -- --run
```

Exit code: `0`

Summary:

```text
Test Files  14 passed | 2 skipped (16)
Tests  103 passed | 8 skipped (111)
Duration  13.14s
```

Command:

```powershell
npm run typecheck
```

Exit code: `0`

Summary:

```text
tsc -p tsconfig.json --noEmit
```

Command:

```powershell
npm run build
```

Exit code: `0`

Summary:

```text
vite v6.4.3 building for production...
dist/index.html                  26.35 kB
dist/assets/index-luYmF_1P.css   59.78 kB
dist/assets/index-DHfNBlpF.js   744.39 kB
✓ built in 7.11s
```

Command:

```powershell
git diff --check
```

Exit code: `0`

## Additional Verification

Command:

```powershell
npm run test:rules
```

Exit code: `0`

Summary:

```text
Test Files  1 passed (1)
Tests  4 passed (4)
```

## Known Warnings

- `npm run build` keeps the existing Vite chunk-size warning for the main bundle.
- `git diff --check` printed Git line-ending normalization warnings for touched JavaScript files on Windows, but exited `0`.
- `npm run test:rules` printed Firebase emulator and expected denied-operation logs. Evidence intentionally omits local account identifiers.

## Recovery Documentation

- `IMPORT_RECOVERY.md` documents recovery filename, hash meaning, restore steps, and failure behavior.
- The recovery file is a normal legacy import JSON, so it can be restored through the existing JSON import control.

## Not Modified

- No Firebase Rules deployment or live Auth/account changes.
- No production backup collection.
- No real ledger data migration.
- No CNY/VND rule changes.
- No T011/T012 behavior changes.
- No unrelated refactor.

## R1 Rework

Reviewer: `docs/task-reviews/T018-R1.md`

Rework scope:

- Original R1 reviewed implementation SHA: `9aa947b5277448fd30a9732e9b8d320834dfeaff`
- R1 rework implementation SHA: `ccd5f40e1fd6f60197a275e3668b1196b6bf8bdc`
- Added schema validation for the normalized current-ledger recovery payload before hash/download/write.
- Added a verifiable local persistence boundary to the production recovery adapter: write to `localStorage`, read back the exact payload, and block overwrite if verification fails.
- Kept browser download dispatch after local verification, with download adapter failures reported as `BACKUP_FAILED`.
- Corrected the T018 base SHA in evidence/status to `470c43066906cb4b0856da6f22910d1f46f84f14`.

R1 RED command:

```powershell
npm test -- --run tests/unit/import-backup.test.js
```

Exit code: `1`

R1 RED key failure output:

```text
FAIL  tests/unit/import-backup.test.js > import recovery backup > stops before overwrite when the current ledger cannot become a valid recovery file
AssertionError: promise resolved "{ ok: true, ... }" instead of rejecting

FAIL  tests/unit/import-backup.test.js > import recovery backup > persists and verifies the recovery locally before dispatching the download
AssertionError: expected undefined to match object { ... }

FAIL  tests/unit/import-backup.test.js > import recovery backup > refuses the recovery download when local persistence cannot be verified
AssertionError: promise resolved "undefined" instead of rejecting

Test Files  1 failed (1)
Tests  3 failed | 6 passed (9)
```

R1 GREEN targeted command:

```powershell
npm test -- --run tests/unit/import-backup.test.js
```

Exit code: `0`

Summary:

```text
Test Files  1 passed (1)
Tests  9 passed (9)
Duration  3.20s
```

R1 full gates:

```powershell
npm test -- --run
```

Exit code: `0`

```text
Test Files  14 passed | 2 skipped (16)
Tests  106 passed | 8 skipped (114)
Duration  12.55s
```

```powershell
npm run typecheck
```

Exit code: `0`

```text
tsc -p tsconfig.json --noEmit
```

```powershell
npm run build
```

Exit code: `0`

```text
vite v6.4.3 building for production...
dist/assets/index-BAvB94es.js   745.19 kB
built in 6.35s
```

```powershell
git diff --check
```

Exit code: `0`

Known R1 warnings:

- `npm run build` keeps the existing Vite chunk-size warning for the main bundle.
- `git diff --check` printed Git line-ending normalization warnings for touched files on Windows, but exited `0`.
