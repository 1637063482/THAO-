# UXS-011 Evidence

## Scope

- Base: `06b235f`
- Implementation head: `bafaac4612b00e2648823a808b3960386ae64869`
- Candidate Rules were exercised only against `demo-no-project`; nothing was deployed and no real Firebase data was read or written.

## RED

`npm test -- --run tests/unit/deposit-schema.test.js tests/integration/deposit-repository.test.ts`

- Exit code: 1
- Both suites failed import resolution because `deposit-schema.js` and `deposit-repository.ts` did not exist.

## GREEN

| Command | Exit | Result |
|---|---:|---|
| `npm test -- --run tests/unit/deposit-schema.test.js` | 0 | 10 passed |
| `npm run test:rules` | 0 | 7 Rules tests passed, including legacy-ledger regression |
| local Firebase 15.24 emulator + repository test | 0 | 2 integration tests passed |
| `npm test -- --run` | 0 | 236 passed, 13 skipped |
| `npm run typecheck` | 0 | strict TypeScript passed |
| `npm run build` | 0 | 54 modules transformed |
| `git diff --check` | 0 | no whitespace errors |

## Review notes

- A fixed `shared_ledger_savings` document owns `depositsById` and `acknowledgementsByKey`; missing document is a legal in-memory empty state.
- Every mutation is transactional, version checked, and carries server-generated audit timestamps.
- Runtime validation rejects unknown fields, invalid dates/status/rates/amounts, unsafe integers and oversized maps.
- Rules preserve legacy yearly-ledger behavior, restrict the savings document to the two established fixture-email identities, deny delete/default access, and validate the one key named by `lastMutation`.
- `lastMutation` was added to ADR-004 because Firestore Rules cannot iterate arbitrary dynamic map members. It is implementation metadata and is excluded from backups.
- `deposit-schema.d.ts` is the minimal declaration needed for strict TypeScript repository code to reuse the JavaScript runtime validator.
- Emulator emits known permission-trace/connection-reset noise for denied requests and shutdown; all assertions and the command exit code passed.
- Build retains the existing large-chunk warning: 779.06 kB minified main bundle.
