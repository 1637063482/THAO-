# UXS-001 Evidence — Define UI, Savings, and Deposit Architecture Boundary

## Task Info

| Field | Value |
|---|---|
| Task ID | UXS-001 |
| Base SHA | `0103f0884a1062ae176d43143327edad8d2f60cc` |
| Implementation Head | `10194eddc1522aec49c07d9c2389c3dad97a31cf` |
| Review Round | 1 |

## RED Phase

### RED Step: Scan for lingering conflicts in existing documents

Used `grep` (via Grep tool) across the four existing documents (`UI_SAVINGS_REDESIGN_PLAN.md`, `PRD.md`, `FRD.md`, `ARCHITECTURE_PLAN.md`) for the following conflict patterns:

| Pattern | Expected | Before Fix |
|---|---|---|
| `TBD\|TODO\|pending.*decision\|等待 T019` | Should be 0 | 0 — already clean |
| `T011\|T012` (outside reference context) | All must reference ADR-003 | Found in ARCHITECTURE_PLAN.md (lines 5, 161, 168, 281, 285) — all already in ADR-003 context, acceptable |
| Transaction migration / independent transaction source | Only legacy matrix | **FOUND**: PRD.md:31 — "同时以独立交易流水作为事实源" |
| English UI references | None as available language | UI_SAVINGS_REDESIGN_PLAN.md:32, 516, 616, 753 — correctly state "no English" |
| External notification references | Only describing prohibition | UI_SAVINGS_REDESIGN_PLAN.md:61, 74, 392-395, 703 — correctly describe prohibition |
| Registration / membership / multi-family | None in current scope | ARCHITECTURE_PLAN.md:40, 44, 58, 114 and FRD.md:3, 9 and PRD.md:3, 46, 161 — correctly describe as out-of-scope |
| Multi-currency (outside VND+CNY) | None in current scope | ARCHITECTURE_PLAN.md:168 — correctly references ADR-003 prohibition |

### RED Conflicts Found and Fixed

**Conflict 1 — PRD.md:31:**
- Before: `"同时以独立交易流水作为事实源"`
- Problem: Contradicted ADR-003 which confirmed legacy matrix as the fact source
- Fix: Changed to `"保留 legacy 年度矩阵（shared_ledger_<year>）作为日常账务事实源（ADR-003 已确认）"`

**Conflict 2 — PRD.md:121:**
- Before: `"T011/T012 仅是未接线的技术探索，是否继续由 T019 ADR 决定"`
- Problem: T019/ADR-003 has already decided (Option A: stabilize legacy matrix, reject migration)
- Fix: Updated to state ADR-003 is decided, T011/T012 not in delivery mainline

**Conflict 3 — PRD.md §7:**
- Before: Listed `"是否继续投入独立 Account/Transaction 迁移"` as still pending
- Problem: No longer a pending decision
- Fix: Restructured to list all confirmed decisions with ADR references

**Conflict 4 — FRD.md (4 FR sections):**
- FR-03 (Account), FR-04 (Transaction), FR-05 (Transfer), FR-09 (Reconciliation): These functional requirements describe the full Account/Transaction model rejected by ADR-003
- Fix: Added prominent "OUT OF SCOPE per ADR-003/ADR-004" notices at document top and in each section

**Conflict 5 — ARCHITECTURE_PLAN.md (4 sections):**
- §3 (Module reorganization): Full directory tree with transactions/, accounts/
- §4.1 (Firestore model): Full collections for accounts, transactions, etc.
- §4.3 (Indexes): Transaction collection indexes
- §4.5 (Migration strategy): Full migration plan from legacy to new model
- Fix: Added "SUPERSEDED per ADR-003/ADR-004" notices; added current-scope preamble at document top

### RED Verification Command

```powershell
rg (via Grep tool) across all 4 docs for conflict patterns
```

Expected: Found conflicts listed above — RED confirmed.

## GREEN Phase

### Changes Applied

**New file:** `docs/adr/004-ui-savings-boundary.md`
- Status: Accepted
- Covers: savings goal storage (§1), deposit storage (§2), reminder lifecycle (§3), mobile daily view (§4), PWA requirements (§5), internationalization (§6), UI style tokens (§7), non-goals (§8), alternatives considered (A/B/C/D), consequences
- ADR-003 references: Confirms legacy matrix, no T011/T012 reconnect

**Modified files:**
1. `UI_SAVINGS_REDESIGN_PLAN.md` — Added ADR-004 reference to document header
2. `PRD.md` — Fixed 3 conflict locations (fact source, T019 status, §7 pending items → confirmed)
3. `FRD.md` — Added scope preamble + 4 "OUT OF SCOPE" notices
4. `ARCHITECTURE_PLAN.md` — Added scope preamble + 4 "SUPERSEDED" notices

**Confirmed aligned (no changes needed):**
- `UI_SAVINGS_REDESIGN_PLAN.md` — Already consistent per ADR-003/UXS plan

### Full Gates Verification

| Gate | Exit Code | Result |
|---|---|---|
| `npm test -- --run` | 0 | 118 pass, 8 skip |
| `npm run typecheck` | 0 | No type errors |
| `npm run build` | 0 | Build succeeded (known chunk size warning, pre-existing) |
| `git diff --check` | 0 | No whitespace errors |

### Files Modified (exact list)

```
M  UI_SAVINGS_REDESIGN_PLAN.md
M  PRD.md
M  FRD.md
M  ARCHITECTURE_PLAN.md
A  docs/adr/004-ui-savings-boundary.md
```

### Files NOT Modified (per prohibition)

All production code, tests, Firebase/Cloudflare configuration, online resources, T011/T012 code — untouched. This task is documentation-only as specified.

### Rework (R1 → R2)

Following `docs/task-reviews/UXS-001-R1.md` (CHANGES_REQUESTED):

| Finding | Fix |
|---|---|
| **High — Deposit interest contract**: ppm vs decimal string ambiguity; calculatedInterestVnd persistence unclear | `UI_SAVINGS_REDESIGN_PLAN.md:345-358` — ppm exclusive, calculatedInterestVnd is derived (not persisted), only expectedInterestVnd/actualInterestVnd persisted. ADR-004:91 — rate rule says ppm exclusively. |
| **High — MATURED persisted vs derived**: ADR-004 schema listed MATURED as stored but rules said derived | ADR-004:73 — removed `MATURED` from persisted enum (`ACTIVE | REDEEMED | ROLLED_OVER`); ADR-004:95 — explicitly states MATURING/MATURED are derived only. UI_SAVINGS_REDESIGN_PLAN.md §9.2 — split into persisted vs derived state tables. |
| **Medium — Savings-goal representation**: §8.1/8.2 standalone records not mapped to 13-key settings schema | UI_SAVINGS_REDESIGN_PLAN.md §8.1/8.2 — added ViewModel-to-persistence mapping tables showing `savings_goal_month_N`/`savings_goal_annual` keys, noted document-level metadata. |
| **Low — Duplicated bullet**: Identical consecutive lines in ARCHITECTURE_PLAN.md | ARCHITECTURE_PLAN.md:43-45 — removed duplicate line. |

### Known Limitations

- Build warning about chunk size > 500 kB is pre-existing (noted in AGENTS.md), not introduced by this task.
- CRLF warnings in git diff are cosmetic on Windows, not whitespace errors.
