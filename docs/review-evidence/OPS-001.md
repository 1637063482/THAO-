# OPS-001 Evidence — Token-efficient Agent context loading

## Task

| Field | Value |
|---|---|
| Task ID | OPS-001 |
| Base SHA | `4c39dd0395678f9dbd7f140f44b9d68463aaf4c5` |
| Implementation Head | pending until implementation commit |
| Branch | `ops/context-loading-optimization` |
| Review Round | 1 |

## RED

Command:

```powershell
npm test -- --run tests/unit/task-context.test.js
```

Exit code: `1`.

Expected and observed failure: Vitest could not resolve `../../scripts/task-context.mjs`; the repository had no role-aware current-Task context loader.

A second RED cycle added repository-bound path validation: 8 tests passed and the new test failed with `TypeError: resolveRepoPath is not a function`. GREEN added canonical path validation so evidence/review paths cannot escape the repository.

## Implemented scope

- Replaced auto-injected `AGENTS.md` with a short router; archived the complete reference in `docs/AGENTS_FULL.md`.
- Added binding compact rules in `docs/CODEX_CONTEXT.md`.
- Added `scripts/task-context.mjs` and `context:coder` / `context:reviewer` npm commands.
- Moved completed history out of default `TASK_STATUS.md` into `docs/TASK_HISTORY.md`.
- Updated workflow, Task plan and Review plan for the compact default path and OPS-001 review contract.
- Added pure tests for status parsing, Task isolation, role/state selection, rework review inclusion, Reviewer evidence inclusion and invalid input rejection.

No business/UI code, Firebase/Cloudflare configuration, online resource, UXS-006 implementation or global `engineering-workflow` Skill was modified.

## Targeted GREEN and CLI smoke

| Command/check | Exit code | Result |
|---|---:|---|
| `npm test -- --run tests/unit/task-context.test.js` | 0 | 1 file, 9 tests passed |
| `npm run context:coder` | 0 | Included compact rules, OPS-001 status/Task; excluded UXS-001 and UXS-015 sections |
| `npm run context:reviewer` while `IMPLEMENTING` | 0 | Returned compact rules/status and `STOP`; no actionable Task/evidence payload |
| `git diff --check` | 0 | No whitespace errors |

## Measured repository-context reduction

Measurement uses repository-controlled default files only; the global platform Skill is explicitly excluded because OPS-001 does not modify it.

| Metric | Characters |
|---|---:|
| Previous default (`AGENTS` + workflow + full Task plan + full Review plan + status at base SHA) | 39,234 |
| New `context:coder` output for OPS-001 | 4,782 |
| Reduction | 87.8% |

The command reads the complete files internally but prints only the compact contract and actionable slices; unread file contents are not inserted into the model context.

## Full gates

| Command | Exit code | Result |
|---|---:|---|
| `npm test -- --run` | 0 | 25 files passed, 2 skipped; 189 tests passed, 8 skipped |
| `npm run typecheck` | 0 | `tsc -p tsconfig.json --noEmit` completed without errors |
| `npm run build` | 0 | Vite built 48 modules; existing chunk-size warning remains, main JS 768.44 kB |
| `git diff --check` | 0 | No whitespace errors |

## Known limitation

The platform-managed `engineering-workflow/SKILL.md` remains outside repository scope and may still be loaded when its trigger applies. Changing that global Skill would affect other repositories and requires separate user authorization.
