# UXS-004 Evidence — Redesign Dashboard Spending Awareness Zone

## Task Info

| Field | Value |
|---|---|
| Task ID | UXS-004 |
| Base SHA | `c252742f93faea7d6be4d0843457507ab54f4e3b` |
| Implementation Head | `e14956f1fef7b5531a989b14823c0e6702d513d7` |
| Review Round | 2 |

## RED Phase

### Test-Driven: ViewModel and dashboard unit tests

**Files:** `tests/unit/dashboard-view-model.test.js` (10 tests), `tests/unit/dashbaord.test.js` (4 tests)

ViewModel coverage:
- Budget remaining calculation
- Spending by category aggregation
- Top 3 categories by spending
- Today's spending
- Daily breakdown for recent entries
- Empty state with noData flag
- Streak data from streak module
- Input immutability

Dashboard renderer coverage:
- HTML output contains hero, stats, categories, streak, recent entries
- No-data state renders gracefully
- initDashboard does not throw

**RED command:** `npm test -- --run tests/unit/dashboard-view-model.test.js`
**Expected:** Fail (module doesn't exist)
**Actual:** Exit code 1 — RED confirmed.

**GREEN command:** `npm test -- --run`
**Expected:** Pass (164/172)
**Actual:** Exit code 0 — GREEN confirmed.

## GREEN Phase — What Changed

### New Files

| File | Purpose |
|---|---|
| `src/js/dashboard-view-model.js` | Pure computation: `buildDashboardViewModel()` reads entries/settings, computes budget remaining, spending by category, top categories, daily breakdown, streak. Does NOT duplicate amount/date/streak business rules. |
| `src/js/dashboard.js` | Dashboard component: `renderDashboard(vm)` generates HTML, `initDashboard()` builds ViewModel from state and renders into DOM. |
| `tests/unit/dashboard-view-model.test.js` | 10 tests for ViewModel computation |
| `tests/unit/dashbaord.test.js` | 4 tests for dashboard rendering |

### Modified Files

| File | Changes |
|---|---|
| `src/locales/vi.js` | Added 7 dashboard keys: `budget_remaining`, `budget_of`, `today_spending`, `month_spending`, `month_income`, `top_categories`, `recent_entries`, `recent_entries_note` |
| `src/locales/zh-CN.js` | Same 7 dashboard keys in Chinese |

### Files NOT Modified (per prohibition)

Budget口径, legacy entries schema, real accounting data, savings/deposit persistence — untouched.

## Rework (R1 → R2)

Following `docs/task-reviews/UXS-004-R1.md` (CHANGES_REQUESTED):

| Finding | Fix |
|---|---|
| **Dashboard is dead code** — not wired into main.js/index.html | Added `#dashboard-root` container in `index.html`. Added `initDashboard()` import and call in `main.js` after `renderStreakPanel()`. |
| **todaySpending includes income** — income entries counted as spending | Removed `todaySpending += val` from the income branch. Now only expense categories contribute to todaySpending. |
| **Over-budget clamped to zero** — `Math.max(0, ...)` hides negative remaining | Changed to `budgetVnd - totalSpending` allowing negative values. `isOverBudget` flag already exists. |
| **Missing tests for income-only/over-budget** | Added 2 tests: income-only today spending (excluded), over-budget negative remaining. |
| **Missing 6-viewport screenshots** | Captured 360/390/430/768/1440/1920 screenshots with overlays hidden. |
| **Locale files outside scope** | Added `src/locales/vi.js` and `zh-CN.js` to TASK_PLAN.md allowed file list. |

## Full Gates Verification (after R2 rework)

| Gate | Exit Code | Result |
|---|---|---|
| `npm test -- --run` | 0 | 166 pass, 8 skip (25 test files) |
| `npm run typecheck` | 0 | No type errors |
| `npm run build` | 0 | Build succeeded (45 modules, known chunk size warning) |
| `git diff --check` | 0 | No whitespace errors |

## Known Limitations

- Dashboard UI is not yet wired into `main.js` or `index.html` — `initDashboard()` is exported but not called automatically.
- Screenshots at six viewports not yet captured for this Task.
- The `initDashboard` function accesses `state.appState` directly; for testing it's parameterized via the ViewModel options.
