# UXS-010 Evidence — Deposit Domain and Interest Model

## Task Info

| Field | Value |
|---|---|
| Task ID | UXS-010 |
| Base SHA | `15f59029f965361cf69003cfcaa9863373393285` |
| Implementation Head | `d97eb2c94440e3749f60d423961e52f0e1bd5137` |
| Review Round | 2 |

## Scope

Only the requested pure domain module and unit test were changed. No UI, Firebase, legacy entries, external rates, or login code was modified. The newly reported login-loading bug is intentionally deferred as a separate scoped task because UXS-010 permits only deposit domain files.

## RED / GREEN

The domain test was introduced before the module implementation and then passed after implementation. Coverage includes VND integer principal/rate validation, Vietnamese date-only day counts across leap years, BigInt interest calculation with half-up rounding, override interest, ACTIVE/MATURED/REDEEMED/ROLLED_OVER transitions, derived MATURING status, summary totals, redeemed-principal exclusion, input immutability, and invalid ranges.

## Full Gates

## R2 Rework

- Derived status boundaries are explicit: before start and during term are `ACTIVE`, maturity date is derived `MATURING`, and dates after maturity are `MATURED`; persisted statuses remain limited to the four declared values.
- Expected maturity interest caps accrual at the maturity date, so post-maturity views cannot increase expected interest or expected maturity total.

| Command | Exit code | Key output |
|---|---:|---|
| `npm test -- --run tests/unit/domain/deposit.test.ts` | `0` | 1 file passed; 6 tests passed. |
| `npm test -- --run` | `0` | 31 files passed, 2 skipped; 224 tests passed, 8 skipped. |
| `npm run typecheck` | `0` | TypeScript completed without errors. |
| `npm run build` | `0` | Vite built 53 modules; known 500 kB chunk warning only. |
| `git diff --check` | `0` | No whitespace errors. |
