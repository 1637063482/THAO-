# UXS-006 Evidence — Derived Daily Ledger and Full Table Toggle

## Task Info

| Field | Value |
|---|---|
| Task ID | UXS-006 |
| Base SHA | `118278304078429a5874e1effc9a8359659226d5` |
| Implementation Head | `47804db22df1fd1894747ac65f0691ecdb42664c` |
| Review Round | 1 |

## Scope

Changed only the UXS-006 allowed files: `src/js/day-ledger.js`, `src/js/render.js`, `src/js/main.js`, `index.html`, `src/css/app.css`, the two new unit tests, this evidence file, and the six synthetic screenshot assets. No legacy key/schema, transaction model, sync protocol, amount rule, or full-table editing logic was changed.

## RED / GREEN

Initial command:

```text
npm test -- --run tests/unit/day-ledger.test.js tests/unit/ledger-view-toggle.test.js
```

Exit code: `1`; the new tests could not resolve the not-yet-created `src/js/day-ledger.js` module.

After implementing the mapping layer and renderer integration:

```text
npm test -- --run tests/unit/day-ledger.test.js tests/unit/ledger-view-toggle.test.js tests/unit/dashbaord.test.js
```

Exit code: `0`; 3 files, 15 tests passed.

Coverage includes:

- income, expense categories, remarks, date keys, empty state, and formula-derived values;
- source-key preservation and write-back to the same legacy cell;
- mobile daily/default and desktop table/default view state;
- toggle state preserving pending updates;
- rendered daily inputs carrying the original `data-key`.

## Implementation Notes

- `buildDailyLedger()` derives daily/category cards from the existing `<month>_<day>_<category>` matrix.
- Daily inputs carry the source key and use the existing `main.js` input persistence path.
- No transaction IDs or new persistence schema are created.
- Mobile widths below 768 default to daily cards; desktop defaults to the existing full table.
- The explicit bilingual toggle is `Theo ngày / Bảng · 按日 / 表格`; no English UI entry was added.
- Full-table rendering remains available and is hidden only while daily view is active; existing local table inputs and horizontal table scrolling remain unchanged.

## Visual Evidence

Generated with Puppeteer against the Vite-served `index.html` page using synthetic legacy state only. Auth/loading overlays were hidden for capture, then the actual `state.js` was populated and `window.fullRebuildDOM()` rendered the delivered page.

| Viewport | Asset | Result |
|---|---|---|
| 360×800 | `assets/UXS-006/ledger-360x800.png` | Daily cards shown by default; bilingual toggle visible; no page horizontal overflow (`scrollWidth=360`). |
| 390×844 | `assets/UXS-006/ledger-390x844.png` | Daily/category cards and original source-backed fields visible; no page horizontal overflow (`scrollWidth=390`). |
| 768×1024 | `assets/UXS-006/ledger-768x1024.png` | Full legacy table shown by default; toggle remains available; `scrollWidth=768`. |
| 1440×900 | `assets/UXS-006/ledger-1440x900.png` | Desktop full table shown by default; `scrollWidth=1440`. |
| 844×390 landscape | `assets/UXS-006/ledger-844x390.png` | Table remains locally scrollable without page-level horizontal overflow (`scrollWidth=844`). |

The fixture uses only synthetic values: dining `120000`, transport `80000`, income `500000`, shopping `250000`, and a synthetic Vietnamese remark.

## Full Gates

Recorded before handoff on the implementation worktree:

| Command | Exit code | Key output |
|---|---:|---|
| `npm test -- --run` | `0` | 27 files passed, 2 skipped; 195 tests passed, 8 skipped. |
| `npm run typecheck` | `0` | TypeScript completed without errors. |
| `npm run build` | `0` | Vite built 49 modules; known 500 kB chunk warning only. |
| `git diff --check` | `0` | No whitespace errors. |
