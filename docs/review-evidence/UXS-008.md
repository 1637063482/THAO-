# UXS-008 Evidence — Persist Annual Savings Goal Settings

## Task Info

| Field | Value |
|---|---|
| Task ID | UXS-008 |
| Base SHA | `be23e87b4cf2b4afe84b808fa5087a4cac76e25b` |
| Implementation Head | `0467d102c6d8bac509f4e3ae10f49094d0562cee` |
| Review Round | 1 |

## Scope

Changed only the requested savings-goal store, import-schema validation/tests, and this evidence/status handoff. No UI, Firestore Rules, online data, or Account/Transaction code was changed. Existing sync queue semantics are reused through the caller-provided pending settings object; no goals collection or new persistence path was introduced.

## RED / GREEN

The targeted store/schema tests were added before implementation and then passed after the store and schema changes.

Coverage includes twelve monthly keys plus annual key, round-trip writes, explicit null clearing, legacy snapshots without new keys, invalid months/values, safe integer validation, and import compatibility.

## Full Gates

Gates passed on implementation head `0467d102c6d8bac509f4e3ae10f49094d0562cee`:

| Command | Exit code | Key output |
|---|---:|---|
| `npm test -- --run tests/unit/savings-goal-store.test.js tests/unit/import-schema.test.js` | `0` | 2 files passed; 23 tests passed. |
| `npm test -- --run` | `0` | 29 files passed, 2 skipped; 212 tests passed, 8 skipped. |
| `npm run typecheck` | `0` | TypeScript completed without errors. |
| `npm run build` | `0` | Vite built 49 modules; known 500 kB chunk warning only. |
| `git diff --check` | `0` | No whitespace errors. |
