# REM-016 R2 implementation evidence

- Base: `e24d333` (the reviewed REM-015 head).
- Scope: only the two REM-016 R1 blockers.
- No Firebase deployment, online Auth/Rules change, or production-data action.

## RED to GREEN

1. Added `tests/unit/composition-root.test.js`; it failed because `src/js/main.js` still contained `switchLanguage`, `switchCurrency`, `toggleDarkMode`, and `exportToCSV`.
2. Moved the existing application runtime to `src/js/application-runtime.js`. `src/js/main.js` now imports styles and calls `startApplication()` only.
3. Updated the existing savings-boundary test to verify application composition in the runtime while retaining the thin `main.js` assertion.
4. `scripts/task-context.mjs` now resolves the status Plan path and falls back to the task section when no separate review-plan row exists; `npm run context:reviewer` loads the REM-016 review bundle successfully.

## Verification

| Command | Result |
|---|---|
| `npx vitest run tests/unit/composition-root.test.js tests/unit/savings-controller.test.js --maxWorkers=1 --no-file-parallelism` | exit 0; 4 passed |
| `npm test -- --run` | exit 0; 343 passed / 16 skipped |
| `npm run test:rules` | exit 0; demo-no-project; 16 passed |
| `npm run typecheck` | exit 0 |
| `npm run typecheck:js` | exit 0 |
| `npm run build` | exit 0; main asset 629.61 kB |
| `node scripts/check-bundle-budget.mjs` | exit 0 |
| `git diff --check` | exit 0 |

The existing jsdom canvas notices and Vite chunk advisories remain non-failing warnings. The build budget remains under 650 kB.
