# UXS-003 Evidence — Establish Apple Warm Design Tokens and Responsive App Shell

## Task Info

| Field | Value |
|---|---|
| Task ID | UXS-003 |
| Base SHA | `a9fc9fcf67e04b15f2ffc99347896f55dbc55bed` |
| Implementation Head | pending |
| Review Round | 1 |

## RED Phase

### Test-Driven: navigation and app shell unit tests

**Files:** `tests/unit/navigation.test.js` (6 tests), `tests/unit/app-shell.test.js` (3 tests)

Covered:
- Navigation: 5 destinations with id/labelKey/icon/route
- Single active state via getActive/setActive
- setActive rejects unknown item IDs
- initNavigation function exported
- App shell: bottom nav and sidebar not simultaneously visible
- Bottom nav hidden on wide viewports (sm:hidden)
- Safe-area bottom padding for mobile

**RED command:** `npm test -- --run tests/unit/navigation.test.js tests/unit/app-shell.test.js`
**Expected:** Fail (navigation.js doesn't exist yet)
**Actual:** Exit code 1, `Error: Failed to resolve import "../../src/js/navigation.js"` — RED confirmed.

**GREEN command:** `npm test -- --run tests/unit/navigation.test.js tests/unit/app-shell.test.js`
**Expected:** Pass (9/9)
**Actual:** Exit code 0, all 9 tests passed.

## GREEN Phase — What Changed

### New Files

| File | Purpose |
|---|---|
| `src/js/navigation.js` | Navigation controller: `NAV_ITEMS` (5 destinations), `getActive()`, `setActive()`, `initNavigation()` (keyboard nav) |
| `tests/unit/navigation.test.js` | 6 tests for navigation API: item structure, active state, validation, keyboard init |
| `tests/unit/app-shell.test.js` | 3 tests for responsive shell: no dual nav, bottom nav hidden on wide, safe-area padding |

### Modified Files

| File | Changes |
|---|---|
| `src/css/app.css` | Expanded Design Tokens (`--spacing-*`, `--font-size-*`, `--radius-*`, `--touch-target`, `--focus-ring`, `--transition-smooth`); `*:focus-visible` focus ring; `prefers-reduced-motion`; safe-area env() support; sidebar styles (`#sidebar`, `.sidebar-item` active/hover); `@media (min-width: 768px)` sidebar visible, bottom nav hidden |
| `index.html` | Added `<aside id="sidebar">` with 5 navigation items (overview, add, stats, import, export) with `data-nav` attributes; bottom nav items now have `tabindex="0"` and `role="navigation"`; both nav sets use existing `data-i18n` keys |
| `src/js/main.js` | Imported `initNavigation`; calls `setTimeout(initNavigation, 50)` for keyboard nav support |

### Design Tokens Added

| Token | Values |
|---|---|
| Spacing | `--spacing-xs`(0.25rem) through `--spacing-xl`(2rem) |
| Font sizes | `--font-size-xs`(0.75rem) through `--font-size-2xl`(1.5rem) |
| Additional radii | `--radius-lg`(1rem), `--radius-xl`(1.25rem) |
| Touch target | `--touch-target`(44px) |
| Focus ring | `--focus-ring` (amber-400 glow) |
| Transition | `--transition-smooth`(0.3s cubic-bezier) |

### Responsive Layout Rules

| Viewport | Navigation | Sidebar |
|---|---|---|
| `<768px` | Bottom nav (`#bottom-nav`) with 3 items + secondary dropdown | Hidden (`display: none`) |
| `>=768px` | Hidden (`display: none !important`) | Sidebar (`#sidebar`) with 5 items, `sticky top-4`, scrollable |

### Files NOT Modified (per prohibition)

Budget/entry/sync calculations, Firestore, business schema, scope-external logic — untouched.

## Full Gates Verification

| Gate | Exit Code | Result |
|---|---|---|
| `npm test -- --run` | 0 | 147 pass, 8 skip (23 test files) |
| `npm run typecheck` | 0 | No type errors |
| `npm run build` | 0 | Build succeeded (45 modules, known chunk size warning) |
| `git diff --check` | 0 | No whitespace errors |

## Known Limitations

- Build chunk size warning (>500 kB) is pre-existing, not introduced by this Task.
- Visual verification at 360/390/430/768/1440/1920 viewports requires screenshot tooling — not automated in unit tests.
- Sidebar items re-use existing `switchMobileView`/`openQuickAdd` handlers; no separate sidebar controller implemented.
