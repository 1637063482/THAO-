# UXS-007 Evidence — Savings Goal Domain Rules

## Task Info

| Field | Value |
|---|---|
| Task ID | UXS-007 |
| Base SHA | `d68e8cdf51bf443b30907cbf1abb9aafecfac7ec` |
| Implementation Head | `be23e87b4cf2b4afe84b808fa5087a4cac76e25b` |
| Review Round | 1 |

## Scope

Only `src/domain/savings-goal.ts`, `tests/unit/domain/savings-goal.test.ts`, and this evidence file are changed. UI, Firestore, legacy entries, and T011/T012 remain untouched.

## RED / GREEN

The new domain test was initially red because `src/domain/savings-goal.ts` did not exist. After implementation, the targeted command passed: 1 file, 4 tests.

The pure functions validate VND safe integers, calculate `income - expense`, keep month/year aggregation independent, exclude principal and expected interest by accepting only ledger income/expense totals, and clamp display progress to 0–100%. Inputs are not mutated and no runtime globals are accessed.

## Full Gates

Recorded on implementation head `be23e87b4cf2b4afe84b808fa5087a4cac76e25b`:

| Command | Exit code | Key output |
|---|---:|---|
| `npm test -- --run` | `0` | 28 files passed, 2 skipped; 200 tests passed, 8 skipped. |
| `npm run typecheck` | `0` | TypeScript completed without errors. |
| `npm run build` | `0` | Vite built 49 modules; known 500 kB chunk warning only. |
| `git diff --check` | `0` | No whitespace errors. |
