# T019 Evidence - Account/Transaction Scope ADR

## Scope

- Task: T019 Decide T011/T012 account and transaction scope ADR
- Branch: `task/t019-account-transaction-adr`
- Base SHA: `ea94b388cd6c5b7ffae178abe879ef76335c3987`
- Implementation SHA: `pending`
- Status SHA: READY_FOR_REVIEW status commit, see repository HEAD after `docs: ready T019 for review`

## Owner Decision

Project owner selected Option A: stabilize the existing legacy yearly matrix and remove T011/T012 Account/Transaction work from the delivery mainline.

## Files Changed

- `docs/adr/003-account-transaction-scope.md`
- `ARCHITECTURE_PLAN.md`
- `TASK_PLAN.md`
- `docs/review-evidence/T019.md`
- `TASK_STATUS.md`

## RED - Document Consistency

Command:

```powershell
rg -n 'T019 需要项目所有者产品选择|若 T019 决定继续|只有项目所有者再次批准|迁移目标命名空间|独立 Account/Transaction 是否值得迁移' TASK_PLAN.md ARCHITECTURE_PLAN.md
```

Exit code: `0`

Key output:

```text
ARCHITECTURE_PLAN.md:159:上述是迁移目标命名空间草案；正式采用前必须以实际 Firebase 路径限制和 Rules 可维护性做 emulator 验证。现有 `shared_ledger_<year>` 在迁移完成前仍是事实源。
ARCHITECTURE_PLAN.md:166:- T011/T012 中的多币字段属于冻结探索；若 T019 决定继续，必须先改为符合 VND-only 产品边界。
ARCHITECTURE_PLAN.md:283:基于真实使用痛点评估独立 Account/Transaction 是否值得迁移。只有项目所有者再次批准，才进行备份、dry-run、双读校验和 UI 切换。
TASK_PLAN.md:129:T019 需要项目所有者产品选择。
```

Why this is valid RED:

- T019 is a documentation/ADR task with no code test requirement.
- The command found stale planning text after the owner decision was known.
- The stale text implied T019 was still waiting for a choice or that Account/Transaction migration remained the default future path.

## GREEN - Document Consistency

Command:

```powershell
rg -n 'T019 需要项目所有者产品选择|若 T019 决定继续|只有项目所有者再次批准|迁移目标命名空间|独立 Account/Transaction 是否值得迁移' TASK_PLAN.md ARCHITECTURE_PLAN.md
```

Exit code: `1`

Meaning: no stale T019 decision-waiting or default-migration wording remains.

Command:

```powershell
rg -n 'ADR-003|方案 A|T019 已|T011/T012|Transaction repository|VND-only|legacy 年度矩阵' docs\adr\003-account-transaction-scope.md ARCHITECTURE_PLAN.md TASK_PLAN.md
```

Exit code: `0`

Summary:

```text
docs/adr/003-account-transaction-scope.md records Option A and T011/T012 disposition.
ARCHITECTURE_PLAN.md records ADR-003 as the current architecture decision.
TASK_PLAN.md records T011/T012 as removed from the delivery mainline and T019 as owner-selected.
```

Command:

```powershell
rg -n 'account-repository|transaction-repository|domain/account|domain/transaction|application/accounts|application/transactions|AccountRepository|createTransactionUseCase|manageAccount' src\js index.html
```

Exit code: `1`

Meaning: the legacy runtime entry points still do not import or instantiate T011/T012 code.

## Full Gates

Command:

```powershell
npm test -- --run
```

Exit code: `0`

Summary:

```text
Test Files  14 passed | 2 skipped (16)
Tests  107 passed | 8 skipped (115)
Duration  14.39s
```

Command:

```powershell
npm run typecheck
```

Exit code: `0`

Summary:

```text
tsc -p tsconfig.json --noEmit
```

Command:

```powershell
npm run build
```

Exit code: `0`

Summary:

```text
vite v6.4.3 building for production...
dist/assets/index-PfNV1DTB.js   745.23 kB
built in 6.61s
```

Command:

```powershell
git diff --check
```

Exit code: `0`

## Implementation Summary

- Added ADR-003 with both options, owner-selected Option A, consequences, and follow-up tasks.
- Updated `ARCHITECTURE_PLAN.md` to mark Account/Transaction migration as rejected for the current roadmap.
- Updated `TASK_PLAN.md` to record T011/T012 as removed from the delivery mainline.
- Left T011/T012 source code untouched and disconnected.

## Known Warnings

- `npm run build` keeps the existing Vite chunk-size warning for the main bundle.
- `git diff --check` printed Git line-ending normalization warnings for touched markdown files on Windows, but exited `0`.

## Not Modified

- No business code.
- No Firestore schema, Rules, indexes, Auth, deployment, or production data.
- No T020/T021 implementation.
- No T011/T012 runtime wiring or migration.
