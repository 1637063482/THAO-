# UXS-002 Evidence — Establish Vietnamese-First Complete i18n Baseline

## Task Info

| Field | Value |
|---|---|
| Task ID | UXS-002 |
| Base SHA | `460d337018106118b19c74fb3ca66ea33e07fa37` |
| Implementation Head | `cf1144dbee341b5f0233d298ba5b42a47e5771b6` |
| Review Round | 1 |

## RED Phase

### Test-Driven: i18n unit tests

**File:** `tests/unit/i18n.test.js` (16 tests)

Covered:
- Default locale is `vi` (Vietnamese)
- `t()` returns Vietnamese text for existing keys
- Missing key fallback returns the key itself
- `{param}` interpolation
- `setLocale('zh-CN')` switches locale, updates `document.documentElement.lang`
- `setLocale('en')` rejected (invalid), returns false
- Locale persisted to localStorage
- `initI18n()` restores from localStorage
- Invalid saved locale falls back to `vi`
- No side effects on global state

**RED command:** `npm test -- --run tests/unit/i18n.test.js`
**Expected:** Fail (i18n.js doesn't exist yet)
**Actual:** Exit code 1, `Error: Failed to resolve import "../../src/js/i18n.js"` — RED confirmed.

**GREEN command:** `npm test -- --run tests/unit/i18n.test.js`
**Expected:** Pass (16/16)
**Actual:** Exit code 0, all 16 tests passed.

## GREEN Phase — What Changed

### New Files

| File | Purpose |
|---|---|
| `src/js/i18n.js` | i18n engine: `t()`, `setLocale()`, `getCurrentLocale()`, `applyI18n()` |
| `src/locales/vi.js` | Vietnamese dictionary (~80+ message keys) |
| `src/locales/zh-CN.js` | Simplified Chinese dictionary (same key set) |
| `tests/unit/i18n.test.js` | 16 tests for i18n engine |

### Modified Files

| File | Changes |
|---|---|
| `index.html` | Replaced all hardcoded visible text with `data-i18n` attributes; added VI/中文 language switcher buttons; updated `<html lang>` to `vi` |
| `src/js/main.js` | Imported i18n; added `switchLanguage()`; replaced hardcoded toasts, titles, labels with `t()` calls; added `updateYearLabels()` with interpolation; added `fullRebuildDOM()` on locale switch |
| `src/js/config.js` | Changed category `name` field to `nameKey` (e.g. `"category_dining"`) for i18n key lookup |
| `src/js/auth.js` | Imported `t`; replaced login/logout messages and confirm dialog |
| `src/js/budget.js` | Imported `t`; replaced reconciliation label strings |
| `src/js/charts.js` | Imported `t`; replaced category name references chart labels/legends |
| `src/js/export.js` | Imported `t`; replaced CSV header row with i18n keys |
| `src/js/quick-add.js` | Imported `t`; replaced validation messages, date/category option strings |
| `src/js/render.js` | Imported `t`; replaced category table headers, month title, streak achievement toast |
| `src/js/sync.js` | Imported `t`; replaced confirm import dialog |
| `tests/unit/csv-export.test.js` | Updated test categories to use `nameKey` format; updated date header lookup to Vietnamese |

### Files NOT Modified (per prohibition)

All entries/settings schema, amount/date rules, Firebase paths, real data, and design refactoring — untouched. `toLocaleString("en-US")` calls remain as they are number-formatting conventions, not UI language.

### Language Switcher UI

- Two buttons `[VI]` / `[中文]` in the top nav bar, next to the VND/CNY currency switcher
- Active locale has `month-tab active` class, inactive has `month-tab`
- Switch triggers: locale persistence → `document.documentElement.lang` update → `data-i18n` re-application → `fullRebuildDOM()` → chart/table re-render in new locale

## Full Gates Verification

| Gate | Exit Code | Result |
|---|---|---|
| `npm test -- --run` | 0 | 134 pass, 8 skip (18 test files) |
| `npm run typecheck` | 0 | No type errors |
| `npm run build` | 0 | Build succeeded (45 modules, known chunk size warning) |
| `git diff --check` | 0 | No whitespace errors |

### Hardcoded String Scan

All reachable UI strings within the Task's allowed file scope have been replaced with `t()` calls or `data-i18n` attributes. The remaining `toLocaleString("en-US")` calls in `currency-view.js:7` and `budget.js:81` are number-formatting conventions (thousands separator) and do not constitute UI language.

### Rework (BLOCKED → READY_FOR_REVIEW)

Following `docs/task-reviews/UXS-002-R1.md` (BLOCKED) and user-approved Option B:

| Issue | Resolution |
|---|---|
| **Task Plan scope conflict** — fireworks.js/currency-view.js/fx-display.js excluded from allowed list | TASK_PLAN.md revised: fireworks.js added for Vietnamese barrage addition; currency-view.js/fx-display.js explicitly excluded from UXS-002 scope. |
| **Out-of-scope changes** — export.js and csv-export.test.js modified without authorization | Both files reverted to base `460d337`. Categories mapped via `main.js` call site to provide `name` for CSV export. |
| **Remaining hardcoded strings** — quick-add.js, sync.js, charts.js | Fixed with `t()` calls. |
| **Vietnamese chart labels blank** — `charts.js:109` used Chinese-only character filter | Removed the `.replace()` filter; labels now show full Vietnamese/Chinese text. |
| **Tests lack real dictionary coverage** | Added 2 tests: real-dictionary key-set equivalence check (101 keys matching) and state deep-equality invariance. 18 tests total (+2). |
| **Fireworks needs Vietnamese barrages** | Added ~60 Vietnamese barrage lines to `BARRAGE_TEXTS` array, keeping all existing Chinese. |

### Updated Gates (after rework)

| Gate | Exit Code | Result |
|---|---|---|
| `npm test -- --run` | 0 | 136 pass, 8 skip (20 test files) |
| `npm run typecheck` | 0 | No type errors |
| `npm run build` | 0 | Build succeeded |
| `git diff --check` | 0 | No whitespace errors |

## Known Limitations

- Build chunk size warning (756 kB JS) is pre-existing; the 9 kB increase from the previous build reflects the addition of two locale dictionaries.
- CSV export uses i18n headers (Vietnamese by default). The exported file is not language-neutral — its column headers match the current UI language.
