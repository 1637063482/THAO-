# MyExpenseApp UI、储蓄目标与存款功能规划

> 文档状态：产品与技术规划草案，等待纳入主施工计划  
> 形成日期：2026-07-20  
> 适用产品：通过 Cloudflare 部署、使用网址访问并可安装的 PWA 个人记账应用  
> 当前执行隔离：本文件在 `plan/ui-savings-redesign` 独立 planning 分支形成，不改变正在实施的 T018 状态、实现范围或工作树。

## 1. 产品背景与目标

应用为越南籍女朋友定制。她是唯一日常记账者，主要问题不是缺少专业会计功能，而是：

- 花钱时缺少金额概念；
- 不清楚钱主要花到哪里；
- 难以持续记账；
- 没有清晰的月度、年度储蓄目标；
- 已有存款缺少统一记录、收益汇总和到期提示。

产品目标从“记录收支”扩展为：

1. 让日常记账足够快速，降低坚持成本；
2. 每次记账后立即反馈本月还能花多少、钱花到哪里；
3. 用可信的连续记账激励培养习惯；
4. 用可调整的月度、年度储蓄目标建立储蓄意识；
5. 统一管理存款本金、利率、预计收益、到期和实际处理结果；
6. 保持私人 PWA 的简单性，不扩张为通用银行或投资平台。

## 2. 已确认产品决策

### 2.1 用户、语言和币种

- 女朋友是唯一日常记账者；项目所有者使用第二个既有 Firebase 账号查看或维护。
- 越南语为默认语言，中文可切换，不提供英语。
- VND 是唯一持久化账务事实币种。
- CNY 仅为项目所有者查看时的只读换算，不回写账务、目标或存款数据。

### 2.2 设备与交互

- 应用是 Cloudflare 部署的 Web/PWA，不假设原生 iOS 能力。
- 手机以快速记账为主。
- 手机仍保留完整月度表格的查看和编辑入口，但不作为默认首页。
- 电脑端保留完整表格作为高效率维护视图。
- 视觉采用方案 B：暖白背景、杏橙主色、克制的 Apple 风格层级。

### 2.3 首页与消费认知

- 首页最大数字显示“本月还可以花多少”。
- 计算口径仍是月度预算减去本月有效支出，不与总资产或存款本金混算。
- 首页突出本月支出去向、预算进度、储蓄目标、连续记账和最近记录。

### 2.4 储蓄与存款

- 月度储蓄目标可按月份调整。
- 年度储蓄目标可按年份调整。
- 存款记录独立于日常消费记录。
- 存款本金属于资产内部转移，不算支出，也不重复增加储蓄进度。
- 预计利息不是已实现收入；实际到账并由用户确认后才可计入收入。

### 2.5 到期提醒

- 提醒只在用户打开应用或应用恢复到前台时，以应用内弹窗显示。
- 不使用系统通知、Web Push、Cloudflare Cron、邮件或其他外部提醒。
- 用户未打开应用时不承诺主动提醒；再次打开时显示已到期或逾期状态。
- 默认提醒阶段为到期前 30 天、7 天、1 天、到期当天和已逾期；每笔存款可调整或关闭具体阶段。

## 3. 非目标

本轮规划不包含：

- 银行账户自动同步；
- 银行 API 或开放银行接入；
- 投资组合、股票、基金或证券收益；
- 贷款、信用评分或税务；
- 自动替用户赎回或续存；
- 后台定时任务、外部推送或短信；
- Household、多家庭、邀请、成员管理或通用权限角色；
- 多事实币种；
- 根据消费行为给出羞辱性或强制性评价。

## 4. 信息架构

### 4.1 手机底部导航

```text
首页 | 账本 | ＋ | 储蓄 | 分析
```

- 首页：可花金额、预算、钱花到哪里、储蓄目标、连续记账、最近记录。
- 账本：默认按日卡片；可切换完整月度表格。
- ＋：打开快速记账 Bottom Sheet。
- 储蓄：月度/年度目标、存款汇总、到期提示和存款列表。
- 分析：月度/年度收支和分类趋势。
- 设置：放在首页右上角，不占底部导航，包括语言、主题、隐私、汇率、导入导出等低频功能。

### 4.2 电脑端导航

```text
左侧 Sidebar
├─ 首页
├─ 账本
├─ 储蓄
├─ 分析
├─ 资产
└─ 设置
```

- 账本页使用主要内容宽度展示完整矩阵。
- 图表不再常驻挤压账本右侧，进入独立分析页。
- 存款在储蓄页使用表格和详情面板。

## 5. 首页规划

### 5.1 手机首页层级

1. 当前月份和同步状态；
2. 最大数字：本月还可以花多少；
3. 预算使用进度；
4. 本月支出与今日支出；
5. 支出去向前三类；
6. 月度储蓄目标进度；
7. 连续记账状态；
8. 存款摘要和最近到期；
9. 最近记账记录。

### 5.2 首页指标定义

```text
本月还可花 = 月度预算 - 本月有效支出
本月实际储蓄 = 本月有效收入 - 本月有效支出
年度实际储蓄 = 年度有效收入 - 年度有效支出
月度储蓄完成率 = 本月实际储蓄 / 当月储蓄目标
年度储蓄完成率 = 年度实际储蓄 / 当年储蓄目标
```

规则：

- 目标为 0 或未设置时显示引导，不计算百分比。
- 实际储蓄为负时显示真实负值和“本月支出高于收入”，进度最低为 0%，不能隐藏赤字。
- 完成率可超过 100%，但进度条视觉封顶；文字保留真实百分比。
- 月度目标和年度目标互相独立；修改某月目标不自动修改年度目标。
- 存款本金不进入上述公式，避免资产转移重复计算。

### 5.3 反馈语气

文案以事实和行动提示为主：

- “餐饮是本月第一大支出，占 38%。”
- “本月还可以使用 4,250,000 ₫。”
- “本月储蓄目标已完成 80%。”
- “按当前支出速度，预算可能提前用完。”

禁止羞辱性文案，例如“乱花钱”“不应该购买”或“消费不理智”。连续记账奖励的是记录习惯，不奖励完全不消费。

## 6. 视觉与响应式设计

### 6.1 Design Tokens

建议初始 Token：

```text
background-light: #F5F3EF
surface-light:    #FFFFFF
primary-apricot:  #E78B4A
primary-pressed:  #CC7135
action-blue:      #3B82F6
text-primary:     #1D1D1F
text-secondary:   #6E6E73
income:           #2E9B65
expense:          #D96855
warning:          #C98228
danger:           #C84941
```

- 卡片圆角 20px，重点卡片 24–28px，普通按钮 12–14px。
- 阴影轻量，主要通过背景、间距和排版表达层级。
- 只在主 CTA、关键进度和小范围强调中使用杏橙。
- 收入统一绿色，支出统一珊瑚红，修复现有颜色/类名倒置。
- 不使用大面积多色渐变标题。

### 6.2 字体

使用 Web/PWA 安全系统字体栈：

```css
-apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans", sans-serif
```

- 不依赖设备一定存在 SF Pro。
- 越南语重音字符必须在 360px 窄屏测试。
- VND 金额使用 tabular numbers，不显示无意义小数。

### 6.3 断点与设备

必须验证：

- 360×800
- 390×844
- 430×932
- 768×1024
- 1024×768
- 1440×900
- 1920×1080

手机必须考虑：

- `safe-area-inset-top/bottom`；
- 浏览器模式与安装后 standalone 模式；
- 软键盘弹出时 Bottom Sheet 不被遮挡；
- 无 hover 也能发现全部操作；
- 横屏可查看完整表格；
- `prefers-reduced-motion`。

## 7. 快速记账

### 7.1 手机 Bottom Sheet

交互顺序：

1. 支出/收入；
2. 金额；
3. 分类；
4. 默认今天，可修改日期；
5. 可选备注；
6. 确认。

保存成功后立即反馈：

- 已记录金额和分类；
- 本月还可花金额；
- 该分类本月累计与占比；
- 储蓄目标进度变化；
- streak 更新结果。

### 7.2 完整表格

- 手机账本页默认按日视图，明确提供“完整表格”切换。
- 完整表格保留查看和编辑能力。
- 固定日期列与表头，并提示横向滑动。
- 电脑端完整表格仍是主要高效编辑入口。
- 所有入口复用同一金额、日期、streak、预算和储蓄聚合规则。

## 8. 可调整储蓄目标

### 8.1 月度目标

每个月拥有独立目标：

```text
period: YYYY-MM
targetVnd: VND integer
updatedAt
updatedBy
version
```

业务规则：

- 可设置、修改、清空；
- 修改只影响该月份，不批量覆盖其他月份；
- 查看历史月份时使用该历史月份保存的目标；
- 未设置的新月份可在 UI 中提供“沿用上月目标”显式操作，但不得静默复制；
- 修改目标后立即重算进度，不修改收入或支出事实。

### 8.2 年度目标

每年拥有独立目标：

```text
year: YYYY
targetVnd: VND integer
updatedAt
updatedBy
version
```

业务规则：

- 可设置、修改、清空；
- 年度目标不是 12 个月目标的自动总和；
- UI 可显示“月度目标合计”和“年度目标”的差异，但不能静默替用户修改；
- 跨年时不自动把上一年度目标写入新年度，可提供显式沿用操作。

### 8.3 校验

- 目标必须是非负 VND 整数；
- 拒绝 NaN、Infinity、小数、公式字符串和超安全整数；
- CNY 切换只改变展示，不修改 targetVnd；
- 并发修改必须按现有同步策略明确处理，不能静默显示虚假成功。

## 9. 存款记录功能

### 9.1 领域边界

存款记录用于管理银行存款或类似固定期限储蓄产品，不是消费交易，也不是投资行情模块。

一笔存款至少包含：

- 名称或银行/产品说明；
- 存入日期；
- 到期日期；
- 本金 VND；
- 年利率；
- 系统参考收益；
- 用户确认的预计收益；
- 到期预计总额；
- 提醒配置；
- 状态；
- 可选备注。

### 9.2 状态

```text
ACTIVE       进行中
MATURING     已进入最近提醒窗口
MATURED      已到期、待处理
REDEEMED     已赎回
ROLLED_OVER  已续存并关联新存款
```

状态规则：

- `MATURING` 和 `MATURED` 可由当前越南日期派生，不把它们作为不可恢复事实；
- `REDEEMED` 和 `ROLLED_OVER` 必须由用户确认；
- 删除采用软删除或归档，不能使历史汇总不可解释；
- 已赎回本金不计入当前存款总额。

### 9.3 金额汇总

```text
当前存款总额 = ACTIVE + MATURING 的本金合计
待处理到期本金 = MATURED 且未赎回/续存的本金合计
预计总收益 = ACTIVE + MATURING 的 expectedInterestVnd 合计
预计到期总额 = 当前存款总额 + 预计总收益
已实现利息 = 用户确认的 actualInterestVnd 合计
```

- 已到期未处理金额单独展示，不能从资产视图悄悄消失。
- 预计收益不进入日常收入或储蓄实际完成值。
- 实际利息只有经用户确认后才可选择写入日常收入。

### 9.4 利息

参考公式：

```text
calculatedInterestVnd =
  round(principalVnd × annualRate × depositDays ÷ 365)
```

由于银行可能使用不同计息天数或报价规则：

- 系统计算值只作参考；
- `expectedInterestVnd` 允许用户修改；
- 保存时保留 calculated 与 expected 两个值；
- 总预计收益使用 expected 值；
- 年利率采用定点比例或规范十进制字符串，不使用二进制浮点作为持久事实；
- 到期日必须晚于存入日；
- 越南本地日用于天数和提醒计算。

### 9.5 到期处理

到期弹窗提供：

- 确认赎回；
- 续存；
- 稍后处理。

确认赎回时记录：

- 实际赎回日期；
- 实际利息；
- 本金转回的现有账户（可选，仅作说明或后续资产接线）；
- 是否将实际利息一键加入收入。

一键加入收入必须二次确认并具有幂等 operationId，避免重复记账。存款本金返回不算收入。

续存创建新存款记录并引用旧记录；不得直接覆写旧存款的历史本金、利率和日期。

## 10. 应用内到期提醒

### 10.1 触发时机

只在以下事件检查：

1. 用户认证完成且存款数据首次加载完成；
2. PWA 从后台恢复到可见状态；
3. 页面保持打开并跨越 `Asia/Ho_Chi_Minh` 日期边界；
4. 用户新增或修改存款后；
5. 用户主动进入储蓄页面或点击刷新。

不启动：

- Web Push；
- Notification API；
- Cloudflare Worker/Cron；
- Firebase Cloud Messaging；
- 邮件、短信或系统日历。

### 10.2 阶段

默认阶段：

```text
D-30
D-7
D-1
D0
OVERDUE
```

每笔存款可以：

- 调整提前天数；
- 关闭某个阶段；
- 关闭该笔提醒；
- 在弹窗中“知道了”；
- 在当前设备上“稍后提醒”。

### 10.3 去重和稍后提醒

提醒事件键建议：

```text
depositId + maturesOn + reminderStage
```

- “知道了”确认该阶段，跨设备需要同步时写入账本侧 acknowledgement；
- “稍后提醒”只影响当前设备，保存 `snoozeUntil` 到 localStorage；
- snooze 到期后只有在下一次应用生命周期检查时重新出现，不承诺后台定时弹出；
- 修改到期日后旧 acknowledgement 不得抑制新日期的提醒；
- 多笔同时到期时合并为一个弹窗列表，避免连续弹窗轰炸；
- 弹窗必须可关闭，不阻断紧急快速记账。

### 10.4 未打开应用的行为

明确产品限制：

- 应用未打开时不会主动提醒；
- 用户错过 D-7/D-1 后再打开，不需要依次补弹所有旧阶段；
- 只显示当前最紧迫状态，例如 D0 或 OVERDUE；
- 已逾期记录持续出现在储蓄页醒目区域，直到赎回、续存或明确处理。

## 11. 数据模型草案

正式路径由 T019 ADR 与实际 Rules 约束决定。推荐逻辑模型：

```text
savingsGoals/{periodKey}
  kind: MONTHLY | YEARLY
  periodKey: YYYY-MM | YYYY
  targetVnd: integer
  version
  createdAt
  updatedAt
  updatedBy

deposits/{depositId}
  institutionName
  productName
  principalVnd
  annualRateScaled
  rateScale
  openedOn
  maturesOn
  calculatedInterestVnd
  expectedInterestVnd
  actualInterestVnd
  reminderDays[]
  remindersEnabled
  status
  redeemedOn
  rolledOverToDepositId
  note
  version
  createdAt
  updatedAt
  createdBy
  updatedBy
  deletedAt

depositReminderAcknowledgements/{ackId}
  depositId
  maturesOn
  stage
  acknowledgedAt
  acknowledgedBy
```

约束：

- 目标和存款使用 VND 整数；
- rate 采用 scaled integer；
- 日期为 `YYYY-MM-DD` 越南业务日，审计时间使用服务端 timestamp；
- UI 不直接拼 Firestore 路径；
- 不把存款数组塞入不断增长的年度 legacy 大文档；
- 不引入 householdId 或成员系统；
- 第二账号真实写权限必须遵循 T017 已核对的线上契约。

## 12. PWA 与 Cloudflare 约束

- 同一 URL 在浏览器和安装后的 standalone 模式都可使用。
- 应用壳、语言包、核心 CSS/JS 和图标进入 Service Worker 缓存策略。
- Firebase 财务数据不能因为静态缓存而长期显示旧版本；恢复在线后必须按现有同步契约更新。
- Service Worker 更新提示不能遮挡存款到期弹窗或快速记账。
- 不新增需要 Cloudflare 服务端 secret 的能力。
- 默认系统字体，避免 Google Fonts 不可用时破坏越南语布局。
- 断网打开时可基于本地已有存款快照显示提醒，但必须明确“离线数据，可能不是最新”。
- 在线写入目标、存款和 acknowledgement 必须显示 queued/syncing/synced/error，而不是伪成功。

## 13. 国际化

### 13.1 规则

- 首次使用默认 `vi`。
- 中文切换为 `zh-CN`。
- 语言偏好优先本地持久化；是否云同步可在实现任务中决定，但不能改变账务事实。
- 切换语言不刷新或重写金额、目标、存款、日期和 pendingUpdates。
- `<html lang>` 随语言切换。
- 所有用户可见字符串从 DOM/JS 中抽离到字典，不保留英语 fallback 作为可选择语言。
- 缺少翻译键时开发环境报错；生产环境可安全回退越南语并记录非敏感错误。

### 13.2 核心越南语词汇

| 中文 | 越南语 |
|---|---|
| 首页 | Trang chủ |
| 账本 | Sổ thu chi |
| 快速记账 | Ghi nhanh |
| 储蓄 | Tiết kiệm |
| 月度储蓄目标 | Mục tiêu tiết kiệm tháng |
| 年度储蓄目标 | Mục tiêu tiết kiệm năm |
| 存款总额 | Tổng tiền gửi |
| 预计收益 | Lợi nhuận dự kiến |
| 到期日期 | Ngày đáo hạn |
| 即将到期 | Sắp đáo hạn |
| 已到期 | Đã đáo hạn |
| 稍后提醒 | Nhắc lại sau |

正式上线前应由越南语母语使用者确认自然程度，不能只依赖机器直译。

## 14. 安全与隐私

- Firestore Rules 默认 deny，只允许 T017 已确认的两个账号按实际权限访问。
- 存款属于高敏感资产信息，不写入日志、analytics、错误堆栈或 review evidence。
- 测试只使用合成金额、假银行名和假 UID。
- 不在前端代码或文档中写真实邮箱、UID、存款金额或银行信息。
- note 必须按普通用户输入防 XSS；禁止 `innerHTML`。
- 写入字段、大小、类型、日期、状态转换和不可变审计字段必须由 runtime schema 与 Rules 双重限制。
- 删除/赎回/续存/写入实际利息需要明确确认，避免误操作。

## 15. 测试与验收

### 15.1 单元测试

- 月度与年度实际储蓄计算；
- 目标未设置、0、负实际值、超过 100%；
- 修改某月不影响其他月份；
- VND 大整数与边界；
- scaled rate 和参考利息；
- expected override；
- 存款状态和汇总；
- D-30/D-7/D-1/D0/OVERDUE；
- acknowledgement、snooze 和到期日修改；
- 多笔提醒合并；
- 越南时区、闰年、跨月和跨年；
- vi/zh 切换不改变事实 state。

### 15.2 集成与 Rules

- 两个账号按已确认契约读写目标、存款和 acknowledgement；
- 匿名与第三 UID 拒绝；
- 直接 SDK 写小数本金、非法 rate、反向日期、非法状态转换被拒绝；
- repository round-trip 无字段漂移；
- 并发版本冲突可见；
- 实际利息一键写收入幂等，不重复。

### 15.3 UI/E2E

- 越南语默认、中文切换；
- 手机快速记账 ≤ 10 秒；
- 手机按日账本和完整表格均可编辑；
- 首页最大数字为预算剩余；
- 目标修改后进度立即更新；
- 存款新增、编辑、到期、赎回、续存；
- 打开应用触发当前最紧迫提醒；
- 后台恢复触发检查；
- 未打开期间错过阶段后只显示当前状态；
- 多提醒一个弹窗汇总；
- 浏览器/standalone、在线/离线、软键盘、安全区域；
- 隐私模式覆盖目标和存款金额。

### 15.4 视觉验收

- 规定尺寸截图对比；
- 无横向页面溢出，完整表格自身允许受控横向滚动；
- 收入、支出颜色一致；
- 越南语无截断；
- 明暗主题达到可读对比；
- 触控目标和焦点状态可见；
- reduced motion 下无强制长动画。

## 16. 候选施工任务

以下任务尚未激活到 `TASK_STATUS.md`。必须等待当前主线 Task 完成，并由 T019 ADR 确认数据模型后再纳入正式 `TASK_PLAN.md`。每个任务保持单一目标、独立 commit、预计不超过半天。

### UXS-001：固化 UI 与储蓄产品 ADR

- 目标：把本文已确认决策写入 PRD、FRD、架构和 ADR。
- 修改文件：`PRD.md`、`FRD.md`、`ARCHITECTURE_PLAN.md`、`docs/adr/004-ui-savings-boundary.md`。
- 禁止修改：业务代码、Firebase、当前 Task 状态。
- 完成标准：语言、PWA、首页、目标、存款、提醒和非目标一致。
- 测试：文档一致性 `rg`；`git diff --check`。

### UXS-002：建立越南语默认的 i18n 基线

- 目标：所有静态和动态文案从字典读取，默认 vi，可切换 zh-CN。
- 修改文件：`src/js/i18n.js`、`src/locales/vi.js`、`src/locales/zh-CN.js`、`index.html`、相关测试。
- 禁止修改：账务数据、币种、Firebase schema。
- 完成标准：切换不改变 state/pending；html lang 正确；无可选英语。
- 测试：缺键、插值、持久化、越南语字符、state 深度不变。

### UXS-003：建立 Apple Warm Design Tokens 与响应式 App Shell

- 目标：统一暖白/杏橙 Token、系统字体、Sidebar/Bottom Nav 和 safe area。
- 修改文件：`src/css/app.css`、`index.html`、UI shell 测试。
- 禁止修改：业务计算和数据路径。
- 完成标准：所有规定尺寸无页面级溢出，浏览器与 standalone 可用。
- 测试：视觉截图、键盘、焦点、reduced motion、安全区域。

### UXS-004：重构手机首页消费认知区

- 目标：首页突出本月还可花、支出去向和预算。
- 修改文件：主页 ViewModel、render、CSS、测试。
- 禁止修改：预算和 entries 事实口径。
- 完成标准：每个数字可追溯到现有聚合；空态/负值明确。
- 测试：预算边界、分类前三、空数据、大 VND。

### UXS-005：快速记账 Bottom Sheet

- 目标：手机 10 秒内完成收入/支出记录，桌面仍可使用。
- 修改文件：quick-add UI/controller、CSS、E2E。
- 禁止修改：streak、同步、金额业务规则。
- 完成标准：键盘不遮挡、关闭手势安全、保存反馈完整。
- 测试：收入/支出、日期、分类、备注、失败保留输入、离线状态。

### UXS-006：手机按日账本与完整表格切换

- 目标：按日视图为手机默认，完整表格继续可看可改。
- 修改文件：账本 ViewModel、render、CSS、测试。
- 禁止修改：legacy key/schema。
- 完成标准：两视图编辑同一事实源且结果一致。
- 测试：视图切换、横屏、固定列、编辑、空态、同步快照。

### UXS-007：可调整储蓄目标领域纯函数

- 目标：定义月度/年度目标、实际储蓄与进度规则。
- 修改文件：`src/domain/savings-goal.ts`、应用层纯函数、单元测试。
- 禁止修改：UI、Firestore、T011/T012。
- 完成标准：月/年独立，VND 整数，负结余和 0 目标清晰。
- 测试：所有第 15.1 节目标边界。

### UXS-008：储蓄目标 repository 与 Rules

- 目标：两个账号按实际权限安全同步目标。
- 修改文件：repository、converter、runtime schema、Rules、emulator tests。
- 禁止修改：线上 Rules、真实数据、角色 UI。
- 完成标准：round-trip、版本冲突和 deny 对称测试通过。
- 测试：unit/integration/rules/full gates。

### UXS-009：储蓄目标 UI

- 目标：首页卡片和储蓄页可设置、修改、清空月/年目标。
- 修改文件：Savings ViewModel、页面、样式、E2E。
- 禁止修改：目标计算规则。
- 完成标准：修改后即时反馈并明确同步状态。
- 测试：vi/zh、移动/桌面、0/负实际/超额、失败重试。

### UXS-010：存款领域模型与利息计算

- 目标：定义 Deposit、状态、scaled rate、参考/预计/实际收益。
- 修改文件：`src/domain/deposit.ts`、利息纯函数、测试。
- 禁止修改：Firestore、UI、日常 entries。
- 完成标准：不使用浮点持久事实，非法日期/状态拒绝。
- 测试：利息、舍入、闰年、override、状态、汇总。

### UXS-011：存款 repository、Rules 与迁移空态

- 目标：独立文档安全同步存款，无需迁移既有账务。
- 修改文件：repository、converter、schema、Rules、integration/rules tests。
- 禁止修改：线上配置、真实数据、legacy annual doc。
- 完成标准：CRUD/归档/版本/权限确定；无存款时正常空态。
- 测试：直接 SDK 非法写、并发、round-trip、deny。

### UXS-012：存款列表、表单和汇总 UI

- 目标：手机卡片、电脑表格支持新增/编辑/归档并显示总额收益。
- 修改文件：Savings page、Deposit ViewModel、CSS、E2E。
- 禁止修改：利息和汇总纯函数。
- 完成标准：当前、待处理到期、预计收益口径正确。
- 测试：移动/桌面、隐私、vi/zh、空态、错误态。

### UXS-013：应用内到期提醒引擎

- 目标：生命周期触发、阶段选择、去重、snooze、多笔合并。
- 修改文件：`src/application/deposits/build-reminders.ts`、lifecycle adapter、local acknowledgement/snooze adapter、测试。
- 禁止修改：Web Push、Notification、Worker、Cron。
- 完成标准：只有打开/恢复应用时弹窗；错过阶段不连环补弹。
- 测试：D-30/D-7/D-1/D0/逾期、跨日、恢复、修改到期日、多笔。

### UXS-014：到期处理、赎回与续存

- 目标：人工确认实际利息，安全赎回或创建续存记录。
- 修改文件：Deposit use cases、到期弹窗、repository、测试。
- 禁止修改：自动赎回、自动续存、自动把本金算收入。
- 完成标准：历史保留，实际利息可幂等写入收入。
- 测试：赎回、续存、重复点击、失败重试、版本冲突。

### UXS-015：PWA 与视觉验收闭环

- 目标：完成多尺寸、standalone、离线、更新、无障碍和包体验收。
- 修改文件：PWA shell/CSS、必要 E2E/视觉测试、验收 evidence。
- 禁止修改：未经批准的产品功能。
- 完成标准：第 15 节全部门禁有新鲜证据，Cloudflare 部署流程不自动执行。
- 测试：全量门禁、视觉矩阵、PWA smoke；部署需用户另行授权。

## 17. 推荐依赖顺序

```text
完成当前 T018
  -> T019 数据模型 ADR
  -> UXS-001
  -> UXS-002
  -> UXS-003
  -> UXS-004 / UXS-005 / UXS-006
  -> UXS-007
  -> UXS-008
  -> UXS-009
  -> UXS-010
  -> UXS-011
  -> UXS-012
  -> UXS-013
  -> UXS-014
  -> UXS-015
```

说明：

- UI Shell 可先建立，但储蓄与存款持久化必须等待 T019 确认目标命名空间。
- UXS-007 和 UXS-010 是纯领域任务，可在不接 Firebase/UI 的前提下独立审查。
- UXS-013 不依赖外部服务，只依赖存款读取、越南 clock 和 PWA 生命周期。
- 每个任务遵循 `AGENT_WORKFLOW.md`：Coder evidence → Terra review → APPROVED 后进入下一任务。

## 18. 整体验收标准

只有同时满足以下条件，UI 与储蓄扩展才算完成：

1. 越南语默认、中文切换，所有核心流程无英语依赖；
2. 手机快速记账为主，完整表格仍可查看和编辑；
3. 首页最大数字准确显示月度预算剩余；
4. 用户能理解钱主要花到哪里；
5. 月度和年度储蓄目标可独立调整，历史目标不被静默覆盖；
6. 存款本金、预计收益、到期和状态汇总准确；
7. 存款本金不算支出、不重复计储蓄，预计利息不算已实现收入；
8. 应用打开/恢复时弹出当前有效提醒，未打开时不作虚假承诺；
9. 不引入 Push、Cron 或外部提醒基础设施；
10. 浏览器与安装 PWA、手机与电脑、在线与离线都有明确行为；
11. 所有新数据受 T017 权限契约、runtime schema 和 Rules 保护；
12. 每个任务具备 RED/GREEN、门禁、evidence 和 Terra 审查记录；
13. 未经授权不部署 Cloudflare/Firebase、不修改线上规则或真实数据。

