# MyExpenseApp 产品需求文档（重规划）

> 本 PRD 基于当前代码与项目所有者确认的真实范围制定：这是为女朋友开发、由两个人共同使用的私人应用。它不是面向多个家庭的 SaaS，不规划公开注册、建家庭、邀请成员或角色管理。

## 1. 产品定位

### 1.1 一句话定位

为一对伴侣提供“双方都能快速记、每一笔都能追溯、共同资产能对上”的私人跨币种账本。

### 1.2 目标用户

核心目标用户只有两名：项目所有者与女朋友。双方共同承担支出，日常使用 VND/CNY，需要跨设备同步、共同查看预算并完成月底对账。

暂不服务：企业财务、复式会计、税务申报、投资交易和银行级实时资金管理。

### 1.3 核心价值

1. 低摩擦记录：10 秒内完成一笔收支记录。
2. 双人协同：两个账号同权记账，每笔变更可追溯，避免互相覆盖。
3. 跨币种可审计：保留原币、汇率与基准币，不让历史金额随展示汇率改变。
4. 预算与对账闭环：从记录、预算、趋势到月末/年末资产核对形成行动闭环。
5. 数据可控：可导出、可备份、可恢复、可删除，默认保护隐私。

### 1.4 竞争优势

- 围绕 CNY/VND 家庭的双币种原生体验，而不是仅做显示换算；
- “固定共同账本 + 双人记录追溯 + 冲突安全”优先于复杂金融功能；
- 保留当前直观的日历矩阵作为汇总视图，同时以独立交易流水作为事实源；
- 年初/年末资产盘点和差异解释是现有产品的特色，可升级为月度对账。

## 2. 用户画像

### Persona A：日常记账者（女朋友）

- 30 岁左右，与伴侣共同生活，家庭支出使用 VND，部分收入/转账使用 CNY。
- 每天手机快速记账，月底在电脑上复盘。
- 痛点：忘记谁记了什么、汇率口径不一致、共同编辑互相覆盖。
- 成功标准：随时知道本月已花多少、还能花多少、哪笔账造成差异。

### Persona B：共同维护者（项目所有者）

- 负责维护应用、账户、分类和预算，与伴侣共同记账。
- 痛点：无法恢复误删/误导入、并发编辑可能覆盖、月底对不上账。
- 成功标准：双方变更有记录、异常差异能定位到交易，应用维护成本低。

## 3. 产品原则

- 交易事实优先：汇总、图表、预算都从独立交易计算，不直接编辑聚合结果。
- 默认安全：账本只对两个既有 Firebase UID 开放，第三方 UID 默认拒绝，敏感操作二次确认并可恢复。
- 金额可解释：每个展示数字都能下钻到交易明细。
- 快速入口一致：快速记账、列表编辑、导入必须遵守同一业务规则。
- 渐进复杂：基础用户只看到必要字段，高级字段按需展开。
- AI 不替用户做账：AI 只能建议分类/解释趋势，用户确认后才写入事实数据。

## 4. 核心用户流程

### 4.1 首次使用

使用既有账号登录 → 打开固定共享账本 → 创建至少一个账户 → 接受默认分类 → 记录第一笔交易 → 查看本月概览 → 可选设置预算。

### 4.2 日常记账

点击“记一笔” → 选择支出/收入/转账 → 输入金额与原币 → 选择账户和分类 → 默认当前时间 → 可选标签/备注 → 保存 → 明细立即出现 → 预算与概览更新 → 云端确认。

目标：熟练用户 10 秒内完成；失败时明确显示“本地待同步”，不得显示虚假的“已同步”。

### 4.3 双人协作

任一既有账号登录 → 读取同一本账 → 同权查看和记账 → 每笔记录显示创建者 → 修改/删除保留审计事件 → 冲突时提示并保留双方数据。授权账号的增删由项目所有者在 Firebase 配置中维护，不在应用内提供入口。

### 4.4 月度预算

设置月总预算/分类预算 → 查看消耗进度 → 达到阈值时应用内提醒 → 点击超支分类下钻交易 → 调整后续计划 → 月末查看最终偏差。

### 4.5 月末对账

查看系统计算账户余额 → 输入/导入实际余额 → 系统显示差异 → 按账户下钻未核对交易 → 标记已核对或补记 → 形成月度对账结果。

### 4.6 数据导入与恢复

选择规范文件 → 本地验证 → 预览新增/重复/冲突/拒绝项 → 用户选择合并策略 → 自动生成恢复点 → 执行导入 → 显示结果报告 → 必要时一键回滚。

## 5. 功能规划

复杂度口径：S ≤ 1 人日，M = 2–5 人日，L = 1–2 周，XL > 2 周；这是产品级粗估，不替代任务计划。

### 5.1 MVP：先把现有原型变成可信账本

| 功能 | 目标 | 用户价值 | 复杂度 | 优先级 |
|---|---|---|---|---|
| 固定双人访问控制 | 两个既有账号可访问，任何第三方账号拒绝 | 私人账务不外泄 | S | P0 |
| 独立交易流水 | 一笔记录一个 ID，支持支出/收入 | 可追溯、可并发、可下钻 | XL | P0 |
| 金额整数模型 | 原币最小单位 + 基准币金额 + 固化汇率 | 消除浮点和历史汇率歧义 | L | P0 |
| 账户管理 | 创建/编辑/归档现金、银行卡、电子钱包 | 知道钱从哪里来、到哪里去 | L | P0 |
| 安全快速记账 | 统一验证日期、金额、账户、分类 | 快且不会写入坏数据 | M | P0 |
| 交易列表与编辑 | 筛选、查看、修改、软删除 | 找得到、改得对、可恢复 | L | P0 |
| 基础分类 | 支出/收入默认分类，可有限自定义 | 符合个人习惯 | M | P1 |
| 月总预算 | 当前月预算、进度、历史月终态 | 控制支出 | M | P1 |
| 基础统计 | 月收支、结余、分类占比、趋势 | 快速了解财务状态 | M | P1 |
| 可靠同步状态 | queued/syncing/synced/error + 重试 | 用户知道数据是否安全 | L | P0 |
| 安全导出与备份 | 规范 CSV/JSON，无公式注入 | 数据可携带、可恢复 | M | P0 |
| 安全基线 | Rules、CSP、输入编码、测试、CI | 达到可发布底线 | L | P0 |

MVP 不包含：银行自动同步、OCR、小票附件、复杂 AI、投资账户、公开社交、会计凭证。

### 5.2 V1：完成双人财务管理闭环

| 功能 | 目标 | 用户价值 | 复杂度 | 优先级 |
|---|---|---|---|---|
| 转账 | 账户间原子双边记录 | 余额准确且不计收入支出 | L | P0 |
| 标签与高级筛选 | 多标签、记录人、账户、金额筛选 | 多维复盘 | M | P1 |
| 分类预算 | 按分类分配预算和阈值 | 更可执行的消费控制 | M | P1 |
| 周期交易 | 房租、工资等自动生成草稿 | 减少重复操作 | L | P1 |
| 月度账户对账 | 账户实际余额、差异、核对状态 | 找到漏记与错记 | L | P0 |
| 变更历史与回收站 | 查看谁在何时改了什么 | 可追责、可恢复 | L | P0 |
| 导入预览与去重 | 规范映射、重复检测、回滚 | 安全迁移旧数据 | L | P1 |
| PWA 离线 outbox | 离线可记，联网可靠补传 | 弱网可用 | L | P1 |
| 预算提醒 | 阈值/到期应用内提醒 | 及时行动 | M | P2 |

### 5.3 V2：智能化与自动化

| 功能 | 目标 | 用户价值 | 复杂度 | 优先级 |
|---|---|---|---|---|
| OCR 小票草稿 | 从图片提取商户、金额、日期 | 降低录入成本 | XL | P1 |
| AI 分类建议 | 基于用户历史推荐分类/标签 | 更快、更一致 | L | P1 |
| AI 财务洞察 | 用可追溯指标解释异常和趋势 | 从“看数据”到“知道原因” | XL | P1 |
| 异常检测 | 重复、异常金额、预算突增提示 | 减少错账与风险 | L | P1 |
| 多基准币报表 | 支持迁居/长期跨币资产口径 | 跨国家庭更准确 | XL | P2 |
| 开放导入连接器 | 银行/电子钱包账单文件适配 | 降低手工录入 | XL | P2 |

AI 前置条件：用户逐次授权、最小化数据、脱敏策略、模型供应商评审、提示注入防护、输出可解释、永不自动落账。当前仓库没有任何 AI 能力，不应在 MVP 先行接入。

## 6. 核心指标

### 北极星指标

每周双方成功记录且完成云端确认的有效天数。

### 产品指标

- 首次价值：既有账号首次登录后 5 分钟内完成首笔交易；
- 记录效率：交易创建耗时 P50/P90；
- 持续使用：每周有效记账天数与连续记账天数；
- 协同：两名使用者每周至少各有一次有效记录的周占比；
- 预算：设置预算后每周查看/下钻的比例；
- 对账：月末成功对平或明确解释差异的账本比例。

### 质量护栏

- 服务端已确认写入率 ≥ 99.99%；
- 未解决同步冲突率 < 0.01%；
- 金额汇总差异必须为 0 个最小货币单位；
- 匿名及第三方 UID 越权测试 100% 拒绝；
- P95 首屏可交互目标 < 2.5 秒（需上线后按真实设备建立基线）；
- 导入必须 100% 生成可下载结果报告与恢复点。

## 7. 范围决策与待确认项

进入开发前，产品所有者必须确认：

1. 基准币默认 VND 是否固定，还是由设置选择后不可变。
2. “连续记账”按交易发生日还是录入操作日；建议按账本时区内的合格交易发生日。
3. 交易删除是否允许永久删除；建议先软删除，30 天后再清理。
4. 数据保留、下载和账号注销需要遵循哪些地区法规；仓库信息不足。

未经确认的项不得由开发 Agent 静默决定。
