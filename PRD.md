# MyExpenseApp 产品需求文档（重规划）

> 本 PRD 基于项目所有者 2026-07-18 补充的真实范围：应用为越南籍女朋友定制，她是唯一日常记账者，账务事实币种为 VND；项目所有者使用第二个既有账号查看/维护，CNY 仅用于显示换算。应用不提供注册、成员管理或多家庭能力。
>
> **实现同步（2026-08-07）：** 当前版本以 legacy 年度账本为事实源，已落地年度分析页、按月独立预算、CNY/VND 显示同步和移动端月表横向滚动。独立账户/交易模型、分类预算和账户级对账仍不属于当前交付。

## 1. 产品定位

### 1.1 一句话定位

为女朋友提供“用越南盾快速记、稳定云同步、预算和连续记账都有反馈”的私人账本，并让项目所有者可按人民币辅助查看。

### 1.2 目标用户

核心使用者：女朋友，越南人，以 VND 记录日常收入、支出、预算和资产。

辅助使用者：项目所有者，主要负责维护和查看；CNY 切换只改变显示，不改变任何账务事实。

暂不服务：企业财务、复式会计、税务申报、投资交易和银行级实时资金管理。

### 1.3 核心价值

1. 低摩擦记录：10 秒内完成一笔收支记录。
2. 可靠反馈：保存状态、连续天数和奖励必须与真实记账行为一致。
3. 展示换算：VND 是唯一事实金额；CNY 仅方便项目所有者理解，不回写或改变历史账。
4. 预算与对账闭环：从记录、预算、趋势到月末/年末资产核对形成行动闭环。
5. 数据可控：可导出、可备份、可恢复、可删除，默认保护隐私。

### 1.4 竞争优势

- 围绕越南本地生活的 VND 记账体验，人民币只做轻量辅助显示；
- “一个人愿意每天记、数据不丢、激励可信”优先于通用财务平台能力；
- 保留 legacy 年度矩阵（`shared_ledger_<year>`）作为日常账务事实源（ADR-003 已确认）；
- 年初/年末资产盘点和差异解释是现有产品的特色，可升级为月度对账。

## 2. 用户画像

### Persona A：日常记账者（女朋友）

- 越南人，日常收入与支出使用 VND。
- 每天手机快速记账，月底在电脑上复盘。
- 痛点：记账后连续天数仍停留 1 天，得不到奖励；担心云端是否真正保存。
- 成功标准：每次记账反馈明确，连续记账和奖励准确，本月花费与预算一眼可见。

### Persona B：查看与维护者（项目所有者）

- 中国人，负责维护应用并查看账务，CNY 换算帮助理解 VND 金额。
- 痛点：不希望为私人应用承担 SaaS、多用户角色或复杂跨币账务的维护成本。
- 成功标准：不改变女朋友的 VND 数据即可切换 CNY 查看，系统结构简单且可验证。

## 3. 产品原则

- VND 事实优先：保存、预算和报表统一以 VND 为事实；CNY 是可丢弃、可重算的 ViewModel。
- 默认安全：账本只对两个既有 Firebase UID 开放，第三方 UID 默认拒绝，敏感操作二次确认并可恢复。
- 金额可解释：每个展示数字都能下钻到交易明细。
- 快速入口一致：快速记账、列表编辑、导入必须遵守同一业务规则。
- 渐进复杂：基础用户只看到必要字段，高级字段按需展开。
- AI 不替用户做账：AI 只能建议分类/解释趋势，用户确认后才写入事实数据。

## 4. 核心用户流程

### 4.1 首次使用

使用既有账号登录 → 打开固定共享账本 → 使用固定四类资产余额字段 → 接受默认支出分类 → 记录第一笔收支 → 查看本月概览 → 可选设置预算。

### 4.2 日常记账

点击“记一笔”或直接编辑当天表格 → 输入 VND 金额 → 选择分类 → 可选备注 → 保存 → 预算与概览更新 → 连续天数重算 → 达到里程碑时奖励 → 云端确认。

目标：熟练用户 10 秒内完成；失败时明确显示“本地待同步”，不得显示虚假的“已同步”。

### 4.3 项目所有者查看

使用第二个既有账号登录 → 读取同一本账 → 可选切换 CNY 显示 → 不改变 VND 原始数据。第二账号是否允许写入以现行 Firebase Rules 为准，本产品不增加应用内角色界面。

### 4.4 月度预算

选择月份并设置该月总预算 → 保存时只更新当前月份 → 查看当月消耗进度 → 切换月份核对各自预算 → 在分析页查看 12 个月预算执行情况。当前版本不提供分类预算或超支分类下钻。

### 4.5 年度分析与月度复盘

进入分析页 → 查看年度收入、支出、结余、储蓄率、记录天数和最高支出日 → 查看月度收支趋势 → 查看支出最高的 5 个分类及“其他”汇总 → 查看年度预算执行 → 查看当前月份详情。分析页只读，所有金额仍以 VND 为事实，CNY 只改变显示。

### 4.6 资产盘点与差异查看

查看固定四类资产的期初/期末余额 → 查看理论资产、实际资产和差异 → 必要时直接修正余额或补记账务。当前版本不提供账户级未核对交易、对账会话或自动调整交易。

### 4.7 数据导入与恢复

选择规范文件 → 本地验证 → 预览新增/重复/冲突/拒绝项 → 用户选择合并策略 → 自动生成恢复点 → 执行导入 → 显示结果报告 → 必要时一键回滚。

## 5. 功能规划

复杂度口径：S ≤ 1 人日，M = 2–5 人日，L = 1–2 周，XL > 2 周；这是产品级粗估，不替代任务计划。

### 5.1 MVP：先把现有原型变成可信账本

| 功能 | 目标 | 用户价值 | 复杂度 | 优先级 |
|---|---|---|---|---|
| 固定双人访问控制 | 两个既有账号可访问，任何第三方账号拒绝 | 私人账务不外泄 | S | P0 |
| 连续记账与奖励修复 | 所有有效录入入口得到同一 streak，7/30 天只奖励一次 | 恢复每日使用动力 | S | P0 |
| VND 事实金额 | VND 整数保存，CNY 仅展示换算 | 避免显示币污染账务 | M | P0 |
| 安全快速记账 | 统一验证日期、金额、分类、备注 | 快且不会写入坏数据 | M | P0 |
| 固定支出分类 | 使用 10 个固定支出分类和收入字段 | 保持越南日常记账简单 | S | P1 |
| 按月独立预算 | 每月独立保存总预算，切换月份互不覆盖 | 控制支出且预算可解释 | M | P1 |
| 年度分析页 | 年度概览、月度趋势、分类构成、预算执行、月度详情 | 快速了解财务状态 | M | P1 |
| 可靠同步状态 | queued/syncing/synced/error + 重试 | 用户知道数据是否安全 | L | P0 |
| 安全导出与备份 | 规范 CSV/JSON，无公式注入 | 数据可携带、可恢复 | M | P0 |
| 安全基线 | Rules、CSP、输入编码、测试、CI | 达到可发布底线 | L | P0 |

MVP 不包含：银行自动同步、OCR、小票附件、复杂 AI、投资账户、公开社交、会计凭证。

### 5.2 V1：完善私人账本闭环

| 功能 | 目标 | 用户价值 | 复杂度 | 优先级 |
|---|---|---|---|---|
| 转账 | 账户间原子双边记录 | 余额准确且不计收入支出 | L | P0 |
| 标签与高级筛选 | 多标签、记录人、账户、金额筛选 | 多维复盘 | M | P1 |
| 分类预算 | 按分类分配预算和阈值 | 更可执行的消费控制 | M | P1 |
| 周期交易 | 房租、工资等自动生成草稿 | 减少重复操作 | L | P1 |
| 月度账户对账 | 账户实际余额、差异、核对状态 | 找到漏记与错记 | L | P0 |
| 变更历史与回收站 | 查看谁在何时改了什么 | 可追责、可恢复 | L | P0 |
| 导入预览与去重 | 规范映射、重复检测、回滚 | 安全迁移旧数据 | L | P1 |
| PWA 离线 outbox | 离线可记，联网可靠补传 | 弱网可用 | L | P1 |
| 预算提醒 | 阈值/到期应用内提醒 | 及时行动 | M | P2 |

独立 Account/Transaction 模型不在已批准版本范围内。T019/ADR-003 已确认：稳定 legacy 年度矩阵，T011/T012 不移入交付主线。本节中涉及账户、交易模型的功能（如转账、周期交易、月度对账）属于未来可能方向，需要新的 owner-approved ADR 方可重新评估。

当前版本同样不实现分类预算；预算以月份为独立配置单位，分类构成只用于分析展示。

### 5.3 V2：智能化与自动化

| 功能 | 目标 | 用户价值 | 复杂度 | 优先级 |
|---|---|---|---|---|
| OCR 小票草稿 | 从图片提取商户、金额、日期 | 降低录入成本 | XL | P1 |
| AI 分类建议 | 基于用户历史推荐分类/标签 | 更快、更一致 | L | P1 |
| AI 财务洞察 | 用可追溯指标解释异常和趋势 | 从“看数据”到“知道原因” | XL | P1 |
| 异常检测 | 重复、异常金额、预算突增提示 | 减少错账与风险 | L | P1 |
| 开放导入连接器 | 银行/电子钱包账单文件适配 | 降低手工录入 | XL | P2 |

AI 前置条件：用户逐次授权、最小化数据、脱敏策略、模型供应商评审、提示注入防护、输出可解释、永不自动落账。当前仓库没有任何 AI 能力，不应在 MVP 先行接入。

## 6. 核心指标

### 北极星指标

女朋友每周成功记录并完成云端确认的有效记账天数。

### 产品指标

- 首次价值：既有账号首次登录后 5 分钟内完成首笔交易；
- 记录效率：交易创建耗时 P50/P90；
- 持续使用：每周有效记账天数与连续记账天数；
- 激励正确性：连续 7/30 天测试场景奖励触发率 100%，同一里程碑不重复奖励；
- 预算：每月预算修改不影响其他月份；年度预算等于 12 个月有效预算之和；分析页可查看年度执行率；
- 资产差异：能够查看理论资产、实际资产和差异，不将其表述为已完成的账户级自动对账。

### 质量护栏

- 服务端已确认写入率 ≥ 99.99%；
- 未解决同步冲突率 < 0.01%；
- 金额汇总差异必须为 0 个最小货币单位；
- 匿名及第三方 UID 越权测试 100% 拒绝；
- P95 首屏可交互目标 < 2.5 秒（需上线后按真实设备建立基线）；
- 导入必须 100% 生成可下载结果报告与恢复点。

## 7. 范围决策与已确认项

### 7.1 已确认

1. **VND 是唯一账务事实币种**；CNY 仅为显示换算，不回写任何金额。
2. **女朋友是唯一日常记账者**；项目所有者使用第二个既有 Firebase 账号查看/维护。
3. **无注册、成员管理或多家庭能力**；账号增删由项目所有者在 Firebase 控制台处理。
4. **Legacy 年度矩阵是日常账务事实源**（ADR-003）；T011/T012 不进入交付主线，不迁移。
5. **UI 默认越南语**，可切换简体中文，不提供英语。
6. **储蓄目标**存于对应年度文档的 `settings` key。
7. **存款数据**存于固定 `shared_ledger_savings` 文档。
8. **提醒仅在应用打开或恢复前台时触发**，不依赖外部推送或定时服务。
9. **ADR-004** 已将上述决策固化为正式架构契约。
10. **月预算按月独立保存**：运行时优先读取 `settings.budget_1` 至 `settings.budget_12`；旧 `monthlyBudget` 和旧 localStorage 值只作为兼容回退，保存新预算不会更新全局旧键。
11. **年度分析页只读**：从年度 `entries` 和 `settings` 派生 12 个月趋势、年度概览、前五分类及其他汇总、预算执行和当前月详情；年度预算是 12 个月有效预算之和。
12. **移动端月表保留横向滚动**：备注列最小宽度为 128px，保证长备注至少有约 8 个中文汉字的可视空间，不通过压缩其他金额列解决。

### 7.2 仍需根据现行 Rules 确认

第二个 Firebase 账号线上是否只读，需核对现行 Rules；在此之前不修改权限。

> 未经确认的项不得由开发 Agent 静默决定。上述已确认项已由 ADR-003 和 ADR-004 正式记录，后续 Task 必须遵守。
