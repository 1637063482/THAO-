# MyExpenseApp 功能需求设计

> 本 FRD 描述目标系统。女朋友是唯一日常记账者，VND 是账务事实币种；项目所有者通过第二个既有账号查看/维护，CNY 仅为显示换算。账号、数据和云同步由 Firebase 管理，应用不提供注册或成员管理。
>
> **⚠ 范围说明：** FRD 包含部分面向通用 Account/Transaction 模型的远期功能描述。T019/ADR-003 已确认：稳定 legacy 年度矩阵，T011/T012 不移入交付主线。ADR-004 进一步固化了 UXS 阶段的产品与架构边界。以下 FR 标记为 **Out of Scope** 的部分属于被 ADR-003 拒绝或延期至新 ADR 的功能，当前版本不实施。以 `UI_SAVINGS_REDESIGN_PLAN.md` 和 `TASK_PLAN.md` 为唯一施工依据。
>
> **实现同步（2026-08-07）：** 当前运行时以 `shared_ledger_<year>` 的 `entries/balances/settings` 为事实源。年度分析、按月独立预算、CNY/VND 显示刷新和移动端月表横向滚动已实现；独立 Account/Transaction、分类预算、账户级对账、通用离线 outbox 仍不属于当前交付。

## FR-01 身份认证与账号生命周期

- 功能名称：既有账号登录、退出、会话保护
- 现状：Firebase 邮箱密码登录已存在；没有邮箱验证、密码重置、账号注销页面。
- 业务规则：应用只提供两个既有账号登录，不提供应用内注册；敏感操作需近期重新认证；会话超时只锁定本地 UI，不替代服务端授权；账号增删由项目所有者在 Firebase 控制台处理。
- 输入：邮箱、密码、认证动作、可选重认证凭证。
- 输出：认证用户、稳定 uid、验证状态、用户可理解的错误码。
- 数据变化：本应用不创建账号或用户资料；不在业务库保存密码。
- 接口需求：Firebase Auth SDK；`GET/UPSERT user profile` 由 Firestore + Rules 或受信后端承载。
- 异常情况：账号不存在、密码错误、限流、离线、token 过期、已登录但不在共享账授权名单。
- 权限要求：用户仅可管理自己的资料；管理员不能读取密码或 token。
- 测试要求：认证状态恢复、超时跨刷新、错误码映射、未验证访问、注销所有权阻断、账号枚举防护。

## FR-02 固定共享账本访问

- 功能名称：两个既有账号访问唯一共享账本
- 现状：客户端固定访问 `shared_ledger_<year>`；账号和线上登录规则已由项目所有者配置；页面“邀请”仅复制网址，不负责授权。
- 业务规则：只有项目所有者在 Firebase 配置的两个既有 UID 可以访问；第三个 UID 即使通过 Auth 登录也不得访问。女朋友是日常记账者，项目所有者主要查看；两账号的实际写权限以线上 Rules 为准，不在客户端另建角色系统。
- 输入：Firebase Auth 的当前用户身份、目标年度。
- 输出：允许访问固定年度账本，或返回不泄露账务内容的无权限状态。
- 数据变化：本功能不创建成员、邀请或角色数据；账号授权变更由项目所有者在 Firebase 配置中操作。
- 接口需求：Firebase Auth 状态监听、Firestore 固定账本路径、Security Rules。
- 异常情况：未登录、token 过期、第三方 UID、Rules 与客户端路径不一致、授权配置误改。
- 权限要求：客户端提示不能替代 Firestore Rules；不得在未读取线上规则时假设第二账号与女朋友写权限相同。
- 测试要求：Firestore Emulator 覆盖匿名、授权 A、授权 B、第三方 UID；线上 Rules 需人工核对，自动测试不得写入真实 UID。

## FR-03 账户与钱包

> **⛔ OUT OF SCOPE per ADR-003/ADR-004。** 当前版本不实现独立账户管理。legacy 四项资产类型（银行卡/支付宝/微信/其他）的年初年末余额字段保持现状，不做账户 CRUD、余额计算或对账。

- 功能名称：账户管理
- 现状：仅有银行卡/支付宝/微信/其他四类年初年末余额字段。
- 业务规则：账户属于固定账本；事实币种固定 VND；类型含 cash/bank/e_wallet/other；有期初余额、排序和归档状态；有交易的账户不可硬删除；余额由期初余额和交易计算。
- 输入：名称、类型、币种、期初余额、期初日期。
- 输出：账户详情、计算余额、最后对账时间。
- 数据变化：创建/更新/归档 account；变更期初余额写审计事件并触发重算。
- 接口需求：账户 CRUD、账户余额查询、归档/恢复。
- 异常情况：重复名称、非法币种/金额、存在交易时删除、引用不存在的账户。
- 权限要求：按线上 Rules；女朋友必须可完成日常记账，第二账号是否可配置账户待 T017 核对。
- 测试要求：整数余额、归档后历史可见、跨币交易影响、权限、并发更新版本。

## FR-04 交易记录

> **⛔ OUT OF SCOPE per ADR-003/ADR-004。** 当前版本不实现独立交易模型。日常记账使用 legacy `entries` 矩阵单元格进行同日同分类聚合。快速记账和直接编辑继续写回同一个 legacy cell，不生成逐笔交易 ID、账户字段或 immutable audit trail。

- 功能名称：创建、查看、修改、删除收入/支出
- 现状：同日同分类聚合为公式，没有交易 ID、账户和创建人。
- 业务规则：每笔交易有不可变 ID、类型、账户、VND 金额、发生时间、分类、创建人；金额必须为正整数，方向由 type 表示；CNY 展示值不持久化为交易事实；修改采用版本号；删除为软删除。
- 输入：type、accountId、amountVnd、occurredAt、categoryId、tags、note、idempotencyKey。
- 输出：规范 transaction、syncState、version。
- 数据变化：新增/更新 transaction；账户余额和统计由查询/投影更新；产生 audit event。
- 接口需求：create/get/list/update/delete/restore；列表支持 cursor 分页与组合筛选。
- 异常情况：重复幂等键、账户归档、非法日期/金额、版本冲突、离线、账号授权已撤销。
- 权限要求：两个授权账号均可创建和修改交易；`createdBy/updatedBy` 必须取自认证上下文并用于追溯。
- 测试要求：幂等、整数精度、时区边界、并发冲突、软删除、分页稳定性、权限矩阵。

## FR-05 转账

> **⛔ OUT OF SCOPE per ADR-003/ADR-004。** 当前版本不实现账户间转账。转账需要独立 Account 和 Transaction 模型，属于被 ADR-003 拒绝的路线。

- 功能名称：账户间转账
- 现状：不存在。
- 业务规则：来源与目标账户不同；同币转账金额守恒；跨币转账同时保存两端金额与汇率；转账不计入收入/支出；两端必须原子创建/撤销。
- 输入：fromAccountId、toAccountId、fromAmountMinor、toAmountMinor、occurredAt、fx metadata、note。
- 输出：transfer 及关联双边 entries。
- 数据变化：原子写 transfer 与两端账务影响；写审计事件。
- 接口需求：create/update/delete transfer；失败必须整体回滚。
- 异常情况：同账户、币种不匹配、非正数、任一账户归档、部分失败、版本冲突。
- 权限要求：与交易创建相同，且必须同时拥有两账户访问权。
- 测试要求：守恒、跨币四舍五入策略、原子性、重试幂等、删除恢复。

## FR-06 分类与标签

> **当前交付边界：** 当前运行时提供 10 个固定支出分类和一个 income 字段，不提供分类/标签 CRUD。下列 CRUD、归档和标签接口属于未来扩展，不能作为当前版本已实现接口。

- 功能名称：分类管理、交易标签
- 现状：10 个支出分类硬编码；无收入分类和标签。
- 业务规则：系统默认分类在首次初始化时写入固定共享账本；分类有 income/expense 适用范围；已使用分类只可归档；标签名称在账本内按规范化值唯一。
- 输入：名称、图标、颜色、适用类型、父分类（V1 可不启用）、标签。
- 输出：可用/已归档分类和标签。
- 数据变化：category/tag CRUD；交易只引用 ID，历史名称通过当前值或审计快照解释。
- 接口需求：列表、创建、更新、排序、归档；标签搜索。
- 异常情况：重名、类型不匹配、删除已引用分类、超长/危险文本。
- 权限要求：按线上 Rules；不在客户端假定两个账号同权。
- 测试要求：规范化唯一性、归档引用、XSS 文本、类型约束、权限。

## FR-07 预算

- 功能名称：按月独立总预算与预算执行
- 现状：预算保存在年度账本 `settings` 中，优先使用 `budget_1` 至 `budget_12`；旧 `monthlyBudget` 和旧 localStorage 值只作为预算编辑控件的兼容回退，分析模型不把新月份写回旧全局键。
- 业务规则：预算属于账本和自然月，以 VND 整数保存；保存某月只更新该月的 `budget_<month>`，不得改变其他月份；没有独立配置的月份使用兼容基线/默认预算；当前版本不提供分类预算。CNY 输入在保存边界按当前有效汇率转换回 VND，CNY 仅影响展示。
- 输入：month、当前币种下的总预算金额、当前展示汇率。
- 输出：当前月已用、剩余、进度和提示；分析页输出 12 个月预算执行情况。
- 数据变化：更新 `settings.budget_<month>` 与对应 `pendingUpdates.settings`，通过现有云保存链路写回年度账本。
- 接口需求：按月读取/保存预算、预算进度刷新、年度预算执行查询。
- 异常情况：空值、负数/非法表达式、CNY 汇率不可用、历史月份修改、年度切换。
- 权限要求：两个授权账号均可查看和编辑。
- 测试要求：修改一个月份不影响其他月份；旧数据回退；年度预算求和；VND/CNY 保存边界；预算进度和无数据状态。

## FR-08 统计分析与报表

- 功能名称：年度概览、月度趋势、分类构成、预算执行与月度详情
- 现状：分析页从当前年度 legacy `entries` 和 `settings` 派生只读 ViewModel，包含年度总览、月度收支趋势、支出最高的 5 个分类及“其他”汇总、预算执行和当前月份详情；不提供账户/记录人维度和服务端筛选。
- 业务规则：所有事实金额使用 VND；CNY 只在展示层换算，切换币种不得改写 `entries/balances/settings`。年度预算为 12 个月有效预算之和；年度执行率为年度支出除以年度预算；预算内月份只统计有记录的月份。分析中非空备注也可使对应日期成为“有记录天数”，但不计入收入/支出。
- 输入：年度、当前月份、legacy `entries`、`settings`、当前展示币种及汇率。
- 输出：年度收入/支出/结余、储蓄率、有记录天数、有支出天数、支出日均、最高支出日、年度分类构成、月度趋势、预算执行和当前月收支/分类详情。
- 数据变化：只读，不写入账本，不生成独立聚合投影。
- 接口需求：本地 `buildAnalyticsViewModel`、分析视图渲染/刷新和 Chart.js 图表；空数据必须显示可理解的空状态。
- 异常情况：空年度、非法或未知 entry key、无预算、汇率不可用、年度/月份边界。
- 权限要求：两个授权账号可见；敏感导出可要求近期重新认证。
- 测试要求：年度/月度汇总、分类 Top 5 与其他汇总、年度预算求和、币种说明刷新、空状态、Canvas 不被文字刷新替换。

## FR-09 对账

> **⛔ OUT OF SCOPE per ADR-003/ADR-004。** 当前版本不实现账户级对账。legacy 年初/年末四类总资产差异说明保持现状。

- 功能名称：账户余额盘点与差异处理
- 现状：只做年初/年末四类总资产差异。
- 业务规则：对账以账户和截止时间为单位；系统余额 = 期初 + 截止日有效交易；实际余额由用户输入；差异不可被自动抹平；可创建调整交易但需明确标记。
- 输入：accountId、statementAt、actualBalanceMinor、note。
- 输出：system balance、actual balance、difference、未核对交易。
- 数据变化：reconciliation session、交易核对标记、可选 adjustment transaction。
- 接口需求：开始/完成对账、列出未核对项、创建调整。
- 异常情况：截止后交易、并发修改、账户币种不一致、历史对账重开。
- 权限要求：两个授权账号均可完成、重开和标记对账。
- 测试要求：截止时间、差异方向、后续交易隔离、调整交易可追溯、并发版本。

## FR-10 CNY 辅助显示

- 功能名称：VND/CNY 显示切换
- 现状：Firestore 保存 VND；CNY 通过当前自动/手动汇率换算显示，切换时同步刷新账表、预算、储蓄和分析页，分析趋势说明显示当前币种。
- 业务规则：所有持久金额、预算和报表事实均为 VND 整数；CNY 只在 ViewModel/显示层按当前汇率计算，切换显示不得改写 `entries/balances/settings`；手工汇率只影响查看和 CNY 输入转换。
- 输入：VND amount、当前展示币种、当前有效汇率及来源。
- 输出：VND 原值或 CNY 辅助显示值，以及与当前币种一致的分析单位说明。
- 数据变化：不创建交易汇率快照，不因切换币种写入账务事实；可缓存最后有效展示汇率。
- 接口需求：获取当前 CNY→VND 汇率、手工展示覆盖、缓存读取。
- 异常情况：服务超时、日期无报价、零/负汇率、货币精度不同、离线。
- 权限要求：自动汇率只读；交易手工汇率随交易权限；修改留审计。
- 测试要求：切换 CNY 前后 Firestore 待写数据完全不变；VND/CNY round-trip；预算输入转换；分析单位说明随币种刷新；舍入、超时回退和汇率不可用提示。

## FR-11 导入、导出、备份与恢复

- 功能名称：安全数据可携带
- 现状：覆盖式 JSON 导入与不规范 CSV 导出。
- 业务规则：导入必须 schema 验证、预览、去重、确认、恢复点、结果报告；导出 CSV 不包含可执行公式；JSON 带 schemaVersion、ledgerId、exportedAt 和校验摘要；恢复只能作用于固定共享账本。
- 输入：文件、映射、冲突策略、目标账本。
- 输出：预览统计、错误行、导入批次、导出文件、恢复结果。
- 数据变化：import batch、backup snapshot/reference、transactions；全程审计。
- 接口需求：validate/preview/execute/rollback import；export job；list/restore backup。
- 异常情况：超限、编码、恶意公式、重复 ID、部分错误、网络中断、恢复期间新写入。
- 权限要求：两个授权账号可导出；覆盖/恢复要求近期认证和明确二次确认。
- 测试要求：RFC 4180、CSV injection、中文/越南文、换行、1 万行性能、幂等、部分失败、回滚。

## FR-12 离线与同步

> **当前交付边界：** legacy 运行时使用 `pendingUpdates` 汇总 `entries/balances/settings` 的待写变更并通过现有云保存链路提交；本节所述通用 operation outbox、版本冲突合并和多端事务同步尚未实现，仍需独立需求和 ADR。

- 功能名称：可靠 outbox 和多端同步
- 现状：800 ms pending map + 3 秒伪成功超时。
- 业务规则：本地操作先获得 clientOperationId；状态为 local/queued/sending/confirmed/conflict/failed；只有服务端确认可显示已同步；重试幂等；撤权后停止上传并提示；冲突不静默覆盖。
- 输入：领域命令、operationId、baseVersion、网络状态。
- 输出：交易及同步状态、可重试错误、冲突详情。
- 数据变化：本地 outbox、服务端 transaction/audit；确认后清理 outbox。
- 接口需求：enqueue/flush/retry/discard；订阅远端变更；等待全部确认。
- 异常情况：离线、页面关闭、超时、重复提交、版本冲突、权限撤销、配额错误。
- 权限要求：服务端仍逐次验证，不信任离线时旧权限。
- 测试要求：断网重开、乱序确认、重复重试、多标签页、多设备同字段、配额/权限失败。

## FR-13 连续记账与激励

- 功能名称：连续记账天数
- 现状：已从当前年度 legacy `entries` 按越南时区派生连续记账天数；直接表格编辑、快速记账、删除和云端快照刷新后统一重算，旧 streak settings 仅保留兼容用途。
- 业务规则：按 `Asia/Ho_Chi_Minh` 本地日，从当前年度 `entries` 中存在至少一笔可计算且非零收入或支出的日期派生；收入与支出均算有效记账；同日多笔只算一天；历史补录只改变对应历史日期；连续天数不是可直接修改的事实字段。
- 输入：ledgerId、date、eligible transactions。
- 输出：current streak、today status、里程碑。
- 数据变化：近期不新增事实字段；旧 `expense_streak/expense_last_date` 暂留兼容但停止作为计算依据。成就事件按里程碑去重。
- 接口需求：纯函数 `buildStreak(entries, today, timezone)`；所有录入、删除、云端快照后统一重算。
- 异常情况：跨午夜、时区更改、离线补传、删除/恢复、并发首笔交易。
- 权限要求：两个授权账号可见，不允许直接写 streak 数字。
- 测试要求：跨午夜、昨天/今天、历史补录、删除回算、并发、时区。

## FR-14 AI 能力（V2，受控）

- 功能名称：分类建议与财务洞察
- 现状：不存在任何 AI 接入。
- 业务规则：默认关闭；用户每个场景明确授权；只发送完成任务所需最少字段；模型输出是建议，不直接写交易；每条洞察必须链接到确定性指标/交易筛选；不得给出投资、税务或医疗结论。
- 输入：脱敏交易特征、用户问题、账本设置、授权范围。
- 输出：建议分类及置信度、可验证洞察、引用的内部指标 ID。
- 数据变化：可选用户确认后的分类；AI 请求审计不保存不必要的原始财务文本。
- 接口需求：受信服务端代理，不从浏览器暴露模型密钥；配额、内容安全、供应商切换。
- 异常情况：提示注入、幻觉、服务不可用、低置信度、敏感数据、超预算。
- 权限要求：只有两个授权账号可使用；服务端按固定账本重新鉴权并坚持数据最小化。
- 测试要求：数据最小化、提示注入、防越权、低置信度不落账、确定性引用、供应商超时与降级。

## 统一非功能要求

- 正确性：金额全链路整数；关键聚合必须有逐笔对账测试。
- 安全：默认拒绝 Rules、CSP、无用户数据 `innerHTML`、无客户端密钥、依赖审计。
- 性能：交易列表 cursor 分页；首屏不加载非必要图表/动画；建立真实设备指标。
- 可用性：保存状态可理解；危险操作可撤销；键盘和移动端均可用。
- 响应式：legacy 月表在小屏容器内横向滚动；备注列最小宽度 128px，保证长备注有至少约 8 个中文汉字的可视空间，不压缩金额列。
- 可观测性：错误码、operationId、ledgerId 哈希、版本和耗时；禁止记录明文备注、邮箱和 token。
- 可迁移：所有持久对象带 `schemaVersion/createdAt/updatedAt`；迁移可重入、可校验、可回滚。
